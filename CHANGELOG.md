# Changelog

All notable changes to this repository are documented here. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Crate versions follow [Semantic Versioning](https://semver.org); the architecture whitepaper carries its own version in its front matter.

This file is a historical record: `spec-graph` treats it as history, so nothing written here is an obligation.

## [Unreleased]

### Added

- **Brief 042: the active set, measured ([ADR-0097](docs/adr/0097-the-active-set-measured.md)); finding F-49 opened; the budget Appendix A's three targets must fit written for per-tick service and for service on arrival, and no integration model chosen; no rule of the engine changed; image format 16 unchanged; the determinism pin untouched; no pinned number moved.** The runtime's `executor.rs`: `Executor::turns()`, the turns every worker has served, counted after each turn in the turns phase and published beside `delivered` at the end of the deliveries phase; it reads and writes nothing the engine does. `tests/active.rs` (a binary on the instrument's shared harness): an oracle of one message's ticks to rest written from `membrane.rs`'s rule and committed before the engine ran — the engine agrees, 2 210 turns at the gain 1.0 and 2 503 at 1.75, against the brief's estimate of about 1 900, which took the one-LSB tail as 512 ticks where it is 1 023; the prediction $1 - e^{-rD}$ in integers, committed before the runs; and four weekly `exhaustive` runs on ADR-0044's prior under the instrument's configuration, every tick's turns held to the units whose gate the tick before left scheduled, each at 1 024 units beside a control wired to nothing. Under ADR-0044's drive the executor serves **99.99 per cent** of the units on every tick at 1 024 and at 4 096 units while 0.0018 per cent fire (1.73 to 1.82 Hz a unit); the control is served on every tick without exception, so the network's own messages lower the fraction slightly. Sixteen and 256 times sparser the network never fires: 71.9 to 72.1 per cent against the floor's 70.54, and 7.35 to 7.41 against 7.35. The bench, run whole on a developer machine (`docs/benchmarks/results/2026-09-25-dancr-win11.md`, not admissible): a turn's gate and integration about 21 ns, a message 4.7 ns; the runs' own wall time 24 to 25 ns of one worker per turn. The budget: per-tick service at ADR-0044's density reaches about 26 000 units in real time on 64 workers, or runs 43 million about 1 700 times slower than real time with 275 TB/s of arena traffic; service on arrival pays one touch per message where per-tick pays $(1 - e^{-rD})/r$ turns, at most $\min(D, 1/r)$, but is cheaper only with a catch-up over a unit's quiet ticks that costs less than integrating each of them, which the tree does not have for the present rule (a closed form is the most evident such catch-up, not shown to be the only one), and at ADR-0044's density it too runs 43 million units about 14 times slower. F-49 records that §1.1's premise and ADR-0023's "a unit at rest costs nothing" describe an executor whose cost follows activity, and that Appendix A's unit count, real-time tick and input density cannot be met together by the executor as built; §1.1's sentence and Appendix A's table are not edited, and a paragraph under the table points to F-49. The integration model is an open question in §11.1 for the next ADR. Whitepaper 4.49.0.
- **Brief 041: the punished pair, built and measured ([ADR-0094](docs/adr/0094-the-signed-gate-built.md), [ADR-0095](docs/adr/0095-an-image-pin-moves-with-its-format.md), [ADR-0096](docs/adr/0096-the-punished-pair-measured.md)); H-18 is yes, its stopping rule at step 3, the configuration the engine learns and revises in named and the next decision named and not taken; image format 16; the determinism pin untouched; no constant of the instrument or of H-18 moved.** The mechanism ADR-0093 decided, built: `cortex-core`'s `SynapseBlock::consolidate_signed` beside `consolidate`, which is untouched — at and above zero `consolidate` call for call, below zero the weight moved against its trace's sign by `round(|trace| × |m|)` and the trace spent by what the magnitude moved, so the magnitude less the trace is conserved and no trace grows; `cortex-neuromod`'s `signed_modulation`, the sum clamped to [−1, 1]; `Config::signed_gate: bool`, unset by default, which makes the addressed word the coordinator already publishes signed, an addressed inhibitory slot taking it at no less than zero, so the sign reaches an addressed excitatory synapse and no other; unset, every run is the run it was, bit for bit, with no new word on the tick. The image's modulator section carries it as a flag byte at `[25]`, anything but 0 or 1 refused, and `CortexFileHeader::FORMAT_VERSION` moves from 15 to **16**; a version-15 header is refused as every foreign version. Tests: the rule at its edges and over the lattice with an `i64` oracle, the signed modulation over the lattice, the configuration and the published word, the modulations over the lattice, a fork of two synapses whose punished weights move against their traces exactly as an oracle block consolidates and do not move unset, the image's byte and its refusals. One pinned number moved with the format and not with the gate — H-17's whole-image CRC, whose header carries the version — and ADR-0095 amended H-18's stopping rule for it before any run: the numbers that stop a round are the engine's and the records', and the H-17 test now holds its image, with the header written back to 15, to the CRC H-17 read, which CRC-64's linearity showed before the run. H-18 run once in `tests/inhibition.rs` on the shared harness, whose oracle gains the signed branch (`consolidated_signed`, `earned_run_signed`, `Composer::moves`): the calibration reproduced ADR-0077's settled candidate with the gate unset; the two arms, 4 608 trials each from the signed image — nine bytes from the inhibited one — under the task as built, the mapping flipped at the 1 536th trial. Read: **128 of the last 128 before the flip and 128 of the last 128 at the 4 608th trial, in both arms**; in the first half the wrong pairs depressed to 0.96 to 0.98 of the image's while the answer pairs rose to 1.37 to 1.44; after the flip the old answer's pairs, punished against a trace of +5.7 to +9.5 per synapse per trial, fell by 9 070 to 13 310 a punished trial at first, slowing, and came back to 0.95 to 1.06 of the image's, each stimulus's selection crossing in the fourteenth to the twentieth block after the flip, while the new answer's pairs rose to 1.41 to 1.55 and still rising; a punishment potentiated 18 to 24 per cent of the addressed synapse-trials it reached, those whose trace was negative, and depressed slightly more; ADR-0093's three predicted readings held; the assertion held — no excitatory synapse outside the four pairs moved and the oracle equalled the record at every trial; the inhibitory sum stopped falling at 0.110 and 0.114 of the image's; the sight 60 to 64 and the stimulus firing once in every block. The gate gains nine tests — two in `cortex-core`, two in `cortex-neuromod`, two in the executor and one each in `tests/modulation.rs`, `tests/image.rs` and `tests/inhibition.rs` — and 635 pass in the debug profile where 626 did, 53 ignored where 51 were; the two arms are two weekly `exhaustive` tests. Whitepaper 4.46.0 to 4.48.0: §1.6, §5.2.1, §5.2.2, §5.2.14, §6.5's loop, §8.7, §8.8's three-factor row (its formula, garbled into tab characters since 4.20.0, repaired), §9, §11.1's H-18 checked yes and its stopping rule at step 3, the operating regime's item; the ADR index, ADR-0086's row moved to its place; `README.md`, `CLAUDE.md` and the Traditional Chinese reader's guide; brief 041 archived.
- **The punished pair consolidates against its trace ([ADR-0093](docs/adr/0093-the-punished-pair-consolidates-against-its-trace.md)); H-18 written with its criterion, its predicted readings and its stopping rule before any run; brief 041; whitepaper 4.45.0; no code, no constant and no test changed.** H-17's stopping rule left a choice between an exploration in the selection and a depression under the gate, with ADR-0091's readings as the need. This takes the depression: every one of the 1 534 punished trials after the flip already addresses the old answer's pair, whose trace holds +5 to +10 per synapse per trial, while an exploration would need about six hundred rewarded selections of the new answer against a lead one reward moves by at most 3 415. The rule decided: a signed gate, a parameter of the image, under which an addressed excitatory synapse consolidates under `clamp(baseline + dopamine, −1, 1)`; a negative modulation moves the weight against the trace's sign and spends the trace by what the weight moved — neither leaving the trace nor applying the conservation rule to a negative transfer, both of which run away. It is the signed reward-modulated STDP of Florian (2007) and Frémaux et al. (2010), which potentiates a punished synapse whose trace is negative; the round reads how often. H-18 is H-17's configuration with the signed gate set over 4 608 trials, the second mapping 3 072 long, a length derived from ADR-0091's lead and trace; no prediction for the verdict. `briefs/041_the-punished-pair.md` builds the mechanism (format 16) and runs H-18, and regenerates the weekly cost table from its own dispatch (ADR-0092).
- **The whole-domain shards dealt by cost ([ADR-0092](docs/adr/0092-the-shards-dealt-by-cost.md), amending ADR-0073 and ADR-0084); no code of the engine changed.** ADR-0091's dispatch read the four shards at 22, 66, 43 and 34 per cent of their bound: the round robin, which reads names, put H-17's mirrored arm and seven of `instrument`'s tests in one shard. `scripts/exhaustive-costs.mjs` (tested, `npm run spec:costs:test`) now deals the tests `--list` names longest first to the least-loaded shard, costed by `scripts/exhaustive-costs.tsv` — a test it does not know at 900 s, as a heavy one — and `scripts/exhaustive-shard.sh` runs each test of a shard in a process of its own, as many at once as the runner has cores, so each test's seconds are its own and the table can be made from them (`exhaustive-costs.mjs from <dir>`). No whole-domain test shares a fixture with another, so the process costs a start and nothing else, and libtest on stable reports no test's time any other way. A test must exit 0 and report one test passed: a name `--exact` does not match runs nothing and is failed. The table decides the balance and never the coverage, and `npm run spec:costs` fails on a line that names no test in the tree. Appendix B's line, which still read "three shards" after ADR-0088, is rewritten.
- **Brief 040: the assignment reversed, measured ([ADR-0090](docs/adr/0090-the-old-answer-held-from-the-trial-after-the-flip.md), [ADR-0091](docs/adr/0091-the-assignment-reversed-measured.md)); H-17 is no, its stopping rule at step 4, the next decision named with the measured need and not taken; no code of the engine changed; the determinism pin untouched; no constant of the instrument or of H-17 moved.** Reading the engine before the run, the round found ADR-0089's assertion one trial early: `Task::trial` writes the addressed set and the reward between trials and `Executor::address` holds the set until it is written again, so the first trial under the second mapping still consolidates the last reward of the first. ADR-0090 amended the assertion before any run — the old answer's pairs held from the end of the 1 537th trial, the pair the 1 536th did not carry from the end of the 1 536th, no excitatory synapse outside the four stimulus–readout pairs moved — and changed nothing else of H-17. The shared harness gains `run_on_flipped` and `earned_run_flipped`, which negate the task's `mirrored` before one trial and touch nothing else; `run_on` and `earned_run_under` call them with no flip and are the runs they were. H-17 runs in `tests/inhibition.rs` beside H-16, its two arms two weekly tests: the calibration reproduced ADR-0077's settled candidate before each rewarded run, and the first half of each arm is H-16's arm bit for bit, held to its tables. Read: **128 of the last 128 correct before the flip and 0 of the last 128 after it, in both arms**; over the 1 536 trials after the flip the other readout was selected 2 and 1 times, with 2 and 1 rewards and 0 and 1 ties, the old answer's readout counting 2.2 to 3.2 times the new's, 16 to 24 spikes a trial; the old answer's pairs, punished 1 534 times in each arm, did not move while their trace went on pairing +5 to +10 per synapse per trial; each reward raised its new pair by 162 to 3 415 against a lead of 2.2 to 3.0 million; the carry-over moved 69 and 68 synapses of B's old pair in the 1 537th trial and nothing else, the assertion as amended holding in both arms; the inhibitory sum went on falling, to 0.17 of the image's. The gate gains one test: the rules at their edges, a few trials over a flip on the instrument's network beside the same run without one — where the carry-over shows too, 560 synapses of A→R0 moving in the first trial after the flip — and the checks over the pinned tables, each coupling after a block the one before it plus what the oracle consolidated. 626 tests pass in the debug profile where 625 did. The next decision, by H-17's stopping rule, is an ADR choosing between an exploration in the selection and a depression under the gate, with these readings as the need each must meet. Whitepaper 4.42.0 and 4.43.0; brief 040 archived.
- **The assignment reversed ([ADR-0089](docs/adr/0089-the-assignment-reversed.md)); H-17 written with its criterion, its prediction and its stopping rule before any run; brief 040; whitepaper 4.41.0; no code, no constant and no test changed.** H-16's stopping rule left the next question to an ADR among four candidates. This one takes the reversal, because the property the configuration has not been asked for is revision, and because three facts of the tree say what will happen: the selection is a deterministic comparison with no exploration in it (`Readout::select`, `compute_gating`); under the reward's gate a pair consolidates only when it is selected and correct, and nothing depresses what a reward raised (ADR-0080's derivation); and a learned engine's answer readout counts about twice the other's. So the prediction is **no** — after the flip the engine keeps selecting the old answer and earns nothing — and the round is run because the size of that no is the reading: how often a deterministic gate crosses under the counts' own variation, and whether a single reward is earned after the flip. That number is the measured need the next mechanism — an exploration in the selection, or a depression under the gate — must meet, and ADR-0078's rule is that a mechanism waits for one. The reward-prediction error is not chosen: it brakes a rise that has met no rail and gives no revision. `briefs/040_the-assignment-reversed.md` runs H-17.
- **A fourth shard for the whole-domain tests ([ADR-0088](docs/adr/0088-a-fourth-shard.md), amending [ADR-0073](docs/adr/0073-the-whole-domain-tests-sharded.md)); no code of the engine changed.** ADR-0087's dispatch read the three shards at 48, 23 and 77 per cent of their 120-minute bound on a runner two and a half times slower than ADR-0084's — the same tests that read 36 per cent there — and every measuring round since ADR-0077 has added a test of five to twenty minutes. The `weekly` matrix now takes `[0, 1, 2, 3]` and the step runs `exhaustive-shard.sh <k> 4`; the script, its enumeration, its round robin and its completeness check are ADR-0084's, unchanged, since ADR-0073 wrote `n` as an argument for the day it changed. The floor the decision leaves is named: a shard cannot be smaller than the longest single test it holds, 32 per cent of the bound on that runner.
- **Brief 039: inhibition off the reward's gate, built and measured ([ADR-0086](docs/adr/0086-the-inhibitory-baseline-built.md), [ADR-0087](docs/adr/0087-inhibition-off-the-reward-gate-measured.md)); H-16 is yes, its stopping rule at step 3, the configuration the engine learns in named and the next decision named; image format 15; the determinism pin untouched; no constant of the instrument or of H-16 moved.** The mechanism ADR-0085 decided, built: `Config::inhibitory_baseline_q16: Option<i32>`, unset by default and refused when set outside [0, 1]; `Modulations` gains a third value, the inhibitory baseline clamped to [0, 1] while set, and `for_synapse` takes the block's polarity, so that every slot of an inhibitory block consolidates under that baseline while it is set, the dopamine term never reaching it, and every excitatory synapse under the two modulations of ADR-0068 as before; the coordinator publishes the third word beside the other two and the fan-out reads it once per tick; unset, every run is the run it was, bit for bit. The image's modulator section carries it — a flag byte at `[24]`, the value at `[28..32)`, the rest reserved — and `CortexFileHeader::FORMAT_VERSION` moves from 14 to **15**; the loader refuses the bytes the writer never produces and a set value outside [0, 1], reads the zeros a format-14 writer left as unset, and refuses a version-14 header as every foreign version; the image's value outranks the configuration's. Tests: the configuration's edges and the published word, the rule over the lattice and four thousand seeded triples, one synapse of each polarity onto one target held to an oracle under the inhibitory baseline through a reward and a punishment and shown to differ from the signal's, the image's bytes and every refusal, the determinism pin unmoved; 625 tests pass in the debug profile where 620 did. H-16 run once in the new `tests/inhibition.rs` on the shared harness, untouched: the calibration reproduced ADR-0077's settled candidate with the parameter unset; the three arms from the frozen image with the inhibitory baseline written at 0.5 — ten bytes from the calibration's — under the task as built. Read: **128 of the last 128 correct in the assignment and in the mirrored assignment**, the crossing at trials 512 and 256 beside H-14's 448 and 384; the inhibitory sum falling in every block to 0.420, 0.409 and 0.418 of the image's, H-15's 0.41 block for block; every excitatory synapse outside the two answer pairs the image's bit for bit and none moving with the reward withheld, the assertion held on every arm; the withheld arm's lean a coin either way, so ADR-0085's second predicted reading did not hold as written for A and is recorded so; the readouts' background rate the image's throughout. H-15's no is thereby the excitatory synapses' unrewarded consolidation and not the inhibitory rule's course; the configuration the engine learns in is named — every excitatory synapse under the reward's gate, every inhibitory synapse under a baseline of its own. Whitepaper 4.38.0 and 4.39.0; brief 039 archived.
- **Inhibition off the reward's gate ([ADR-0085](docs/adr/0085-inhibition-off-the-reward-gate.md)); H-16 written with its criterion, its prediction and its stopping rule before any run; brief 039; whitepaper 4.37.0; no code, no constant and no test changed.** H-15's stopping rule left a choice between consolidation gated by the reward and the operating regime. This takes the reward's gate — the configuration H-14 learned in — and its named cost first: under the gate the inhibitory rule, which consolidates through the same modulation, stops. ADR-0083's readings say H-15's no had two parts one run could not separate, the excitatory synapses' unrewarded consolidation (every pair up by a third to a half, leaning to readout 1) and the inhibitory rule's course (the inhibitory sum to 0.41 of the image's). The rule decided here: an inhibitory synapse may consolidate under a baseline of its own, a parameter of the image, the dopamine term never reaching it; unset, today's rule bit for bit; the format moves to 15 when brief 039 builds it. H-16 in whitepaper §11.1 is H-14's configuration with that inhibitory baseline at 0.5 — one variable against H-14 and one against H-15 — and predicts yes; a yes names the configuration the engine learns in, a no makes the operating regime the next decision. §8.8's three-factor row now records the reads of ADR-0079, ADR-0081 and ADR-0083. `briefs/039_inhibition-off-the-reward-gate.md` builds the mechanism and runs H-16.
- **The shards take tests, not binaries ([ADR-0084](docs/adr/0084-the-shards-take-tests.md), amending [ADR-0073](docs/adr/0073-the-whole-domain-tests-sharded.md)); finding F-48 resolved; no code of the engine changed.** `instrument.rs` read 4 426 s of its shard's 7 200 in ADR-0083's dispatch with nothing added to it — ADR-0073's named floor, the largest single binary, at 61 per cent on a runner a few per cent slow, so a runner as slow as ADR-0072's would have passed the bound. `scripts/exhaustive-shard.sh` now reads every test `--list` names, sorts them, deals them round robin to the three shards, and runs each binary's share in one process with its tests named as exact filters (libtest takes several; verified on the pinned toolchain), each binary checked against the names it was given. ADR-0073 had rejected sharding by test as either a written list or a process per test; reading the names at run time and passing them together is neither. Verifying it found F-48: a listing that failed — a build that did not link — named no test, and the script exited 0; it now fails the shard. ADR-0082's rule on a new binary's name is superseded. The job's name, shard count and bound are unchanged.
- **Brief 038: plasticity everywhere, measured ([ADR-0083](docs/adr/0083-plasticity-everywhere-measured.md)); H-15 is no, its stopping rule at step 4, nothing written beside H-12 and the next decision named; image format 14 unchanged; the determinism pin untouched; no constant of the engine, the instrument or H-15 moved.** `tests/instrument.rs`'s harness — every item that is not a test — moved to `tests/instrument/harness.rs`, a module shared with the new `tests/everywhere.rs` (`#[path]`, its root items `pub(crate)`), whose name sorts before `instrument` so that the weekly job's round robin leaves `instrument` alone in its shard; `instrument.rs` keeps its forty-five tests and every pinned number, and the whitepaper's fourteen directives that named a harness symbol by the old path name the new one. The oracle consolidates every stimulus–readout synapse under the baseline plus the signal where the synapse is addressed and the baseline alone elsewhere (`Composer::baseline`, zero in every older run, which stays bit for bit what it was); `earned_run_under` takes the feedback, the assignment and the baseline and asserts the withheld arm's zero reward and resting signal; `settled_engine` and `frozen_image_checked` write the one settled engine twice, with the baseline patched to zero for the calibration and as encoded at 0.5 for the arms. H-15's constants, its criterion, ADR-0082's assertion as an integer rule (the exact bounds 13 923 and 51 613 in Q16.16, 0.2125 and 0.7875, which ADR-0082's 0.212 and 0.788 round) and its two predicted readings as rules were committed before the first rewarded run; the three arms are one weekly `exhaustive` test with their tables pinned, and the gate gains one test. Read: 78 of the last 128 correct in the assignment and 106 in the mirrored against 80; the withheld arm's drift toward readout 1 for both stimuli and the readout-0 stimulus deciding each arm, both as predicted; every coupling up by 35 to 63 per cent of the image's, the reward's part two to nine per cent; the inhibitory sum at 0.411 of the image's after twenty-four blocks. Whitepaper 4.35.0; brief 038 archived.
- **Plasticity everywhere ([ADR-0082](docs/adr/0082-plasticity-everywhere.md)); H-15 written with its criterion, its predicted readings, its relation to H-12 and its stopping rule before any run; brief 038; whitepaper 4.34.0; no code, no constant and no test changed.** H-14's stopping rule left the next question to an ADR. This one takes the baseline first: H-13 and H-14 read yes with the modulation baseline at zero, under which nothing in the arena moves but the pairs a reward reached — exact for attribution, and a network in which ADR-0055's settling and the inhibitory rule do not act. H-15 asks H-14's question with the baseline at 0.5, the one ADR-0077's lead-in settled under and H-12's rewarded runs used (the executor's default is 1.0, at which a reward adds nothing), every other constant H-14's, beside a withheld arm. No prediction is written for the verdict; from ADR-0077's terms the unrewarded consolidation is predicted to favour readout 1 for both stimuli, so the stimulus whose answer is readout 0 decides each arm. A yes adds one sentence beside H-12: what separated its runs from these yeses is the settled network, not the baseline. The runs go to a test binary of their own whose name sorts before `instrument`, because `exhaustive-shard.sh` assigns sorted binaries round robin and any later name would put two heavy binaries in one shard. `briefs/038_plasticity-everywhere.md` runs H-15.
- **Brief 037: the reinforced form, measured ([ADR-0081](docs/adr/0081-the-reinforced-form-measured.md)); H-14 is yes, its stopping rule at step 3, the note written beside H-12 and the next decision named; image format 14 unchanged; the determinism pin untouched; no constant of the engine, the instrument or H-14 moved.** `tests/instrument.rs`: the task's own delivery composed in the harness with no change to the task (`earned_run`: `Task::trial` under `Delivery::Addressed` with the arm's feedback, the task addressing the presented stimulus onto the selected readout and delivering the reward signed by the outcome, the harness asserting the task's contract at every trial); ADR-0079's oracle followed from the outcome with one field (`Composer::rewarded`, the reward the task delivers before the reading), every older run bit for bit what it was; the criterion as an integer rule (`reinforced`: `last_correct` of each rewarded arm at least `REWARDED_MIN`), the arms (`Earned`, `EARNED_ARMS`), the length (`REINFORCED_TRIALS`, 1 536), ADR-0080's prediction as constants, the assertion's shape (`reachable_pairs`, `derivation`) and the lock-in reading's shape (`splits`, `last_splits`, `locked_in`) committed before the first rewarded run; one weekly `exhaustive` test, `the_reinforced_form_at_1024_units_exhaustive` — the settled image built as ADR-0077 builds it and held to its tables, a frozen block from it held to ADR-0077's frozen run before any rewarded run, then the assignment, the mirrored assignment and the shuffled reward from the one image, each dumped and the verdict computed before the assertion, each held to its reach, to the derivation and to its pinned tables (`REINFORCED_*_1024`); the gate gains `the_first_eight_trials_of_the_reinforced_form_at_1024_units_and_the_rules_over_their_tables`. The reading: 128 of the last 128 correct in both assignments against 80; the selection crossing 40 of 64 per block at trial 448 and 384, the mirrored first, as predicted; the answer couplings at 1.366 and 1.348 (the assignment) and 1.445 and 1.354 (the mirrored) of the image's and still rising; every synapse outside the reachable pairs the image's bit for bit and the wrong pairs never consolidating, the signal at every trial's end at most 0.712 and negative after every punishment, as ADR-0080 derived; the shuffled reward locking stimulus A onto readout 1 (60 of 61) at chance accuracy. Whitepaper 4.33.0: §11.1's H-14 checked yes with its scope, its stopping rule at step 3 with the next decision named and not taken, the note beside H-12, the operating regime's item open with one more reading; §6.5; §9. `briefs/037_the-reinforced-form.md` archived.
- **The reinforced form ([ADR-0080](docs/adr/0080-the-reinforced-form.md)); H-14 written with its criterion, its prediction, its relation to H-12 and its stopping rule before any run; brief 037; whitepaper 4.32.0; no code, no constant and no test changed.** H-13's stopping rule named the reinforced form as the decision after a yes. This ADR takes it with the task the engine already has — `Delivery::Addressed` and `Feedback::Answer`, the reward reaching the synapses from the presented stimulus onto the readout the engine selected, its sign the outcome's — on ADR-0077's settled image with the baseline at zero, and writes H-14 in whitepaper §11.1: 80 correct selections of the last 128 in the assignment and in the mirrored assignment. The run is 1 536 trials, not 512: from H-13's blocks, the reinforced form delivers about the accuracy times what the taught one did, starting near half, and the arithmetic puts the selection's crossing of 40 of 64 by about trial 900; at 512 it would still be near the frozen network's level. The lock-in question is answered by the rules: the signal at a trial's end is at most 0.712, so the punishment leaves it negative while a wrong selection's pair is addressed, and that pair never consolidates — asserted by the round, not a criterion. The shuffled reward is run and read for lock-in and bounded by no clause, because under positive feedback its accuracy is not binomial; the mirrored pair is the information control. A yes is recorded beside H-12's closing sentence, which is not re-decided. `briefs/037_the-reinforced-form.md` runs H-14.
- **Brief 036: the reward's direction, measured ([ADR-0079](docs/adr/0079-the-rewards-direction-measured.md)); H-13 is yes, its stopping rule at step 3 and the next decision named; image format 14 unchanged; the determinism pin untouched; no constant of the engine, the instrument or H-13 moved.** `tests/instrument.rs`: ADR-0072's oracle extended to replay the consolidation — `Composer::taught`, `decayed_signal` and `signal_course` (the executor's per-tick decay written a second time), `consolidated` (`SynapseBlock::consolidate` written a second time) — the record's weight now held to the oracle's magnitude and the record's signal at every trial's end to the course, every older frozen run bit for bit what it was; the taught delivery in the harness (`taught_run`: `Executor::address` to the presented stimulus's units onto its assigned readout's units between the trial's last tick and the reward, then `Executor::reward`, whatever the selection); the criterion as integer rules (`couplings_rose`, `paired`, `taught_blocks`, `last_paired`, `direction`) and the prediction (`COUPLINGS_PREDICTED`) committed before the first rewarded run; the signal's figures computed apart from the tree and held (the fixed point 1.712 after a reward and 0.712 at a trial's end, the modulation at the ceiling for 9 694 ticks of 16 384; ADR-0078's arithmetic said about 1.58 and 0.58, the rule's step being the floor of $2^{-14}$ of the signal); one weekly `exhaustive` test, `the_rewards_direction_at_1024_units_exhaustive` — the settled image built as ADR-0077 builds it and held to its tables, the withheld arm's first block held to ADR-0077's frozen run before any rewarded run, then the assignment and the mirrored assignment from the one image, each held to its delivery's reach and to its pinned tables (`DIRECTION_*_1024`); the gate gains `the_first_eight_trials_of_the_taught_delivery_at_1024_units_and_the_rules_over_their_tables`. The reading: the assigned couplings rose to 1.118 and 1.138 of the image's in the assignment and 1.153 and 1.139 in the mirrored, every synapse outside the pairs the image's bit for bit; the assigned readout counted more than at the same trial of the withheld arm in 128 and 127 trials of the last 128, by six spikes per trial; the selection followed, 106 and 115 of the last 128 to the assigned readout, a reading that reopens nothing of H-12; the trace's potentiation grew as the couplings rose and the rise was not settling. Whitepaper 4.31.0: §11.1's H-13 checked yes with its scope, its stopping rule at step 3 with the next decision named (an ADR on the reinforced form) and not taken, the operating regime's item open with one reading added; §6.5; §9. `briefs/036_the-rewards-direction.md` archived.
- **After H-12, the reward's direction ([ADR-0078](docs/adr/0078-the-rewards-direction.md)); H-13 written with its criterion and its stopping rule before any run; brief 036; whitepaper 4.30.0; no code, no constant and no test changed.** H-12's stopping rule left the choice among three routes to an ADR. This one chooses the task that asks for a sign rather than a difference, because it is the smallest step and its answer routes the other two, and writes it as H-13 in whitepaper §11.1: on the network ADR-0077 settled — the first configuration where the trace on a stimulus's synapses is net potentiation on average while the readout sees — with the modulation baseline at zero, a reward delivered after every trial to the synapses from the presented stimulus onto its assigned readout, whatever the engine selected, is asked to raise those couplings and that readout's response, read trial by trial against the same trials with the reward withheld, in the assignment and the mirrored one. The couplings are predicted to rise; for the response no prediction is written, and the two mechanisms in the pair rule that bound the rise are named. Its stopping rule is written with it: one round, a calibration that must reproduce ADR-0077 first, a yes that makes the reinforced form the next decision and a no that makes the operating regime the next decision, no constant moved after a rewarded run. The structural rule is deferred for want of a measured need, and the operating regime is recorded in §11.1 as an open item. `briefs/036_the-rewards-direction.md` runs H-13.
- **Brief 035: the background side ([ADR-0077](docs/adr/0077-the-background-side.md)); H-12's stopping rule at its second step, and its third applied: H-12 closed as a no for this task in this regime; image format 14 unchanged; the determinism pin untouched; no constant of the engine or the instrument moved.** `tests/instrument.rs`: `run_on` and `compose_on`, a run and a composition from an executor the caller made (`run_behind` and `compose_shaped` build the network and call them, bit for bit), the composer's oracle seeded from the record — each block's stamp and trace, each unit's last spike, the cursor at the executor's clock — and held to the record at every trial; ADR-0055's criterion as an integer rule (`settled_within`: sixteen windows within 75 per ten thousand of the window sixteen before, strict), a lead-in that stops at the first window it holds at and is bounded at 160 (the eightieth the bound the brief names), a quiet run bounded at one window, the settled engine saved as an image with the modulator's baseline patched to zero, and the three measures written first. The settled network: the criterion held at the ninety-sixth window, the excitatory sum at 0.925 of the prior's, the rate 1.6 to 1.8 Hz and the readout units' rate the prior's 0.28 per trial; the quiet run 2 500 ticks moving the inhibitory sum by 114 LSB; over the frozen run the stimulus fires once, the sight 62, the sign 53 of 64, the terms +4.3, −0.1, +6.4 and −10.2 per synapse per trial summing to +0.4, every pair's sum positive after the block, the offset the criterion needs 2. The controller at ADR-0055's step: settled at the thirty-fifth window at 0.453 of the prior's, the gain swinging between 2.2 and 2.8 and the rate 12 to 41 Hz, the readouts seven to twenty-four times louder; the volley short (48.4 of 51), the sight 51, the sign 47; every clause of the prediction written first held. No candidate passes, there is no rewarded run, and by the rule's step 3 the three-factor rule with ADR-0068's addressed delivery does not learn the two-alternative difference task on the reference network at 1 024 units, because its pairing statistics are the background's; the next decision chooses by an ADR among a task that asks for a sign, a structural rule that adds a synapse and a review of the operating regime against ADR-0036's criticality, with the readings that bear on each in the ADR. The gate gains one test (the rules over the pinned tables and the settled lead-in's first two windows); the weekly gains two runs, so `instrument.rs` runs twenty-six `exhaustive` tests. Whitepaper 4.29.0: H-12 and its stopping rule closed in §11.1, §8.8's three-factor row, §6.5, §9, the directives; the ADR index, `README.md`, `CLAUDE.md` and the reader's guide; brief 035 archived.
- **Brief 034: two injections ([ADR-0076](docs/adr/0076-two-injections.md)); F-46 resolved; F-47 narrowed; H-12's stopping rule at its first step, the stimulus side exhausted; image format 14 unchanged; the determinism pin untouched; no constant of the engine or the instrument moved.** The runtime's `task.rs`: `Cancel { offset, ticks, messages, efficacy_q16 }`, a negative basal message into the stimulus's units injected between ticks before each of `ticks` consecutive ticks of the trial from `offset`; `Stimulus::cancel` (`None` injects what the stimulus injected before, held by a test against ADR-0059's loop message for message and tick for tick), `Stimulus::cancel_at`, `Task::trial` calling it before every tick, four refusals in `Task::check` (`EmptyCancel`, `CancelAtInjection`, `CancelOutsideTrial`, `CancelNotNegative`) each at its edges, a lattice property that a cancel injects at its ticks and at no other, and the engine held to the oracle's timing at a gain of 1.0; the `Stimulus` comment F-47 showed false corrected; `Cancel` exported. `tests/instrument.rs`: the harness takes a cancel and every pinned run passes none, so every number of ADR-0065, ADR-0066, ADR-0069, ADR-0070, ADR-0072 and ADR-0074 reruns unchanged; the composer counts the volley's spikes by tick. The brief's cancel — injected at `REFRACTORY_TICKS`, sized to the basal residual, twice it, or the bound of one message — was read against the tree before it was built and probed as written: the membrane rule drops an input inside the refractory window (ADR-0018), so a cancel landing on tick 201 is dropped by every unit that fired in the volley, and what fires the unit again is the soma the coupling primed through the window, so the residual and twice it and one message at the bound are too small at the tick the unit integrates again; every one of the six probes fires the unit twice. The cancel derived from the rule by an integer oracle over `integrate` (`alone`, with the executor's scaling and timing, held to the engine's probe through the task on every row): the offset `REFRACTORY_TICKS + 1`, the span 9 from the volley's census (ticks 1 to 9 over sixty-four trials, `VOLLEY_TICKS_1024`), and the size in messages at the bound per tick — 3, the least at rest; 6, the least at the extreme standing state; 12, twice it — probed on a unit at rest (each leaves it its one spike) and run over frozen runs of sixty-four trials at 1 024 units and the gain 1.75 under ADR-0074's measure: 3 leaves 0.83 spikes per unit after the opening and fails; **6 fires every unit of the presented set once and not again — no spike in the pair window after the opening in any trial, against F-46's 2.06 per unit — and is the pick**; 12 reads the same. Under the pick the sight reads 62 of 64 and the sign 13, so there is no rewarded run. The composition under it, per synapse of A→R0: the volley's depression goes to nothing (−0.2 against −10.4) and the background's halves (−12.7 against −26.8), the background's potentiation stays (6.6 against 7.5), but the volley's potentiation halves (8.0 against 17.2), because the response ADR-0072 measured in the pair window after the opening was the response to three volleys — the after-spikes were volleys too — and the response to one is 8.9 spikes per readout set per trial against 19.2; the terms sum to −2.0 per synapse per trial against the +5 hypothesised, one pair standing positive after the block for the first time at the present gain. Deliverable D under the pick: the bias +6/34 and +3/30, toward the answer on both stimuli; the offset the criterion needs 1; what the delivery moved ADR-0074's reading, quoted. The gate gains one test (the probes, the rules over the pinned tables, the first eight trials of the pick); the weekly job one test of three frozen runs, so `instrument.rs` runs twenty-four. By H-12's stopping rule (whitepaper §11.1), whose first step this round was, the stimulus side is exhausted; the next round is step 2, the background side. Whitepaper 4.28.0.
- **H-12's stopping rule, written before brief 034's outcome (whitepaper §11.1); no code, no constant and no criterion changed.** Seven rounds on the learning line have produced three rewarded runs that failed their criterion and three that could not reach one, and the problem has narrowed each time — to the sign of the trace, first, with the magnitude within two to four times of enough (ADR-0074). But two attempts on one knob each met two clauses that do not overlap (the gain in ADR-0072, a stimulus of one message in ADR-0074), which is the network's regime pushing back rather than a constant left to find. So the line now has a written bound: brief 034's two injections are the last attempt on the stimulus side; at most one round follows on the background side (the controller, or a network settled before the task); and if that cannot make the sight and the sign pass together, H-12 closes as a no for this task in this regime, recorded as a result, and the next round chooses by ADR among a task that asks for a sign, a structural rule, and a review of the operating regime against ADR-0036's criticality. A calibration that passes suspends the rule. It is written now so that the decision to stop is not shaped by the result it stops on. Whitepaper 4.27.0.
- **The dispatch's scope follows the diff ([ADR-0075](docs/adr/0075-the-dispatch-scope-follows-the-diff.md), amending [ADR-0067](docs/adr/0067-the-weekly-dispatch-has-a-scope.md)); no code of the engine changed.** ADR-0067 sent a round to the whole-tree sweep when it “added or removed a test” and put the choice in the brief. Every measuring round since added tests, so every one was sent there — and three of the four (briefs 031, 032 and 033, #80, #83, #88) changed no file under `src/`, which is all `cargo-mutants` mutates, so their sweeps could only re-run the Monday before: 031's slowest shard took 111 minutes against its exhaustive job's 69, 032's 136 against 103, and brief 033's round cancelled its `scope=both` dispatch nineteen minutes in once the reason was pointed out. “Added” never belonged in the rule: a surviving mutant is one no test catches, and an added test can only catch more; the `#[ignore]`d runs a measuring round adds are not in the swept suite at all. A round now dispatches `scope=both` only when its own diff changes a file under a `src/` directory, deletes a test or takes one out of the swept suite, or changes `.cargo/mutants.toml` or the sweep's job — and **the executing round decides it from the diff it has**, because a brief is written before the diff exists and brief 033's author guessed wrong. A brief now cites the rule rather than naming a scope. Monday still sweeps the whole tree. Whitepaper 4.26.0.
- **Brief 033: a stimulus that fires once ([ADR-0074](docs/adr/0074-a-stimulus-that-fires-once.md)); finding F-47; F-46 narrowed; no code of the engine changed; image format 14 unchanged; the determinism pin untouched; no constant of the engine or the instrument moved.** The runtime's `tests/instrument.rs`: the harness takes a stimulus shape (`Shape`, messages and efficacy), and every pinned run passes ADR-0065's `SHAPE_F46`, so every number of ADR-0065, ADR-0066, ADR-0069, ADR-0070 and ADR-0072 reruns unchanged; `compose_shaped` reads a frozen run under a shape with every trial's readout counts beside the composition. The one change F-46 names, tried in the brief's order — (a) one message at the unit's threshold, (b) one message of 1.25, (c) one message of 1.125, the midpoint, argued and committed before it was run — each read over the calibration's frozen run at 1 024 units and the gain 1.75 by a measure written first that reads no selection, reward or outcome: one spike per unit within two before the readout window opens, at most a tenth of a spike per unit in the pair window after it. **The executor scales an injected message by the tick's synaptic gain as it scales a synapse's (F-47)**: F-46's two messages of 1.25 arrive as 4.375 and fire a unit at rest twice, at 5 and 206 ticks (`PROBED_1024`, the engine's own probe in the gate); one message of 1.0 fires it not at all, one of 1.25 once at 22 ticks. Under the drive's standing potential one message fires 95 to 99 per cent of the units in the volley (48.6, 50.4 and 50.1 of 51 per presentation) and leaves 0.28, 0.70 and 0.46 of a spike per unit in the pair window after it against F-46's 2.06 — monotone in the drive, so the after clause's tenth lies below the drive at which the volley clause first holds and **no candidate passes** (`SHAPE_PICKED_1024` none). The sight passes under each (63, 63, 62) and the sign fails under each (2, 1, 0 of 64): the trace stays net depression, the terms −3.7, −5.5 and −4.7 per synapse per trial against ADR-0072's −15.7 and its Hypothesis of +5, which is not met — the volley's depression fell to about 3 rather than nothing, the background's to 15 to 18, and the volley's potentiation fell with the response it pairs. **There is no rewarded run.** What the criterion requires, read off the trials for the first time as integers: the instrument's bias +18/34 and −16/30 spikes per trial (half a spike toward readout 0 on either stimulus); the offset the criterion needs, by a scan on the recorded counts, 2, 2, 1 and 3 spikes per trial on the answer readout (F-46's, (a), (b), (c)); what ADR-0069's delivery moved, read off its pinned blocks beside the fixed modulation's drift, 0.4 to 0.8 of a spike per trial downward — of the size needed and in the wrong direction, so the sign is the obstacle first and the brief's rule for a finding on the bias does not fire. The gate gains one test (the rules at their edges, Deliverable D over the pinned tables, the probe, the first eight trials of candidate (a); 23 s in debug on a developer machine); the candidates are one weekly `exhaustive` test, so `instrument.rs` runs twenty-three. What remains, named and not built: a stimulus of two injections, a change to the task's trial. Whitepaper 4.25.0.
- **The whole-domain tests run in three shards ([ADR-0073](docs/adr/0073-the-whole-domain-tests-sharded.md)); F-45's deferred decision taken; no code of the engine changed.** [ADR-0071](docs/adr/0071-the-exhaustive-job-times-its-own-binaries.md)'s first table, from brief 032's dispatch, read 41 tests in four binaries over 6 174 s — `instrument` 22 in 3 007, `reference` 7 in 2 081, `learning` 11 in 1 084, `cortex-core` 1 in 2 — so the job took **1 h 43 m of its 120-minute bound**, the nearest a run has come, with half of it in one binary. ADR-0072 read from the same table that its own tests had added about a minute and the unchanged binaries were 1.35 to 1.67 times slower than the run before, and handed the shape on as “the first thing a measuring round must decide”. Runner or content, one job had no margin for both. `scripts/exhaustive-shard.sh <k> <n>` takes every n-th binary that `--list` names at run time — never a list written into the workflow, which is how F-44 and F-45 happened — runs each directly, times it, and **checks what it reported running against what the list counted**, as [ADR-0058](docs/adr/0058-the-weekly-sweep-and-its-timeouts.md)'s completeness check does for the sweep. Three shards put `learning`, then `cortex-core` with `reference`, then `instrument`: the margin against the slowest is 2.4 times where it was 1.15. The floor is now the largest single binary, which sharding cannot split, and the ADR says so. Whitepaper 4.24.0.
- **Brief 032: what the trace is made of ([ADR-0072](docs/adr/0072-what-the-trace-is-made-of.md)); finding F-46; no code of the engine changed; image format 14 unchanged; the determinism pin untouched; no constant of the engine or the instrument moved.** The runtime's `tests/instrument.rs`: `run_behind` takes an observer called after every trial, which reads and never writes, so `run` and every pinned run are unchanged; `Composer` reads, after every trial of ADR-0065's calibration run at 1 024 units (the weights frozen), the record's summed `eligibility_q1_15` over the synapses from each stimulus set onto each readout set, the terms that entered those traces split by what paired them (a term is the volley's when the readout spike that paired it lies within one pair window of $2^{11}$ ticks from the arrival of that synapse's volley message, the presented unit's volley spike plus the synapse's own delay, and the background's otherwise) and by their sign, and the census around the readout window's opening (the readouts' and the presented set's spikes in the pair window before it and after it). The terms are an oracle's: the pair rule of `cortex-core` replayed over the executor's own train, synapse by synapse (`pair_window`, `depression_at`, `decayed`, `volleyed`), held equal to the record's trace at every reading of every run. The ladder — 1.75, 1.5, 1.25, 1.0, in that order — each rung a frozen run of sixty-four trials read by the sight (ADR-0065's measure, 56 of 64) and the sign (the presented stimulus's summed eligibility onto both readouts positive in 56 of 64), both written first; a rung passes only when both pass, the first that does is the gain (`ladder_pick`), and none did (`GAIN_PICKED_1024`). Two weekly `exhaustive` tests (the composition at the present gain, `CALIBRATION_1024[0]`'s run bit for bit; the ladder's three lower rungs) pinned per trial and per block; the gate gains one test of eight trials at 1 024 units ($2^{17}$ ticks): the oracle's arithmetic and the class rule at their edges, the measures and the pick at theirs, the pick over the pinned ladder, the synapses per pair, and the first eight trials held to the first eight rows. **The reading:** at 1.75 the volley's pairing is net potentiation (+180 k to +230 k in Q1.15 per pair of about 800 synapses) and the background's net depression of four to five times it (−830 k to −960 k), so ADR-0066's sentence, from which ADR-0068 and ADR-0069 reasoned, is a measurement; the record's sum is negative after every trial but the first (the sign 1 of 64, the sight 62). The mechanism is in the census: each stimulus unit fires about twice more within one pair window after its volley (105 spikes per presentation for 51 units beside the volley's 52), so a stimulus unit fires 6.7 times as often as a readout unit, and the rule depresses once per presynaptic spike and potentiates at most once per postsynaptic one (F-46: the stimulus described since ADR-0065 as firing each unit once fires each three times within the pair window). At 1.5 the trace turns net potentiation (every sum positive after the block, the sign 45) while the readout no longer sees the stimulus (the sight 49); at 1.25 and 1.0 the readouts are all but silent. No rung passes both, so there is no rewarded run and the gain stays; what a next round would change is named and not built: a stimulus that fires each unit once within the pair window. Whitepaper 4.23.0; §6.5, §8.8, §9, §11.1's H-11, H-12 and F-46; the README, `CLAUDE.md` and the reader's guide; brief 032 archived.
- **The whole-domain tests report what each binary took ([ADR-0071](docs/adr/0071-the-exhaustive-job-times-its-own-binaries.md)); finding F-45; no code of the engine changed.** The weekly `exhaustive` job reported one duration for thirty-seven tests across four binaries, so what a round added could not be seen: over seven runs it read 32, 55, 33, 40, 71, 69 and 69 minutes while about twenty-one heavy runs were added, and two runs of **identical content read 40 and 71**, runner variance of about 1.8 to one being the larger term. Two readers reached opposite conclusions from that number — ADR-0070 recorded the job as growing and "still undecided", a reviewer asserted a trajectory to the 120-minute bound the series does not support. `--report-time` is a nightly flag and the toolchain is stable (ADR-0009), but cargo prints a `Running` line per test binary and a `test result: …; finished in <seconds>` line after it and runs them in order, so the job tees its output and a second step pairs the lines into a table — the binary, its count of `exhaustive` tests, its seconds — in the job summary and as `exhaustive-times.txt` in an artifact kept ninety days. It decides nothing else: no shard, no run retired, no bound moved; sharding the job as [ADR-0058](docs/adr/0058-the-weekly-sweep-and-its-timeouts.md) sharded the sweep is left to the round that has the numbers, which is the same choice [ADR-0063](docs/adr/0063-the-sweep-reads-its-own-timeouts.md) made for F-42. Whitepaper 4.22.0.
- **Brief 031: where 256 units settle ([ADR-0070](docs/adr/0070-where-256-units-settle.md)); no code of the engine changed; image format 14 unchanged; the determinism pin untouched; no constant of the instrument moved.** The runtime's `tests/instrument.rs`: the settling measurement, the executor of the rewarded runs at 256 units run under the drive alone, no task and no stimulus, for eighty whole windows, with the population's spikes, the two sums by polarity and the fraction of units at the target pinned per window as a weekly `exhaustive` test; brief 026's clause as an integer rule over the table (`settled_at`), the lead-in it derives (`derived_lead_in`: the smallest window satisfying the clause, or the bound) with a Node oracle apart from the tree; `lead_in` in windows and `run_behind`, of which `run` is the case of none; the rewarded run at 256 units behind the derived lead-in of nine windows, pinned block for block with its trace as a weekly test, under a criterion written before the run (`holds_through`: the calibration's measure at least 56 of 64 in every block) whose outcome is a constant the gate reads. The gate gains one test of $2^{19}$ ticks: the clause at its edges, the lead-in as derived, the criterion at its edges over ADR-0066's tables and over the run behind the lead-in, and the first four windows of the settling held to the weekly table. The reading: the clause first holds at the ninth window, at 0.818 of the prior's excitatory sum, and the sum is still falling at the eightieth, at 0.551, by 0.24 per cent per window, so the clause reads a rate and §11.1's item from ADR-0055 is discharged with both numbers; behind the lead-in the measure reads 50, 44, 43, 35, 41, 34, 36, 36 against 57, 46, 44, 42, 38, 42, 36, 31 without it, below the mark from the first block, because the measure follows the sum's level and the drive alone carries the sum below the level at which the instrument sees; 256 units is not usable for this task behind a lead-in of any length, and a learning round's criterion stays at 1 024 units. Whitepaper 4.21.0; brief 031 archived.
- **Brief 030: the reward addressed and measured ([ADR-0068](docs/adr/0068-the-reward-addressed.md), [ADR-0069](docs/adr/0069-the-addressed-reward-measured.md)); image format 14 unchanged; the determinism pin untouched; no constant of the instrument moved.** The runtime's `executor.rs`: the modulation a synapse consolidates under is a function of both its ends. `Shared` gains two flags per unit, a source and a target, and `modulation_at_rest` beside `modulation`; `Modulations::of` computes the two modulations of a tick by `cortex-neuromod`'s rule, once with the signal and once at rest, and `tick` publishes both before the barrier; `phase_fan_out` reads the source flag once per spiked unit and the target flag once per slot and consolidates slot by slot through `consolidate_each` (`consolidate_all` call for call under one modulation, held with the previous call as its oracle over two thousand seeded blocks). `Executor::{address, address_all, is_source, is_target, addressed_counts}` and `AddressError::NoSuchUnit` (a set with a unit outside the arena on either side is refused whole, the set left as it was); every unit is a source and a target until narrowed, the image does not carry the set and a loaded engine addresses every unit. `task.rs`: `Delivery::{Global, Addressed}` on `Task`; under the addressed delivery a trial writes, after the selection and before the reward and whatever the feedback, the presented stimulus's units as the sources and the selected readout's units as the targets, no target at a tie; `TaskError::Address`. `cortex-core`'s rule, the records and the image are unchanged. ADR-0068 argues the shape, the presynaptic narrowing (the reward is consolidated at the next presynaptic spike, the next presentation of a stimulus, so without it the reward of one trial would reach the other stimulus's synapses at half the trials) and, before any rewarded run, what the delivery can do on this task: with the reward symmetric the sign and the selection combine into the stimulus's answer, so the delivery is a teaching signal toward the answer lagged by one trial, whose direction on the behaviour is the sign of the trace it consolidates. `tests/instrument.rs`: the harness takes the delivery; the addressed criterion (ADR-0066's clauses with the global form beside the controls) and the rule for a size that stops seeing (the calibration's measure below 56 of 64 in any block before the criterion's window is not measured, never a pass or a fail) written before the first addressed run; six addressed runs as weekly `exhaustive` tests (the rewarded run on four workers and on one, the mirrored assignment, the shuffled reward and the fixed modulation at 1 024 units, the rewarded run at 256), the gate gaining the addressed rewarded run's first block at 256 units and the criterion over the pinned tables. **The outcome** (ADR-0069): at 1 024 units the addressed rewarded run reads 56 correct of the last 128 and the mirrored assignment 65 against the 80 the clause asks for, the addressed shuffled reward 53, the fixed modulation 60 (ADR-0066's run bit for bit under the addressed delivery) and the global form 49 (ADR-0066's rewarded run reproduced under this round's code, table for table and trace for trace), the run the same on one worker and on four: the rewarded clause fails with the global form reproduced. The readings: the couplings from a stimulus into its rewarded readout part from the other two by four to five per cent in the rewarded run and two to three in the mirrored, downward, against a drift of about one per cent between runs under the global form, the asymmetry ADR-0066 said a global scalar could not make, in the direction the trace's sign set before the run; the two readouts' responses still differ by under 1.5 spikes per trial against a spread of about 3.3, so the behaviour reads nothing of it; at 256 units the calibration's measure is 55 of 64 in the first block and the size is not measured under the rule written first, its first block's couplings parting the same way. Named and not built: a background under which the pairings can rise, a reward of one sign so that the selection decides which synapses consolidate, a consolidation at the trial's end for that trial's stimulus, a structural rule, a different task. Whitepaper 4.20.0; §6.5, §8.8, §9, §11.1's H-11 and H-12; the README, `CLAUDE.md` and the reader's guide; brief 030 archived.
- **The weekly dispatch has a scope ([ADR-0067](docs/adr/0067-the-weekly-dispatch-has-a-scope.md)); no code of the engine changed.** `workflow_dispatch` gains `scope` (`both`, the default, `exhaustive` or `mutants`), and the `weekly` and `mutants-weekly` jobs read it; a scheduled run always does both, and a push or a pull request skips both as before. Since [ADR-0061](docs/adr/0061-the-learning-runs-leave-the-gate.md) moved the learning runs out of the gate, a round's own measurement lives in the exhaustive tests, so a round must dispatch — and was paying for the seven-job sweep to do it. Brief 029's dispatch (`35502753399`) priced the difference: the exhaustive job 40 minutes, the sweep's slowest shard 2 h 06 m, the two hours added for a result that arrives in about an hour. The exhaustive job is a range rather than a figure — 33 m, 56 m, 40 m and 1 h 11 m over the four runs on record, runner variance of about two to one dominating it — which against its 120-minute bound is a second reason not to spend the sweep's two hours beside it. The rule that replaces Appendix B's “and when a round ends”: every round dispatches `scope=exhaustive`; it dispatches the sweep as well only when it added or removed a test, moved one between the gate and the weekly job, or changed `.cargo/mutants.toml` or the sweep's jobs, bounds, shards or completeness check — the class in which the gate's `--in-diff` scope is blind, and the class ADR-0061 was in when the sweep exposed `Task::coin_at`. Otherwise the schedule covers it and its survivors are the next round's list, which is what [ADR-0030](docs/adr/0030-verification-governance.md) already says they are. Whitepaper 4.19.0.
- **Brief 029: the instrument recalibrated and the reward path measured again through it ([ADR-0065](docs/adr/0065-the-instrument-recalibrated.md), [ADR-0066](docs/adr/0066-the-reward-path-measured-again.md)); image format 14 unchanged; the determinism pin untouched.** The runtime's `task.rs`: `Set { first, period, mask, count }` is a periodic pattern of units (the contiguous run a period of one through `Set::contiguous`, a lattice a period of the stride, several roles interleaved in one period one mask each), with `is_well_formed`, `len`, `end`, `contains`, `units()` and an `overlaps` over the smaller set's units; `Task` gains a `Window { from, ticks }` of the trial that the readout counts through `Readout::count_window`, which passes over the train's entries after the window and stops at the first before it (`count` stays as ADR-0059 wrote it); `TaskError::{MalformedSet, EmptyWindow, WindowOutsideTrial}`; the count's width bound is the window's. `tests/instrument.rs`, brief 029's harness: the geometry a rule of the prior's window and inhibitory rule (periods of twenty from a rotation the census picks, the first at which the four excitatory couplings are equal within ten per cent: 17 at 256 units, 0 at 1 024; a stimulus unit at the first and twelfth place, nine and eleven apart, never inhibitory; the readouts the odd and the even places less those, nine each, four of either on either side of every stimulus unit), the readout window the local delay band's rule (100 to 600 ticks after the injection), a trial of $2^{14}$ ticks (one dopamine time constant), the block, run, baseline, reward and seeds as brief 027's; the calibration before any reward, with the weights frozen (the baseline at zero, no reward, the sums asserted unchanged), the readouts' spikes in the window after the volley against the window before the injection over sixty-four trials at 1.75 then 2.0, a pass at fifty-six: at 256 units 1.75 fails (50) and 2.0 passes (58), at 1 024 units 1.75 passes first (62), and the presented set fires one spike per unit in the volley; every constant committed before the first rewarded run. The measurement, the same rule, controls and seeds as ADR-0060's, under a criterion over the last 128 trials written first (at least 80 correct in the rewarded run and the mirrored assignment, at most 76 under a control, the sequence the same on one worker and on four; the rates by an integer oracle over Pascal's rule, one in 337 and one in 74): the rewarded run reads 53 and 49 at 256 and 1 024 units, the mirrored assignment 53 and 65, the controls 51, 51, 55 and 60, and the run is the same on one worker and on four, so the rewarded clause fails at both sizes with the calibration passed and no constant moves. The readings: the instrument resolves the stimulus from the background but not one stimulus from the other (the two readouts' responses differ by under 0.7 spikes per trial in every block); every coupling from a stimulus set into a readout set falls alike under every feedback and the four stay equal within a few per cent; at 256 units the calibration's measure falls from 57 of 64 to 31 over the run as the excitatory sum falls to 0.55 of the prior's, while at 1 024 units it stays at 57 to 64. The gate gains the geometry's census check, the picked gains, the criterion's oracle and the recalibrated rewarded run's first block at 256 units; the ten full runs and the two calibrations are weekly `exhaustive` tests. Whitepaper 4.17.0: §5.2.5, §6.5 with the directives, §8.8's three-factor row, §9's two rows, §11.1's H-11 and H-12; README, `CLAUDE.md`, the reader's guide, the ADR index; brief 029 archived.
- **The sweep reads its own timeouts, and the documentation gate is one command ([ADR-0063](docs/adr/0063-the-sweep-reads-its-own-timeouts.md), [ADR-0064](docs/adr/0064-the-documentation-gate-and-the-version.md)); findings F-43 and F-44; F-42 narrowed; no code of the engine changed.** `scripts/read-timeouts.mjs` reads a `mutants.out` directory and says, for every name in `timeout.txt`, the window and the slot it ran in (`debug.log`'s Apply and Revert pair), the mutants that overlapped it with their verdicts and the longest overlap, the binary its log ends in, whether any test reported after the last long-running warning, and whether a test of the mutant's own module is among those that never reported. The weekly job runs it after the completeness check and writes `timeout-evidence.txt` into the artifact and into the step summary, at no wall-clock cost; `npm run mutants:timeouts <dir>` is the same reading on a downloaded artifact. It reports and does not classify: on run `35485126396` it reproduces brief 028's hand reading exactly (twenty-six timeouts, one of F-42's kind, nineteen stalled, six in the `crates` area whose thirty-three-second bound ends before cargo's sixty-second warning), and the one signal that looks sufficient calls a hang a starved neighbour (`injector.rs:91:9` finished another thread's test after its warning while the test that would catch it never returned). The `specs` job now runs `npm run spec`, one step, because the hand-written list of steps had drifted from `package.json`: `spec:decisions`, added on 2026-09-20 to the gate and to three documents, was never added to the workflow (F-44). `scripts/check-scripts.test.mjs` holds every `spec:*` and every `*:test` script to being run by `spec`. `scripts/check-version.mjs` holds the whitepaper's front matter and its Document control table to the same version and date — they disagreed at 4.15.0 and 4.14.0 for a day and a half (F-43) — and, on a pull request, holds the version to having moved when the document did: patch for a correction that changes no claim, minor for new or changed content, major for a restructuring that supersedes. Whitepaper 4.17.0.
- **Brief 028: the first complete sweep's list, twenty-one survivors dispositioned and the loops whose end one operator can undo classified by a rule ([ADR-0062](docs/adr/0062-the-first-complete-sweeps-list.md)); findings F-41 and F-42; image format 14 unchanged; the determinism pin untouched.** The triage rule of ADR-0062 (another thread; a test's own loop; an ordering comparison on moved state; a bound the mutant removed; none of these), written before it was applied, over the thirty-six timeouts of weekly run `35459078284`: twenty-five inherent and left alone, ten directive-class, one the instrument's. `stp_decay_factor_q16` runs its binary exponentiation over the interval's significant bits as a range; `convergent`, `deepest` and `deepest_verdict` walk a range of depths; the hippocampus test's loop is a range over the tag; each is held to its previous form bit for bit by a property test in `mod prop` (`the_decay_factor_over_a_range_of_bits_is_the_previous_form_bit_for_bit`; `the_walks_over_a_range_are_the_previous_forms_bit_for_bit_and_leave_the_same_slot`, which compares the whole scratchpad slot afterwards). The twenty-one survivors: tests in `cortex-core` (a soma exactly at a positive threshold fires, `0xFFD4` from §8.8 by hand), `cortex-embodiment` (`is_empty` after a publish; the longest period the bound admits, 65 535 samples, renders and 65 536 is refused), `cortex-linguistic` (the last speech act marks a frame indirect; tact opens a closed particle slot and keeps the metaphor bit), `cortex-social` (courteous at 0.5 and `0xBFFF`, familiar at `0xC000`), `cortex-thalamus` (closed is a mode) and the runtime (`workers()` on one, two and four; `holds` before and after an append; a half entry is an early end); shapes in `cortex-cerebellum` (`set_plant_delay` by `min`, with the stored byte tested; `plant_delay`, `filled` and `mul_q16` by range patterns) and `cortex-knowledge` (the hazard by `max`); one exclusion beside `Worker::steal`'s entry. The write-ahead log's `read_at` and `write_at` are one loop each on every platform over the platform's positioned call (`pread`/`pwrite` on Unix, `seek_read`/`seek_write` on Windows); the Windows pair is excluded by name, the old `Ok(())` entry, which also silenced the Unix forms, is removed, and the Unix form no longer retries an interrupted call. `.cargo/mutants.toml` is one entry shorter. The decode timeout (`image.rs:718`) is not a loop: a slow pass starved by the hang the sweep ran beside it, F-42, open, the sweep unchanged. The sweep dispatched on the branch (`35485126396`): 3 456 of 3 456 mutants, every job green, 0 missed (21 before), 26 timeouts (35 before: the twenty-five inherent and one of F-42's kind), the slowest shard 1 h 59 m; the in-diff gate 43 of 43. Whitepaper 4.16.0: §9 gains ADR-0061's row (F-41, resolved, and `scripts/check-decisions.mjs` now holds every ADR to its row there and in the index, `npm run spec:decisions`) and ADR-0062's; §11.1's item is closed; Appendix B's V-6 row reads a timeout by the rule; the document control table's version, which had stayed at 4.14.0, matches the front matter. `CLAUDE.md` gains the loop invariant and `CONTRIBUTING.md`'s code rules its row. Brief 028 archived.
- **Brief 027: a task, a readout and a learning curve, the reward path closed on a behaviour the engine reads back ([ADR-0059](docs/adr/0059-a-task-a-readout-and-a-reward.md), [ADR-0060](docs/adr/0060-the-reward-path-measured.md)); finding F-40; hypothesis H-12; image format 14 unchanged; the determinism pin untouched.** The runtime gains `task.rs` and a path dependency on `cortex-basal-ganglia`, the crate that owns action selection and was composed by nothing: `Set { first, len }`; `Stimulus { set, messages, efficacy_q16 }`, injected before a trial's first tick; `Readout` over two disjoint sets and two `BasalGangliaChannelState`s (`count` over a tick-ordered train read at the trial's end, `select` writing each count as Q16.16 into its channel's direct drive and the other's indirect drive and calling `compute_gating`, a tie selecting nothing); `Feedback { Answer, Shuffled, Withheld }`; `Task::{check, trial, stimulus_at, coin_at, answer}` and `Outcome`; `TaskError` names every refusal (an empty set, one outside the arena, two that overlap, a stimulus without a message, a trial without a tick, a train below `units × spikes_per_unit(ticks)` with `MIN_INTERVAL_TICKS` the shorter refractory window, a readout count that could reach the width, a negative or a zero reward, a reward at a baseline of 1.0, the injector's refusal); a test for each, the counts of a hand-built train across the tick wrap, the reward's sign and magnitude against an oracle modulator, one worker against four, and the lattice property over `testkit/prop.rs`. `tests/learning.rs` runs the loop on the reference network with the gain held at 2.0 and no controller: the ring in four quarters (stimulus A, readout 0, stimulus B, readout 1, each readout touching both stimuli), two messages of 1.25 per unit as the stimulus, trials of $2^{12}$ ticks in blocks of 64, the baseline 0.5 and the reward ±1.0, 512 trials at 256 units on the gate and 2 048 at 1 024 in the weekly job, the rewarded run against a shuffled reward, a fixed modulation, the mirrored assignment and a second worker count, each run its own test with every block pinned. Under the criterion written before the run the rewarded clause fails at both sizes (−3 and −7 of 64 between the last block and the first), the two controls' clauses hold, the mirrored clause fails and one worker reproduces four trial for trial. The readings: a readout quarter holds about 20 spikes per trial at 256 units and 70 at 1 024 from the background alone and a stimulus adds the same few to both readouts (their difference under one spike per trial); the excitatory coupling from a stimulus quarter into a readout quarter falls under every feedback alike (1.02 M to 0.51 M in eight blocks at 256; 2.83 M to 1.29 M at 1 024, settled from the sixteenth block) while the arena's excitatory sum is 0.80 and 0.74 of the prior's; a stimulus quarter reverberates (3.4 spikes per unit per presentation) and the dopamine signal sums the last four or five outcomes. No constant moved after the run; what a next round would change (the readout's window, a stimulus that fires once, the trial's spacing, the gain) is named in ADR-0060 and not built. Whitepaper 4.15.0: the executive summary, §1.6, §5.1 (the runtime's dependency list and layer row), §5.2.5, §6.5 (R-5), §8.8, §9 (with ADR-0058's row, which was missing: F-40), §11.1 (H-11's disposition, H-12); README, `CLAUDE.md`, the reader's guide, the ADR index; brief 027 archived.
- **Brief 026: a weight that settles, the term arena compacted at slow-wave onset, the inhibitory rule read from below the rail ([ADR-0055](docs/adr/0055-a-weight-that-settles.md), [ADR-0056](docs/adr/0056-a-compaction-of-the-term-arena.md), [ADR-0057](docs/adr/0057-the-inhibitory-rule-from-below-the-rail.md)); image format 14 unchanged; the determinism pin moved once, for the depression's amount.** `cortex-core`: an excitatory pairing's depression is `round(A_minus * window * M / 2^13)`, `M` the slot's magnitude before the pairing and `STDP_DEPRESSION_REFERENCE_Q1_15` (0x2000, a quarter of the width) the magnitude at which it is `A_minus` (van Rossum, Bi and Turrigiano 2000; the potentiation additive as it is, so a night's replay still reaches the rail): 344 at the reference, 1 376 at the rail, nothing below a magnitude of twelve, so a weight the rule depresses cannot reach zero from above, and under stationary pairing a magnitude settles where its depression equals its potentiation (the unit test walks into the oracle's band [16 289, 16 352] from both sides); the inhibitory rule, the amplitudes and every signature unchanged. The day of ADR-0053 read again under the rule: at 1 024 units under the controller over eighty windows the excitatory sum settles at 0.45 of the prior's and holds within 0.75 per cent over the last sixteen at both periods, a night inside them, where the additive rule drained it to nothing in fifteen; at 256 units with the gain held over sixteen windows it is still falling at the sixteenth, by 1.2 per cent per window at the 5 Hz period and 1.9 at 20 Hz, decelerating from 2.6, so the brief's 256-unit clause failed at the 20 Hz period by 0.27 points in one window and the §11.1 item stays open with the reading; §8.8's per-unit gain stays Specified, since a gain multiplies what arrives and cannot lift a weight from zero. Every pin of the reference tests whose synapses the rule depresses moved once (the nights' sums before a night, the days, the capture's spikes and burst); every night still ends with the tagged cluster at the rail and completes six of six; `PINNED_ARENA_HASH` moved from `0x27e12eea1ee625a5` to `0x6c27858ece2dd412` with 95 spikes as before. `cortex-reasoning` gains `compact(arena, free, roots, forward)`: one descending pass marks from the roots (the arena is bottom-up by construction, so a marked node's children lie below it), one ascending pass moves the live nodes down and remaps their children through the caller's scratch, the roots remapped in place, the tail zeroed, `Compaction { live, reclaimed }`; refused unchanged for a root at or beyond the cursor, a child at or beyond its parent or a short scratch; three unit tests and a property test over 2 000 seeded arenas. The executor compacts its arena onto the store's clauses on `Executor::compact()` between ticks and by itself at every entry into slow-wave sleep (in `tally`, after `step_sleep`), the record's cursor moved, the last search's discoveries cleared, the affect state and the search's cursor standing (the store reads the same node for node), `Executor::{compactions, reclaimed}`; over the exit store's two commits 113 nodes become 83 with thirty reclaimed; `tests/store.rs` (three tests), `tests/no_alloc.rs` (a night's onset allocating nothing; the two tests serialised, since the counter is the process's), the capture nights and a slow-wave onset compacting the arena bit-identically on one and four workers; an index an input returns names its node until the next compaction, and the inputs' docs say so. `tests/reference.rs`: `prior_below_rail` (the reference generator with `inhibitory_gain_q4_4` 32: inhibitory weights in [-24 000, -12 000]), the day's ninth reading `at_target_q16` (the fraction of units whose spikes in the window are within a factor of two of the period's count, from the executor's own train), the 256-unit days at sixteen windows, the below-rail days at 256 units (the gate) and 1 024 units (`exhaustive`), the priors' sums before any window pinned. Read from below the rail: at the 20 000-tick period the inhibition rises by 4.8 per cent in ten windows at 256 units with the gain held and climbs from 0.55 of the rail to the rail within thirteen windows at 1 024 units under the controller; at the 5 000-tick period it is stripped to nothing within fifteen windows at 256 units and nine at 1 024; the fraction at the target's rise at the slower period at 256 units is read on the prior at the rail too, so it is the rate's fall under the new excitatory rule, not the inhibitory rule's work, and the ADR says so. No target is chosen and no registry gate class is made: `CLAUDE.md`'s invariant (a trial whose behaviour hashes are equal) is outside a brief's empowerment, and §11.1's registry item is restated under it. Whitepaper 4.14.0; README, `CLAUDE.md`, the reader's guide and the ADR index; 577 tests (569 run and 8 ignored); brief 026 archived.
- **Brief 025: the term arena in the image and the discovery loop inside the tick, the waking day and the inhibitory target period as an image parameter, the causal count inside the loop ([ADR-0052](docs/adr/0052-the-term-arena-in-the-image.md), [ADR-0053](docs/adr/0053-the-waking-day-and-the-target-period.md), [ADR-0054](docs/adr/0054-the-causal-count-inside-the-loop.md)); finding F-37 resolved; image format 14; the determinism pin moved to `0x27e12eea1ee625a5` for the unit's stamp (95 spikes, the dynamics unchanged).** `cortex-reasoning` gains `TermNode::{encode, decode, is_well_formed}`, `instantiate` (a term copied through the bindings, children before their parent, copy on need, bounded by `INSTANTIATE_DEPTH` and `WALK_LIMIT`), `InduceScratch::unbind`, and a third record, `InductionState` (the arena's cursor and counters, the store's length, the search's cursor as the pair after which the next search resumes, its budget, cadence and tag); `cortex-affect` gains `InteroceptiveState::{encode, decode, is_well_formed}`; `cortex-hippocampus` gives `Episode` its `symbol` at `[56..60)` with `bind` (written once, never zero); `cortex-connectome` names `SECTION_AFFECT` (47), `SECTION_INDUCTION` (48) and `SECTION_CLAUSE` (49) and moves `FORMAT_VERSION` to 14. The runtime's `search_from` resumes after a cursor, instantiates a commit's three outputs and unbinds, and reports where the next search resumes (the exit search's lengths unchanged: 106 to 100 nodes, two commits); the executor owns the arena, the store, the scratch, the affect record and the induction record (`store.rs`; `Config::{terms, clauses, search_shift, search_budget, discovery_tag}`), takes `term` and `assert_clause` as inputs between ticks (the affect state primed to the store's length), runs the loop through `discover()` and on `Cadence::new(search_shift, 0)` inside `tick()` while awake (the search from the cursor, the committed rewards into the modulator, the ripple before now tagged from the executor's own train and bound to the first commit's predicate; refusals counted as `untagged` and `search_failures`), `bind_episode`, `discoveries()`; the image writes the four sections and the loader refuses a node not well formed or not bottom-up, a store index that is not a clause below the cursor or named twice, a record that does not describe what was loaded, an affect state not primed, an episode bound outside the band; the compositions over a caller's store (`discover`, `ClauseSearch`, `Tagging`) are gone. `cortex-core` gains `istdp_alpha_q1_15(period)` with `ISTDP_PERIOD_MIN_TICKS` (100) and `ISTDP_PERIOD_MAX_TICKS` (1 000 000), `step_stdp` and `step_stdp_all` take the depression per spike as their last argument, and the executor publishes it from `Config::istdp_target_period_ticks` (default 20 000), which the modulator section carries at `[20..24)` and the loader bounds; `MESSAGE_SYNAPTIC` (bit 19), `synaptic_message`, `message_is_synaptic`, `CAUSAL_LATENCY_TICKS` (128), `DendriticSuperNeuron::last_synaptic_tick` at `[16..20)` with `note_synaptic_input` and `is_descendant`; the executor's fan-out and deliveries make synaptic messages, the turn stamps the unit and counts a descendant, the tally sums them into `Executor::descendants()`. `tests/reference.rs` gains the in-loop ratio per window beside the oracle's gross and net ratios at every size and on both priors, a `day` harness (spikes, gain, estimate, stage, descendants, the inhibitory and excitatory sums, replays per window) with a gate test at 256 units (eight windows, the gain held, at the 5 Hz and the 20 Hz period) and an `exhaustive` test at 1 024 (eighty windows under the controller at shift 5, a night at the sixty-sixth window); every night's pin stood. The readings: the in-loop count reads the input's density at each gain (0.27, 0.56, 0.66 on the lattice at 256, 1 024 and 4 096 units against gross ratios of 0.30 to 1.00; 0.48 against 0.08 on the random network), so under the rule written first the record and the controller are untouched; on the reference prior, whose inhibition starts at the rail, the inhibitory rule only weakens it (a third of a per cent per window at 5 Hz, eight per cent at 20 Hz), the excitatory arena drains to nothing within fifteen windows at 1 024 units under the controller's limit cycle at the ceiling, and a night's 320 replays carry a tagged cluster back to the rail; the sleep placeholders stand. `tests/store.rs` (six tests), `tests/no_alloc.rs` with a store on a cadence, `tests/image.rs` and `tests/modulation.rs` extended; 562 tests (555 run and 7 ignored). Not done: a compaction of the arena, standardising apart, a chosen target rate, a registry gate for behaviour-changing parameters, a stabilising rule for the excitatory weights, anything at 4 096 units beyond the count.
- **Brief 024: Dale's principle in the pair rule, the spike train inside the executor, the estimator at 4 096 units ([ADR-0049](docs/adr/0049-dale-principle-in-plasticity.md), [ADR-0050](docs/adr/0050-the-train-inside-the-executor.md), [ADR-0051](docs/adr/0051-the-estimator-at-4096-units.md)); finding F-36 resolved; image format 13 and the determinism pin untouched.** `cortex-core` gains `Polarity { Excitatory, Inhibitory }` with `Polarity::of_flags` from `FLAG_INHIBITORY`, an argument of `step_stdp`, `step_stdp_all`, `consolidate` and `consolidate_all`: the eligibility trace is the change of the weight's magnitude, consolidation clamps the magnitude to $[0, 1)$ under the polarity's sign (an excitatory weight never falls below zero, an inhibitory one never rises above it; a weight on the wrong side of zero is brought to zero by the first consolidation), and an inhibitory block takes the symmetric window of Vogels et al. 2011 (`−α` at every presynaptic spike, `ISTDP_ALPHA_Q1_15` 67 from a target rate of one spike per `ISTDP_TARGET_PERIOD_TICKS` 20 000, 5 Hz; $A_+$ for both pairings whichever way round); the executor passes the presynaptic unit's polarity in the fan-out phase. The nights of `tests/reference.rs` are pinned again: the patterns, the bursts, the readout units, the stages and the determinism pin stood; at 1 024 units the network's own pattern's six inhibitory synapses end at the negative rail (its twenty sum to 262 136, against 655 340 with every one at the positive rail under the old rule); at 256 units the eleven synapses from the unit two episodes share end at zero (six of them were at the negative rail) and the invention's 127 sum to 3 605 986 after the night. The executor gains `Config::train_capacity`, one shared slot per unit with an atomic cursor that every worker appends its spike to, and `merge_spikes` after the tick's last barrier (the tick's units sorted and appended as `(tick, unit)` to a ring, the oldest let go and counted when it is full); `Executor::train` reads it between ticks, `train_overwritten` and `train_capacity` the counters; `fork` returns the executor's train (`train_of`; `NoTrain` for a configuration that keeps none), `trace` stays for the workers' reports and the determinism test holds the two equal on one and four workers. `episode.rs` gains the forms over the executor's own train (`tag_recent`, `tag_burst_in`, `tag_discovery_recent`) and `discover(exec, ClauseSearch, Tagging, discoveries, associations)`: the search of ADR-0045, the committed rewards' total into the modulator, the coincidence before the reward tagged once for the rewarded search and one association per commit to that episode; `capture_night` no longer forks the start image for its train. `tests/reference.rs` gains the 4 096-unit `exhaustive` sweep on both priors (thirty-two kicks per gain, a twelve-window loop), pinned, under the decision rule brief 024 wrote before the run: the fine lag-one slope is within 0.15 of the gross causal ratio in five rows of six and 0.28 above it on the lattice at 2.25, where the window holds 112 272 spikes against a ceiling of 131 072, and the coarse estimate reads zero in seven windows of twelve; no line is added to the record. Not adopted, with the reasons in the ADRs: a sign bit per synapse, the target rate as a registry entry, the store, the affect state and the association in the image and a capture on a cadence inside the tick (three sections and format 14, the round that gives the image a term arena), a night at 4 096 units, a change to the estimator or the controller on the strength of the sweep. Whitepaper 4.12.0; 541 tests (535 run and 6 ignored: the five `exhaustive` reference tests and the whole-domain one).
- **Brief 023: the lexicon on ids, a second prior for the estimator, episodes tagged from the spike train ([ADR-0046](docs/adr/0046-lexicon.md), [ADR-0047](docs/adr/0047-second-prior-and-the-estimator.md), [ADR-0048](docs/adr/0048-episodes-tagged-from-the-train.md)); findings F-35 and F-36; H-9's open item and H-11's synaptic half decided at 256 and 1 024 units; the estimator's open question resolved without a record line; image format 13 and the determinism pin untouched.** The runtime gains `lexicon.rs`: `Entry { token, concept, shape, formal }` in a caller's slice sorted by token, eight lexical shapes (a noun, a nominal, a determiner, an adjective, an intransitive and a transitive verb, a hedge and a tag) each instantiated on the term arena with fresh variables, `Lexicon` with its atoms and its act and particle markers, `comprehend_tokens` (a marker token sets the act, a particle the prosody marker, every other token its category, then `comprehend`) and `realise` (the opener, the roles in the template's order as tokens with the formal variant at or above `POLITENESS_FORMAL`, a nested frame in place at `ROLE_CHILD` by an explicit stack of at most eight, the closer, the particle; `surface_token_id` written, for the first time by any rule); `comprehend` reads a role term `ROLE(c)` as a role whose filler is `c`, so a hedge or a tag fills the affect role with its own concept. `tests/language.rs` holds the round trips on a table of thirteen tokens (the pinned distances of ADR-0040 unchanged; an epistemic frame's four roles at 3 195, 3 157, 3 157 and 3 227 bits; "please spot chased felix ba" realised as exactly the tokens it came from and read back to the same frame). `cortex-homeostasis` gains `regression.rs`: `count_bins` (a train's spikes per bin of any width) and `slope_at_lag` (the lag-$k$ least-squares slope with an intercept, whose lag-one case over a window's bins is exactly the record's `estimate_branching_ratio`, held by a property walk); `tests/reference.rs` cross-checks the record's estimate against the train on every window and measures the sparse random network (the same generator at its widest window, nothing rewired) beside the lattice: the coarse estimate is near zero and swings on both priors because a bin is ten to forty generations, the slopes at bins of 256 ticks decay with the lag but read a driven population's autocorrelation and not $\sigma$ (0.68 at 256 units where the causal oracle reads 0.10; at 1 024 units within 0.15 of the gross ratio), so no line is added to the record, the ceiling stays the actuator's bound at this scale, and a sweep at 4 096 units is what the round that adds a line runs first. `cortex-hippocampus` gains `capture.rs`: `burst` (the densest span of a train) and `capture` (a span's units ranked by their spikes, first spike and index, the first twelve); the runtime gains `episode.rs`: `tag_from_trace`, `tag_burst` and `tag_discovery` (for a rewarded invention, the densest basal time constant of the ripple before the reward, tagged and returned with the invented predicate's id as the caller's `Association`). The capture night at 256 units: the invention's pattern is eleven of the twelve cued neighbours and one unit the drive fired twice in the same span, the network's own pattern a cascade of 64 spikes around the ring's wrap; after the night a cue of six of the invention's pattern fires five of the other six, the network's own pattern (45 synapses among twelve) does not complete, and the unit the two episodes share has its synapses into the other pattern depressed by the other's replays, six of eleven to the negative rail; at 1 024 units the invention's pattern is all twelve cued neighbours with every synapse at the rail and five of six firing on a cue in rank order, and the network's own pattern holds five inhibitory units whose synapses the replay carried across zero to the excitatory rail (finding F-36: the pair rule does not conserve a synapse's sign; recorded with an open question, not changed). Not adopted, with the reasons in the ADRs: the online capture inside the tick (Specified: a per-tick merge of every worker's spikes), a concept field in `Episode`, a second record line or a finer bin for the controller, a laminar prior, a conjunction shape (H-10's chart), words in a crate. Whitepaper 4.11.0; 535 tests (530 run and 5 ignored: the four `exhaustive` reference tests and the whole-domain one).
- **Brief 022: the reference network and the executive clause search ([ADR-0044](docs/adr/0044-reference-network.md), [ADR-0045](docs/adr/0045-clause-search.md)); finding F-33; H-8 and H-9 dispositioned at 256 and 1 024 units; H-11's symbolic half decided; image format 13 and the determinism pin untouched.** `cortex-connectome` gains the anatomical prior (`prior.rs`): a seeded, integer-only rule that yields every synapse of a network from one walk of Knuth's generator, a ring lattice with a local window and a rewired fraction, every $k$-th unit inhibitory with negative weights at a gain, a local and a far delay band; the runtime gains `synthesize` (a prior into the arenas, `Executor::arenas_mut`), `Drive` (an input that is a function of the tick and a seed), and the oracle of `branching.rs` (`fork`, `cascade`, `Attribution`): two forks of one image under one drive, one kicked, the extra spikes attributed through the kicked unit's synapses, gross and net of the spikes the kick advanced, into `update_branching_ratio`, which has its first caller with attribution. `tests/reference.rs` (a reduced form at 256 units in the gate; the full sweep at 256 and 1 024 as the weekly `exhaustive` tests): the one-window slope is not a reading of $\sigma$ (0.560 then 0.032 at one gain), the causal ratios are of its order and share no trend with the gain (gross 0.53 to 1.00, net 0.34 to 0.48 at 1 024 units), the controller turns at the saturation ceiling; after a night every synapse among a tagged pattern is at the rail, a cue of six completes a local cluster of twelve and never a random pattern. `cortex-reasoning` gains `next_pair`, `resolve_literal`, `prove` (a bounded depth-first proof search, the leftmost literal selected, `Frame`, `Proof`, `InduceError::Budget`); the runtime gains `search` over a clause store (`SearchReport`, `DiscoveryError::{StoreFull, OutFull}`): the exit test commits two inventions over twenty-four clauses (106 nodes become 100), keeps every proof and reads the invented predicates back at one step each (six steps become eight, eight and seven). The lexicon (the next brief's first item), the cortico-striatal gating, a prior in the image, a change to the estimator or the constants, the multistep regression and the synaptic half of H-11 are not adopted, with reasons in the ADRs. `criticality.rs`'s comment of a 48-unit network corrected to 64. The first test to construct the production wheel geometry overflowed the wheel builder's stack on Linux in the debug profile (finding F-34): the builder now reserves eight wheels and a megabyte. An adversarial review in a scratch clone found five behaviour defects, fixed here: `cascade` paired a descendant with the first missing spike in range, paired or not; `fork` ran past its end for a kick beyond it; `Drive::unit_at` collided message indices 65 536 apart; a local target wrapped on a ring past $2^{31}$ units; `next_pair` given a malformed `after` could pair a clause with itself; and six document defects, corrected. The prior's walk ends by a countdown of the synapses left, not a comparison: the gate's one timeout was a mutant that turned its guard's `||` into `&&` and walked forever. Whitepaper 4.10.0; the brief archived.

- **Brief 021: induction on the term arena, exact continued fractions and the discovery path ([ADR-0041](docs/adr/0041-induction-on-the-term-arena.md), [ADR-0042](docs/adr/0042-continued-fractions.md), [ADR-0043](docs/adr/0043-discovery-path.md)); finding F-31; hypothesis H-11; image format 13 and the determinism pin untouched.** An architectural proposal (a geometric intuition manifold over three records, inverse-resolution predicate invention and a Ramanujan-machine search, a free-energy drop as a dopamine reward, a fail-closed certificate round trip) was answered with three decisions after its premises were re-derived against the tree: `certify` was Implemented, not Specified; no document mentioned continued fractions; §6.10 said the engine stores a certificate hash while the record stores the statement's and no document defined a prover's payload (F-31); the executor holds no affect and nothing computes a free energy; the canvas has no potential to descend; ADR-0036 does not claim $\sigma$ settles at 1; the tree ran 446 tests, not 442. `cortex-reasoning` gains `induce.rs`: a definite clause is a compound over `CLAUSE` (`0xFFFF_FF03`, a head and up to seven literals), an invented predicate takes the next id of `INVENTED_BASE`..`INVENTED_LIMIT`, and `lgg` (Plotkin), `absorb`, `identify`, `intra_construct` and `resolve_definite` are bounded rules over `InduceScratch` matching first fit by unification, every output resolving back to its inputs (the identity the tests hold on named examples and 3 000 random pairs), every failure restoring the arena, the bindings and the counters; `size`, `term_hash` (FNV-1a over the pre-order walk) and `free_variables` read through the bindings. `cortex-arithmetic` gains `fraction.rs`: the convergents of $a_0 + b_1/(a_1 + b_2/(a_2 + \dots))$ with degree-two polynomial coefficients through the slot's `OP_MUL` and `OP_ADD` (a flag ends the walk with the slot showing the operation), `within` deciding a rational tolerance exactly by reduced cross-multiplication, and `search` enumerating coefficient tuples and reporting those whose deepest decidable convergent is within; the pins (one tuple near $e$, $a_n = n + 3$ and $b_n = -n$, among 2 401; one near $1 + \sqrt 2$ among 16; overflow at depth 32) come from an oracle outside the tree. `runtime/cortex-runtime` gains `discovery.rs` and path dependencies on `cortex-affect`, `cortex-tools` and `cortex-knowledge`: a clause store's description length in nodes is the free energy `update_valence` reads (`prime`, `invent`, `NotPrimed` when the state is stale), the valence shifted right by two and clamped is the reward `Executor::reward` takes, and `certify_from_frame` certifies a node only from a completed prover frame whose payload is the statement hash and a non-zero certificate hash, five refusals leaving the node untouched. The exit test `tests/discovery.rs`: thirty nodes become twenty-five, a valence of five and a reward of 1.0 that consolidates a pending trace at the next presynaptic spike; eighteen become nineteen, a reward of $-0.25$ that, under the test's baseline of zero, moves nothing; the invention's hash `0x21a1c619` pinned; the pending frame refused and the completed one accepted. The manifold, an interoceptive record in the image, the broker, a cryptographic hash, the meet-in-the-middle search and a dispatch threshold are not adopted, with reasons in the ADRs. The mutation gate's first run left three survivors (a depth bound, a slice bound, a scope test), each answered by a test; the review's malformed-arena fuzz found a panic on a corrupted arity, a cyclic arena that walked forever (also the occurs check of ADR-0025; finding F-32) and an after-measure that left `invent`'s scratch changed, fixed by `WALK_LIMIT` ($2^{20}$ nodes per walk), an arity test in `is_clause` and a public `InduceScratch::mark`/`restore`; `invent` also refuses a store that lacks either clause (`NotInStore`); seven document defects corrected. Whitepaper 4.9.0; the brief archived.
- **Brief 020: hypervector bodies and categorial reduction ([ADR-0039](docs/adr/0039-hypervector-body.md), [ADR-0040](docs/adr/0040-categorial-reduction.md)); finding F-30; hypothesis H-10; image format 13 and the determinism pin untouched.** An architectural proposal (a 10 000-bit hypervector body arena with SIMD unbinding and a `popcnt` codebook search, Gärdenfors quality spaces, CCG type reduction over `TermNode`, a cortico-striatal thematic gate that buffers tokens in the cerebellar delay line, a five-axis stance record and a "surface realization boundary") was answered after its premises were re-derived against the tree: 160 words are 1 280 bytes and 10 240 bits, not 1 250 bytes; every `core::arch` intrinsic is `unsafe` and `unsafe_code` is forbidden in every state crate, `core::simd` is nightly, so the "SIMD" is the compiler's vectorisation of `u64` loops and the result is the same integer on every target; no invariant "What stays outside. Words." exists (§1.5 and rule L-3 carry the rule, and the tree gains the one directive it lacked, that no state crate pulls `alloc` in); H-9's readout is pattern completion on the executor, not a linguistic trace, so the proposal's sequencing claim is answered under H-9 rather than adopted; the cerebellar ring is a scalar delay line and no token path reaches the loop. *The body* (ADR-0039): `HypervectorBody`, a second record of `cortex-symbolic` under ADR-0016's test, `[u64; 160]`, 1 280 bytes, twenty cache lines asserted at compile time, every bit used (`DIMENSIONS` is now 10 240, F-30); `from_seed` (splitmix64), `bind` (XOR, its own inverse), `permute` (rotation modulo the width), `bundle` (the per-bit majority of up to fifteen bodies in four carry-save bit-planes, an even count made odd by a fixed tie-breaker), `hamming`, `nearest` (the lowest index on a tie), `confidence_q16` ($1 - 2d/\text{BITS}$ floored at zero), and the header's `record_readout`, which writes the two fields nothing wrote; the capacity case pinned against an independent oracle (three bound roles on a codebook of sixty-four: each filler recovered at 2 545, 2 556 and 2 501 bits, the nearest wrong entry above 4 900, a fourth role at 4 979). *The categories* (ADR-0040): a category is a term on the arena of ADR-0025, an atom with its head or a functor over `CATEGORY_FORWARD` or `CATEGORY_BACKWARD` with three children (the result, the argument and the role the argument fills); forward and backward application and forward and backward harmonic composition as unifications, in that order; `reduce` a greedy shift-reduce reducer over caller slices (`ParseScratch`) with a log of `Reduction`s and every bound a result (`Empty`, `NoDerivation`, `StackFull`, `ArenaFull`, `StepsFull`, `BoundExceeded`, `Malformed`), a reduction that could not be completed undone; `head`; type raising and a chart Specified, the reducer's coverage hypothesis H-10. *The composition* (`runtime/cortex-runtime/src/language.rs`; the runtime depends on `cortex-symbolic`, `cortex-reasoning` and `cortex-linguistic` by path; no executor field): `comprehend` (a category sequence reduced and its derivation's roles bound into a frame with each argument's head, the root's head as the action), `encode_frame` (the bundle of role-bound concept bodies), `read_role` (Layer 1: unbind, the nearest entry, the confidence), `decode_frame` (every role at or above a floor of 0.125), `concept_in`, the role band `ROLE_CONCEPT_BASE | bit`; `comprehend` takes the sentence category and refuses a root that is not it (`LanguageError::NotASentence`). *The exit test* (`runtime/cortex-runtime/tests/language.rs`; a lexicon of seven words exists in the test alone): "the dog chased the cat" reduces in four steps (the dog; chased the, by forward composition; cat; the subject) into a causative frame (dog, chase, cat), sealed and realised as subject–action–object; encoded on a codebook of sixteen seeded bodies, each role reads back at 2 574, 2 562 and 2 482 bits with the affect role at 4 997 and unbound; the decoded frame is the same three concepts at the subject's confidence 0.497; the swapped sentence reads back swapped; "the bird slept" binds two roles; "dog cat" is no derivation; bit-identical on a second run and on both CI targets. *Not adopted, with reasons in the ADRs*: the Dominey gate (a second owner for the role assignment on a token path that does not exist; what a function word firing a gate is, symbolically, is the application rule), the stance record (three of five axes have owners, two have no reader), Gärdenfors coordinates as a second metric (level bodies under the same distance, Specified), intrinsics, "zero semantic distortion" claims, CKY and type raising. Whitepaper 4.8.0: the executive summary, §1.6, §2.2 (the `alloc` directive), §5.1 (its dependency sentence was stale at five of seven crates), §5.2.9, §5.2.20, §5.2.30, §6.9, §8.8 (three rows and one), §9, §11 (F-30), §11.1 (H-9's readout, H-10), Appendix A (row 9 at 1 280 B), Appendix C (M8), Appendix D (references 56 to 65), the glossary; the ADR index; the README, `CLAUDE.md`, the reader's guide; brief 020 archived with every deliverable dispositioned. The mutation gate's first run in CI tested 198 mutants in the changed lines and found seven survivors: a full trail with nothing to bind (`unify_in`'s bound compared at equality), an application that could not be logged retracting a node it never pushed (`retract`'s guard, now a `checked_sub` with no comparison of its own), the affect role never read back through `concept_in`, a `|` on the disjoint bits of the role band (now an addition), and two `|` on disjoint bits by construction in the rotation and the bit-sliced majority (excluded by name with the reason, the class `.cargo/mutants.toml` already carries); the first three have their tests. *A review pass* (probes in a scratch clone: a per-bit oracle over a hundred thousand random bodies for every rule of the algebra and splitmix64 for a thousand seeds; twenty thousand random category sequences with every logged reduction re-derived under the final bindings and two million scratch-size combinations without a panic; 5 400 frames over three codebook sizes and every role subset decoded exactly; a brute-force derivation oracle) found no behaviour defect; it found one contract gap, fixed (a malformed category was reported only when a rule touched it, so an index outside the arena "derived" alone; every shifted category and every result is now checked), one interaction, fixed (`comprehend` bound a root's head as the action whatever the root was, so "the dog" had an action; it now takes the sentence category and refuses a phrase), a wrong figure (the decode floor sits twelve standard deviations of the noise below chance, not forty; forty is a bound role's own spread), a statistical property stated as a rule (an item of a bundle is expected to agree with it on more than half the bits; a body with two copies of its complement does not), a probe result recorded under H-10 (the reducer matches the oracle on the exit lexicon's shapes; with a verb-phrase adverb it misses 13.6 % of derivable sequences, all of one form), and four stale sentences.
- **Brief 019: sleep as a state machine and the episodic ledger ([ADR-0037](docs/adr/0037-sleep-regulation.md), [ADR-0038](docs/adr/0038-episodic-ledger-and-replay.md)); image format 13; finding F-29; the determinism pin unmoved.** An architectural proposal (an episodic event ledger, slow-wave replay with synaptic downscaling and a sleep-phase clock sweep, REM recombination with affective recalibration, an ultradian state machine with an emergency wake) was answered after its premises were re-derived against the tree: the sleep gate was a rule nothing in the runtime called, on a fatigue field nothing wrote, with a threshold (32 768.0) no value reaches; the clock sweep is the caller's call between ticks and compaction does not exist; no sensory or salience path reaches the loop, so a wake is an input between ticks, as a reward is. *Sleep* (ADR-0037): `HomeostaticDrivePool` `[4..8)` is the sleep pressure (process S of Borbély's two-process model), `[18)` the stage (awake, slow-wave, REM), `[22)` the sleep shift and `[23)` the stage's windows; `step_sleep()` once per window on the window cadence advances the circadian phase by one (sixteen bits are 23.86 h), raises the pressure awake by $(1 - S) \gg k$ and lowers it asleep by $S \gg \max(k - 2, 0)$ with a one-LSB floor, and moves the stage at the onset thresholds (0.875 by day, 0.5 in the night quarter), the wake thresholds (0.375, 0.125) and the budgets (four windows of slow-wave sleep, two of REM), the wake test first; `wake()` is an input; `update_circadian_tick` is gone; at the default shift of 0 the stage and the pressure stay where the image put them. *The ledger* (ADR-0038): `Episode`, a second record of `cortex-hippocampus` under ADR-0016's test (a tagged tick, a tag, a replay count, up to twelve unit indices), appended by `Executor::tag_episode` between ticks and never overwritten; `HippocampalAttractorState` `[12..16)` is the ledger's length (the replay countdown gone, F-29) and `[24..28)` its hand; every $2^{11}$ ticks (four basal time constants, so that successive drives do not sum in the dendrite; the brief's $2^9$ fired every pattern twice after the first ripple) the coordinator walks the hand to the next unspent episode: in slow-wave sleep worker 0 delivers its units two messages of 1.25 in phase 3 (the middle of the measured band 2.25 to 2.875 that fires a unit at its base threshold exactly once), so that they fire together about twelve ticks on and the three-factor rule pairs every synapse among them as potentiation; in REM the tag falls by one and nothing is delivered. Sections 44 (the hippocampal state, always) and 45 (the ledger, when not empty); `Config::{sleep_shift, episodes}`; `ConfigError::{SleepShiftOutOfRange, TooManyEpisodes}`; `TagError`; `ImageError::{MalformedHippocampus, MalformedEpisode}`; the loader refuses a stage of three, a pressure above 1.0, a sleep stage at its budget, a shift above 15, a hand at the length, a length without a section or not the section's count, and eight malformed episodes each on its own clause. *The exit test* (`runtime/cortex-runtime/tests/sleep.rs`): the machine on the executor equals an oracle record through a whole cycle (transitions at windows 66, 70, 72 and 73 at a shift of 5); a tagged ring of four units fires together at every ripple, once per unit, and its synapses end at the weight an oracle block gives, 120 per ripple after the first; an untagged ring does not move; REM's tags fall in the hand's order and a spent episode is skipped; the wake keeps the pressure; two episodes in turn are bit-identical on one and four workers (256 replays, 768 spikes, 45 depotentiations); the ledger and the stage round-trip through the image mid-sleep. *A review pass* (probes in a scratch copy: three million any-byte records through the sleep step, 180 scenarios of the two-process arithmetic against an independent oracle over 200 000 windows each, two million records of each hippocampal type against independent well-formedness oracles, the loader's clauses at their edges, one to four workers bit-identical through eviction and re-hydration, a trial on a sleeping image) found one behaviour defect, fixed: a ripple takes twenty-four mailbox nodes on worker 0 and a pool sized by the old rule could be exhausted, aborting the process (`ConfigError::TooFewNodes` now refuses a pool below one ripple's worth while the ledger has room); one interaction now stated: the replay drive is scaled by the synaptic gain like every input, so with a control step above zero a small network whose replay reads as saturated replays intermittently; and five document defects (120 per ripple, not 121; a stale test count; "Measured" for a probe; the fall's clamp missing from two formulas; the wake at onset unstated). *Not adopted, with reasons in the ADRs*: a weight-downscaling sweep in slow-wave sleep, affect recalibration in the executor, sensory gating during sleep, a sleep-gated clock sweep, a completion-driven alternation, a use-dependent pressure, a priority queue over the ledger, a replay through the injector ring; the canvas hydration of an episode is Specified. Whitepaper 4.7.0: §1.6, the executive summary, §5.2.1, §5.2.2, §5.2.15, §5.2.16, §5.2.32, §6.1, §6.6, §8.4 to §8.8, §9, §11 (F-29), §11.1 (H-9 and the Targets), Appendix A (rows 44 and 45), Appendix C, Appendix D (six references), the glossary; the README, `CLAUDE.md`, the reader's guide (its §11 row was stale at F-27 and H-6); brief 019 archived.
- **Brief 018: the substrate re-examined ([ADR-0034](docs/adr/0034-synaptic-density.md), [ADR-0035](docs/adr/0035-cadence-and-the-population-tally.md), [ADR-0036](docs/adr/0036-criticality-control.md)); image format 12; the determinism pin unmoved.** An architectural proposal (density, multirate execution, dynamical stability) was answered with three decisions after its premises were re-derived against the tree: the weight is 2 of a synapse's 16 bytes, so eight-bit weights cannot halve the arena; the runtime composes no slow subsystem, so no barrier skew between fast and slow nodes existed to measure; short-term plasticity is the unit's, not the synapse's. *Density* (ADR-0034, no code): the four-synapse block stands, with the byte accounting; eight-bit logarithmic weights (a 1 % pairing below one level; ADR-0012's resolution argument), eight synapses per block (no room for the delay's horizon, the stored release or the trace) and an in-engine far-memory synapse tier (a quiet unit's blocks are pages nothing touches; placement is the OS's, a hint the `mmap` round's) rejected; the density levers named in order (the stored release as the block's factors, a column-relative target, the token's width) with the measurement that admits each; hypothesis H-7. *Cadence* (ADR-0035): `cortex_core::Cadence` (`new(period_shift, phase)`, `is_due`, `period`, `phase`), a power-of-two period so that the test is a mask and the clock's wrap is exact; slow rules run between ticks on the coordinator or inside a phase by the record's holder, never behind a new barrier; every worker publishes the units it fired and the coordinator sums them (the tally §5.2.16 and R-12 called Specified); `executor/idle_tick/{1,2,4}` in `cortex-bench` (a developer-machine run, not admissible: 52 ns on one worker, 570 ns on two, 1.68 µs on four, unpinned); conservative lookahead an open question of §11.1 with that benchmark's admissible run as the decision rule. *Criticality control* (ADR-0036): `HomeostaticDrivePool` changed shape (`thermal_stress`, a hardware vital `cortex-autonomic` owns, and `target_threshold_bias`, read by nothing, are gone: finding F-28; the open bin's count and the synaptic gain took their bytes, the circadian phase narrowed to sixteen bits and the sleep flag to one byte, the reserved bytes became the estimator's window); `count_activity`, `close_bin`, `estimate_branching_ratio` (the lag-one least-squares slope with an intercept over a window of $2^5$ bins of $2^{12}$ ticks, Q16.16 in $[0, 16]$; Wilting and Priesemann 2018), `is_saturated`, `regulate` ($g \leftarrow g\,(1 - \kappa\,\operatorname{clamp}(\hat\sigma - 1, -1, 1))$ rounded to nearest, clamped to $[0.25, 4]$; a window at or above the ceiling read as supercritical; at $\kappa = 0$ the gain never moves), `is_well_formed`, `encode`, `decode`, `new` at a gain of 1.0; constants `ACTIVITY_BIN_SHIFT` (12, at or above the wheel's horizon, asserted at compile time in the runtime), `ACTIVITY_WINDOW_SHIFT` (5), `ACTIVITY_COUNT_MAX`, `GAIN_MIN_Q16`, `GAIN_MAX_Q16`, `CONTROL_STEP_MAX_Q0_16` (0.5), `SIGMA_MAX_Q16`. The runtime depends on `cortex-homeostasis`: `Config::control_step_q0_16` (default 0; `ConfigError::ControlStepOutOfRange`), one record per engine with `homeostasis()`, the gain published before every tick and applied by the turn holder to the basal and apical sums (exact at 1.0, so every pinned value held), the tally after every tick, the bin closed and the window regulated on two cadences with a ceiling of one spike per unit per bin; section kind 43 (`SECTION_HOMEOSTASIS`) always written and required, refused for a count other than one, a step above its bound, a record that is not well formed or a window that is not the clock's (`ImageError::MalformedHomeostasis`); the image's step and gain outrank the configuration's. Per-unit synaptic scaling (Turrigiano) written down as Specified with its bytes and its rule; columnar annealing and an immune quench not adopted, with reasons. The exit test (`runtime/cortex-runtime/tests/criticality.rs`): the executor's record equals an oracle record fed the same per-bin counts after every window (growth lowers the gain, decay raises it, a straight line leaves it, saturation lowers it by an eighth, one below the ceiling is regulated by its slope); a silent engine's gain climbs by half per window to 4.0 and stops there exactly; a recurrent 64-unit network begets almost no spike at a gain of 1.0 and, eight windows on, several per kick with the gain between 1.5 and 3; the loop is bit-identical on one and four workers; the state round-trips mid-window and a loaded engine continues alike; the step is refused above its bound. What the exit test does not show, that the branching ratio settles at 1 on a network, is hypothesis H-8 with its protocol: the estimator reads a stationary branching process at about its generation time, and a small network with depleting synapses is not one. Documents: whitepaper 4.6.0 (§1.6, §2.3, §5.2.1, §5.2.2, §5.2.16, §6.1, R-12, §8.3 to §8.8, §9, §10.2, §11 F-28, §11.1 H-7, H-8 and two questions, Appendix A row 43, Appendix C, Appendix D with four references and reference 31 made unique, the glossary; the document-control cell that still said 4.4.0 corrected), README, `CLAUDE.md`, the benchmarks README, the ADR index; brief 018 archived with every deliverable done and its departures noted. The mutation gate in CI found two survivors in one clause of `is_well_formed` (both test cases started from a record that held a pair, so another clause masked the one under test): fixed. A review pass with probes found no behaviour defect (three million hand-built records through every rule without a panic; the slope against an exact rational oracle over 200 000 windows; one to four workers bit-identical; the window at the clock's bin count at every tick and across the wrap; the turn's scaling exact at 1.0 over the `i32` lattice) and five document defects, folded in: `is_well_formed` gained the consistency clauses the documents promised (the square of the sum at most the pairs times the sum of squares, the pair sum at most the cap times the sum, a one-bin window's first and last the same) and the documents say what it holds; the ceiling's comment named a refractory fraction the executor does not use; §5.2.16 said fourteen tests for twelve; the idle-tick figure is recorded under the benchmarks convention (`docs/benchmarks/results/2026-09-12-dancr-win11.md`, not admissible) and ADR-0035 points there; the ceiling's rate is per unit, not across the population; every rounding to nearest is stated as a tie toward positive infinity.

- **Brief 017: three-factor plasticity ([ADR-0032](docs/adr/0032-three-factor-plasticity.md)) and the tick in the header ([ADR-0033](docs/adr/0033-tick-duration-in-the-header.md)); image format 11; the determinism pin moved once, with its reason.** `SynapseBlock` `[56..64)` is `eligibility_q1_15: [i16; 4]`, the eligibility trace per slot, and the apical mask moved into bits 28–31 of the chain word `chain` at `[32..36)` (`CHAIN_MASK`, `MAX_CHAIN_INDEX`; a chain index is bounded to 26 bits by the token, finding F-23; `next_encoded` reads the low bits, `link` and `unlink` keep the compartments): `step_stdp` puts ADR-0022's pairing amount into the trace, `decay_eligibility` decays every trace by $(1 - 2^{-16})^{\Delta t}$ (`ELIGIBILITY_TAU_SHIFT`, about 655 ms) rounded to nearest and by at least one LSB so that a trace reaches zero exactly, `step_stdp_all` decays by the ticks since the stamp before it pairs, and `consolidate` moves $\operatorname{round}(e \cdot m)$ into the weight with $m$ clamped to $[0, 1]$ (`MODULATION_ONE_Q16`), taking what the weight absorbed out of the trace (the sum is conserved; a weight at the rail keeps its pending change). `cortex-neuromod` gains `modulation(baseline)` (`clamp(baseline + dopamine, 0, 1)`), `DOPAMINE_TAU_SHIFT` (14), `new`, `is_at_rest`, `encode`, `decode` and `Default`. The runtime depends on `cortex-neuromod`: `Config::modulation_baseline_q16` (default 1.0, refused outside $[0, 1]$), one `NeuromodulatorState` per engine with `modulator()` and `reward(rpe)` between ticks, the modulation published to the workers before every tick and the signal decayed after, `consolidate_all` after `step_stdp_all` in the fan-out phase, and section kind 42 (`SECTION_MODULATOR`: one 64-byte record holding the modulator and, at `[16..20)`, the baseline, always written and required by the loader, so that the image defines the run and a trial's forks run under the live baseline; refused when its count is not one, a reserved byte is not zero or the baseline is out of range). The loader also refuses a trace or a compartment bit on an empty slot. The header's `[60..64)` is `tick_ns`, written from the new `cortex_core::TICK_NS` (10 000), refused at zero by `validate` and refused by the loader when it differs (`ImageError::TickMismatch`); its `[40..48)`, `layers_offset` (unused since the directory), is `written_tick`, the executor's tick at the write, which the loader resumes: until now a loaded image's clock restarted at zero, so every stamp it carried read as $2^{32}$ ticks old at the first spike (short-term plasticity fully relaxed, pairings paired with nothing, a never-spiked unit's quiet time restarted), which brief 017's exit test found; the trials of `tests/amendment.rs` now set their quiet bounds in engine ticks. With the baseline at 1.0 the rule is ADR-0022's for a weight that never comes within one pairing of a rail; at a rail the pairing's two terms now sum in the trace before the weight saturates and the rest stays pending where ADR-0022 discarded it, so the histories differ from that pairing on. `PINNED_ARENA_HASH` moved from `0x7603c27186e59994` to `0x1724f3486c1d674e` for the block's bytes alone (the pinned network's weights stay within $[8\,180, 31\,986]$; a first draft gave the rail as the reason, from the brief's wrong context line, until a review pass reran the network) and `PINNED_SPIKE_COUNT` stayed at 95; the ring, the contention, the accounting, the image, the amendment and the oscillator tests held. Eleven tests: four in `cortex-core` (the rail, the decay, the decay before the pairing, the consolidation), two in `cortex-neuromod`, five in `runtime/cortex-runtime/tests/modulation.rs` (brief 017's exit test: pairings wait in the trace with the baseline at 0 and match an oracle block; a reward consolidates them at the next presynaptic spike with the sum conserved; a reward before any pairing consolidates nothing; a late reward consolidates less; a loaded engine continues alike, at the writer's tick; the baseline's bounds), plus the connectome's and the image tests' new clauses. One `.cargo/mutants.toml` exclusion (`link`'s `|` on cleared bits); the local mutation run found `is_end` unconstrained (a linked block is now asserted not to be the last) and `is_apical`'s bound equivalent under `<=` (a slot past the four shifts its bit out of the word, so the bound is a range pattern now). Whitepaper 4.5.0: §1.1, §1.6, §5.2.1, §5.2.2, §5.2.14, §6.1, §8.4, §8.7, §8.8, §9, §11.1 (the tick question closed), Appendix A row 42, reference 31; README, `CLAUDE.md`, the ADR index. Brief 017 archived. A review pass with probes outside the repository, after every local gate was green, found: the pin's stated reason false (above); §8.7 still saying format 10 and naming `next_block_idx`; §5.2.2's `validate` order missing the tick; ADR-0033 placing `ZeroTick` before the version check; two references numbered 17; the archived brief's banner claiming deliverable 2 landed as written (the executing round chose to decay a stampless block from tick 0, and says so under the box); the loader no longer bounding the bytes of an empty slot (fixed: a trace or a compartment there is refused); and the baseline changing what a run does while living in neither the image nor the trace (fixed: it is in the modulation section, which is always written, required, and outranks the configuration). It confirmed the widths, the chain word, one-to-four-worker determinism with rewards, the clock resume against the original engine, the section's refusals and the reward's saturation.
- **Brief 016, part three: the runtime under the arithmetic lint, 185 sites converted by name; finding F-4 resolved; the brief archived.** `#![deny(clippy::arithmetic_side_effects)]` in `runtime/cortex-runtime/src/lib.rs` and in its six integration-test crate roots: every crate of the workspace and every test crate root now refuse plain integer arithmetic, and a whitepaper directive holds the runtime's count beside the state crates'. The barrier's arrival count and spin counter, the deque's and the injector's indices and cursors, and the pool's node encodings advance by `wrapping_*` (Chase–Lev and Vyukov indices wrap by design; the index + 1 encoding of ADR-0017 beside its `u32::MAX` refusal); the reported counters (`evictions`, `rehydrations`, `delivered`, the dropped traces, `in_flight`) by `saturating_*`; the ring-neighbour steps of the workers, the sweep hand and the wiring by a `next_in_ring(i, n)` helper (`wrapping_add(1).checked_rem(n)`, the divisor refused at zero by `Executor::new`); the executor's tick by `wrapping_add` (the dynamics already see it as `u32`); `propose`'s id by `checked_add(1)` with `ArenaFull` as the refusal; the loader reads the header with `split_first_chunk`, walks the directory with `chunks_exact`, slices every section through one `section_of(bytes, entry)` (`checked_add` → `Truncated`, which also replaced the inline directory check) and compares the amendment ids by `checked_add`; the writer's capacities and offsets and the log's length by `saturating_*` with a `LOG_ENTRY_BYTES` constant as the divisor; `align64` by `saturating_mul`; the trial consumes its sorted injections with a peekable iterator and checks the sweep cadence by `checked_rem`; `MAX_TOKEN_BLOCK + 1` is the compile-time `MAX_BLOCKS`. The test fixtures' wiring and offsets use `checked_rem(..).expect(..)`, `wrapping_*`, `step_by` and re-slicing (`img[at..][..64]`); the pins `PINNED_ARENA_HASH` and `PINNED_SPIKE_COUNT` and every asserted value held in both profiles. The differences a named operation makes are all beyond any run or file: the $2^{32}$-th proposal (`ArenaFull` where release had wrapped the id to 0), an amendment section of $2^{32} + 1$ records (the same refusal in both profiles), a node count above `usize` (a capacity panic in both profiles), the u64 counters at $2^{64}$ and the log after $2^{64}/72$ appends (saturation). Four `.cargo/mutants.toml` exclusions that named arithmetic the runtime no longer has (`SpinBarrier::wait`'s `+=`, `build_wheels`' `*`, `read_at`/`write_at`'s `+=`, `Image::decode`'s `-`) are removed. The mutation gate's first CI run on these lines found eight survivors: two in a `const` initializer (`MAX_TOKEN_BLOCK + 1` in the executor, now the loader's own `too_many_blocks`, unit-tested at its edge, so no constant is formed there), a directory bound that divided where the chunk iterator already counts (now `directory.len()`, with a test that a header with no sections is missing them, not truncated), a sweep-cadence guard `checked_rem` already answered (removed), and four equivalent by construction, excluded by name with the reason: `Worker::steal` returning nothing (work stealing changes no result; the contention test shows exactly-once either way) and the platform-conditional log I/O forms the other platform does not compile. Whitepaper §2.2 (the two directives), §11 (F-4 resolved: 376 sites across seventeen crates and the runtime, every pin held, the one unreachable result change named), `CONTRIBUTING.md` (the row names the lint alone and the no-exemption rule), `CLAUDE.md` (an invariant), README. Brief 016 is archived: its deliverables 1 to 3 landed in two pull requests instead of the six it asked for (the mutation gate scales with the diff, and one request per group of crates kept each run under fifteen minutes), 4 and 5 as written.

- **Brief 016, part two: `cortex-core` under the arithmetic lint, ninety-four sites converted by name, no result changed.** `#![deny(clippy::arithmetic_side_effects)]` in `lib.rs` and in the crate roots `tests/oscillator.rs` and `tests/mailbox.rs`; every state crate now carries the lint. The encodings (`serial.rs`, twenty-two spans) compute no offset any more: `u16_le`/`u32_le`/`u64_le` read the field's own bytes, every caller slicing by the literal range `encode` writes, and the per-slot arrays of `SynapseBlock` are one literal range cut by `chunks_exact`. The wheel's horizon is the literal `HORIZON_TICKS` (2 560) tied to `COARSE_SLOTS × FINE_PER_COARSE` by a compile-time assertion; its tick advances and its slots are formed by `wrapping_*` (a tick counter wraps by name, §8.4; the slot is a mask); `push`'s `n + 1` wraps below the checked capacity. The index + 1 encodings of the delta chain, the mailbox and the synapse chain (`PlasticDelta::{new, block, next_delta, link}`, `DeltaChain::next`, `delta_head`, `set_delta_head`, `push_delta`, `MailboxDrain::next`, `mailbox_push`, `FanOut::next`, `Chain::next`, `next`, `target`, `link`, `set_synapse`, `first_block`, `set_first_block`) add and subtract one by `wrapping_*` beside the `u32::MAX`/zero refusal each already had; every budget and countdown (`remaining`, `bac_plateau_ticks`, `refractory_ticks`) steps by `saturating_sub(1)` under its `> 0` guard. Integration: `leak`'s two negations became one `unsigned_abs` (exact at `i32::MIN`) and its step `saturating_sub`/`saturating_add`; `add`'s widened sum, the compartment differences and their coupling sum, the adaptive threshold's excess and decay, `saturating_*` on the Q16.16 fields. Plasticity: every widened intermediate of `stp_decay_factor_q16`, `relax_q0_8`, `step_stp` and `window` (`saturating_mul`/`saturating_add`/`saturating_sub`, each bounded below $2^{63}$); `synaptic_efficacy_q16`'s triple product by `saturating_mul` (the exhaustive test covers every input). Test helpers: counters became iterators, a weight table became a literal, tick differences `wrapping_sub`, a guarded latency `abs_diff`, the steady-cycle slices `saturating_sub`. The runtime's determinism pin (`PINNED_ARENA_HASH`, `PINNED_SPIKE_COUNT`), the oscillator periods (1 527, 3 793, 2 433), the efficacy table, the exhaustive efficacy test and every asserted value held in both profiles; the existing `.cargo/mutants.toml` exclusions for `add` and `leak` still name their sites. Two theoretical differences, both unreachable: the wheel's tick at $2^{64}$ advances (a debug panic became the wrap the release profile had; the slot is a mask either way) and the private `leak` at a zero shift with `i32::MIN` (never called: the shifts are the constants 9, 10 and 11). The mutation gate's first CI run on these lines found three survivors: two comparisons the conversions had made equivalent at their bound (the plateau countdown's `> 0` beside a `saturating_sub`, now a `checked_sub` with no comparison; `relax_q0_8`'s `gap > 0` after the zero gap returned above, now `is_positive`) and one test gap, the negative leak's exact step, now pinned with the positive one (a basal potential of 1.0 leaks by 2^-9, an apical by 2^-10, a displacement of three LSB by one).

- **Brief 016, part one: sixteen crates under the arithmetic lint, ninety-seven sites converted by name, one result changed where the domain cannot reach.** `#![deny(clippy::arithmetic_side_effects)]` in `cortex-sensory`, `cortex-homeostasis`, `cortex-neuromod`, `cortex-thalamus`, `cortex-arithmetic`, `cortex-imagination`, `cortex-knowledge`, `cortex-symbolic`, `cortex-workspace`, `cortex-connectome`, `cortex-social`, `cortex-cerebellum`, `cortex-affect`, `cortex-linguistic`, `cortex-reasoning` and `cortex-embodiment` (and in `tests/spsc.rs`, its own crate root): thirty-one crates carry it; `cortex-core` and the runtime follow. The fifteen small crates (sixty-seven sites): `checked_div`/`checked_rem` with the refusal the zero check above already gives (the branching ratio, `rebase`'s modular sum, the section record count and shape); `abs_diff` where a subtraction was guarded by a comparison (the anomaly, the sincerity gap, the stake and mirth averages, the ignition distance); `saturating_*` on Q16.16 state fields and widened products (dopamine decay, the relay gain, the wander fraction, trust, the affect load and mood, the recurrent cell, the cerebellar product, the ignition threshold); `wrapping_*` where a bound is checked a line above (the term arena's stack tops and trail counts, the cerebellar ring head and fill, the linguistic order loops, the parent and speech-act encodings after their refusals); `checked_sub(1)` for the index + 1 decodings (`parent`, `intended_act`, `TermNode::child`, `Binding::term`, `term_of_literal`); `checked_rem(..).unwrap_or(0)` for `OP_REM` (the same 0 for `i128::MIN % −1`); a `const fn sign_bit(slot)` for the cerebellar sign shift; function-local constants where a literal was negated or summed; `for _ in 0..8` for the CRC loop; test counters by `wrapping_add`. One result changed, for an input the domain cannot reach: `unify_inner` binding a variable to the term at index `u32::MAX` (which needs $2^{32}$ nodes for `deref` to accept it) now answers `Malformed` with its bindings undone, where the release profile had wrapped the binding to 0, left the variable unbound and reported `Unified`; no test can build that arena, so the change is recorded here and in F-4 rather than in an ADR. `cortex-embodiment` (thirty sites): the torque decoder's `agonist − antagonist` and its product with the gain saturate in `i64` (the widening already bounded them); the SPSC ring's cursors advance and are compared by `wrapping_*` (`len`, `producer_claim`, `producer_publish`, `consumer_release`; the record's doc comment now says the cursors are monotonic counters that advance and compare by wrapping arithmetic: the only four sites whose debug-profile behaviour moved, from a panic on a cursor a foreign writer had corrupted to the wrap the release profile already had, so no result the engine's profile produces changed); the voice's `mul_q16` saturates its `i64` product (no operand pair in the crate reaches $2^{63}$), the exponential and cosine series (`exp_neg_q16`, `cos_q16`) and `shape`'s factors add and subtract by `saturating_*`, `Resonator::new` multiplies by `saturating_mul`, divides by the non-zero sample rate with `checked_div` (the same refusal as the zero check above it) and negates by `saturating_neg`, `step` adds by `saturating_add`, `VocalSynth::from_frame` divides by the non-zero fundamental with `checked_div`, `draw` subtracts by `wrapping_sub` (the sixteen high bits minus `0x8000` cannot wrap), `next_source` multiplies and adds by `saturating_*` and advances the phase by `wrapping_add` (reset before it reaches the period); $2\pi$ became the compile-time constant `TWO_PI_Q16`. The render pin, the resonator coefficient pins, the exponential pins and the shaping fractions did not move; the crate's tests, the release profile and the voice benchmark held. The mutation gate's first CI run on the part-one lines found six mutants no test constrained (the stake, the mirth and the anomaly at an input equal to the field, the falling mirth step, and the cerebellar zero delay, which a `||` or a `>=` would have let read the ring); four tests now pin them as fixed points, exact steps and a ring never read.

- **Self-amendment (ADR-0031; whitepaper 4.4.0; image format 10).** A directive asked for "verifiable self-code evolution and safe digital self-improvement": the engine proposing, verifying and incorporating changes to its own code or rules in a closed loop. Decided: two lanes, and the word that decides them is *verifiable*, a test the engine can run. The engine may amend a **parameter** of its own policy, from a registry with owners and bounds (`cortex-executive::REGISTRY`: the clock sweep's quiet bound and budget today; `OWNER_VETO_GATE` is defined and carried by no entry, so the gate's own parameters are out of reach by a test), through four gates a new 64-byte record keeps in order (`PolicyAmendment`, the second record of `cortex-executive` under ADR-0016's test): the bounds (`propose`, every proposal a record, a rejected one with its reason), the veto gate of `cortex-ethics` consulted by id (`Executor::admit` refuses a gate evaluated on another proposal; a gate not evaluated admits nothing), a **trial in two forks of the image** that must hash to the same behaviour (`cortex_runtime::trial::run`: `Image::decode` twice, the candidate under the proposed value, the same ticks, injections and sweep cadence; CRC-64 over every unit's bytes with evicted ones read from the log, every block, the spike train, the delivered and dropped counts), and a cost that fell by the amendment's `min_gain` and by one (`OBJECTIVE_RESIDENT_UNITS`, `OBJECTIVE_REHYDRATIONS`). The rule that makes the lane verifiable: **the engine may change what it costs, never what it does.** The verdict enters the arena only through `cortex_runtime::trial::run`, which refuses an image written before the last commit to the parameter (`StaleBaseline`), a fork that does not end at a quiescent point (a pending mailbox names the delivering worker's pool node) and a spike train not wholly traced; `Executor::commit` applies it between ticks and refuses a superseded one (a later proposal for the parameter committed first, so commits keep the arena's order, which the loader replays) and a stale one (the live value moved since the trial); `sweep_by_policy` runs under the policy; the arena is section kind 41 of the image, from which the loader replays the committed records into the policy in order and refuses a record the state machine could not have produced (`is_well_formed`; `ImageError::MalformedAmendment`), so a run is still `(image, seed, trace)`. The engine never amends its own **code**: no compiler, interpreter or code loader is in the process, and a proposed rule leaves as a frame to the amendment register (`cortex-tools`: `TOOL_CATEGORY_AMENDMENT_REGISTER` 0x0006, `ACTION_FILE_PROPOSAL`, `ACTION_RECORD_COMMIT`; the broker Specified), where the repository's gates (ADR-0029, ADR-0030) and a maintainer are the verifier. Not adopted, with reasons in the ADR: an in-process interpreter or JIT, `rustc` behind the broker with a hot reload, a new crate, a behaviour-changing objective, the policy in the header, a commit inside the tick. The runtime depends on `cortex-executive` and `cortex-ethics` by path, as ADR-0023 intends; no state crate gained a dependency. Whitepaper §1.2 (FR-10), §1.5 (the boundary moved), §1.6, §3.2, §5.1, §5.2.2, §5.2.10 (the record's layout and rules), §5.2.21, §5.2.28, §6.16 (R-16), §8.6, §8.7, §8.8, §8.10, §8.18 (what the engine may change about itself, and what it may not), §9, §11.1 (which parameters join the registry is open), §12, Appendix A (row 41), B (the property-kit count), C (M9); README; the reader's guide; `CLAUDE.md` (an invariant); `CONTRIBUTING.md` (a code rule); `SECURITY.md` (in scope). 29 new tests (17 in `cortex-executive`, one more than the layout test it had, two of them property tests over the lattice and a seeded walk of twenty thousand histories; 12 in `runtime/cortex-runtime/tests/amendment.rs`, the loop end to end, its refusals, the sweep cadence, the commit order, the stale commit, persistence and ten forged records); 364 in all. A reviewer pass before the merge found, and the second commit fixed: a live executor whose commits to one parameter were out of index order wrote an image its own loader refused (now `Superseded`); a trial on an image written before a later commit measured the gain against a baseline that was not the live one (now `StaleBaseline`); a fork ending with a pending mailbox hashed the delivering worker's pool node, so identical forks on four workers hashed differently (now the fork must be quiescent); the spike train was hashed only up to the trace capacity and not at all at zero (now `NoTrace`); the test network could not spike; a zero id was well-formed on a live record; `amendment_room` read the allocation's capacity rather than the configured one; a refused injection reported as `NotQuiescent`. The mutation gate on the diff passed in CI after its first run caught seven mutants in the trial's cost, injection and sweep-cadence lines that no test constrained, and its second one redundant match arm, removed (the local run had reported the seven unviable: on Windows a transient link failure in the tool's build copy reads as unviable, so the CI run is the gate's truth).

- **Verification governance (ADR-0030; whitepaper 4.3.0).** A directive asked for mutation testing, a red–green–invariant protocol, in-house property fuzzing and formal verification. Adopted, with no manifest change: a **mutation gate** on every pull request (`cargo-mutants` 27.1.0, pinned; `--in-diff` over the lines the request changes; one survivor fails; `.cargo/mutants.toml` excludes the equivalent mutants by name with the reason); a **property kit** (`testkit/prop.rs`: a Knuth MMIX generator and the `i32`/`u32`/`i16`/`u8`/`i128` lattices, `include!`d into `mod prop` of `cortex-core`'s membrane, plasticity, synapse and wheel, `cortex-arithmetic`, `cortex-cerebellum` and `cortex-embodiment`'s voice; TC-2 holds); **exhaustive** `#[ignore]`d tests where a domain is $2^{32}$ or smaller (`synaptic_efficacy_q16` over every input); the tests in the **release profile** as a blocking step; a **determinism pin** (the seeded 128-unit network's CRC-64 after 20 000 ticks, checked by CI on `ubuntu-24.04` and `ubuntu-24.04-arm`: T-1's two-architecture form). The whole-tree mutation run (2 214 mutants) found 141 survivors: 45 equivalent by construction, 96 test gaps now closed (finding F-26): a default gate's reset sign never asserted negative; rest not asserted a fixed point of integration; the threshold's decay rate; the cerebellar sign bit; the relaxation rounding; the walk's compartment flag; three autonomic cuts and the basal-ganglia selection at equality; the affect stake's direction and the calm boundary; the voice's shaping fractions, its exponential series, its render; the imagination walk; the anomaly average; the tact, prosody and role-order boundaries; the sincerity gap and the common ground; the attention-schema hash; the spatial decay; the term arena's bounds; the loader's validation clauses one by one; the sweep's quiet bound; the trace accounting; a cyclic chain; a double activation; the log's entry count. Not adopted, with reasons in the ADR: a red commit per rule, `cargo-kani`, Miri, `loom`, coverage, `proptest`. The gates' first run on the pull request that introduced them caught two things: the mutation gate found that no test distinguished a zero motor command from a negative one in `CerebellarMicrozone` (a test now does), and the release-profile step found that the two-worker contention test asserted a timing-dependent fact (that both workers delivered something), which on a four-core runner in release is not guaranteed; exactly-once delivery is asserted, the shares are reported. Whitepaper §8.3, §10 T-1, Appendix B (V-6, V-7, the release row, the local command list, three directives), Appendix C M7; `CONTRIBUTING.md` definition of done; `CLAUDE.md`; the brief template. 47 new tests; 332 in all.
- **Structural enforcement of the invariants that were held by review (ADR-0029; whitepaper 4.2.3).** An audit of where each invariant of §2.2 is enforced found TC-4 (no floats) and TC-9 (no `unsafe` in state crates) held by spec-guard directives alone, TC-2 (no dependencies in state crates) by review alone, saturating arithmetic (§8.1, F-4) by review alone, and rustdoc never built in CI. Now: `[workspace.lints]` forbids `unsafe_code` and denies `clippy::disallowed_types` over `f32`/`f64` (`clippy.toml`), inherited by every state crate and the benchmark crate; the runtime keeps its one `unsafe` under its own table with the float rule; `scripts/check-deps.mjs` (`npm run spec:deps`, inside `npm run spec`) holds TC-2 from the manifests with no cargo and no dependency; `cargo doc --workspace --no-deps` with `RUSTDOCFLAGS="-D warnings"` is a blocking step and a documented command; `#![deny(clippy::arithmetic_side_effects)]` in the twelve state crates that pass it, held by a spec-guard count that only grows; the three CI actions are pinned to commits with their versions in comments and the runner to `ubuntu-24.04`; `.gitattributes` (`* text=auto eol=lf`) and `.editorconfig` normalise line endings (the index was already LF). Whitepaper §2.2 rows TC-2, TC-4, TC-9 and F-4, Appendix B (V-5, the rustdoc gate, the local command list), five spec-guard directives; `CLAUDE.md` and `CONTRIBUTING.md` name the enforcement beside each invariant. Not adopted, with reasons in the ADR: Dependabot, `cargo-deny`, Miri, a Windows CI leg, branch protection that requires the checks (a repository setting, recommended to the owner).
- **Four faculties as integer rules (ADR-0026, ADR-0027; whitepaper 4.2.0; image format version 8; frame ABI version 2).** A directive asked for social acumen, paradigm innovation, vocal expression and humor as "universal faculties". Every part with an $O(1)$ integer rule is now a tested rule in the crate that owns the quantity: `cortex-social` `assess_sincerity` (the stated-versus-outcome gap, its slow average, trust broken at 0.5, suspect at 0.25), `expect_of_self`, `would_surprise`, `tom_depth` (derived); `cortex-linguistic` `mark_indirect` (the intended act beside the surface act), `apply_face` (tact: bad news to a courteous or formal listener is softened, last), `mark_play` (`PROSODY_PLAYFUL` at the mirth threshold unless the register is formal); `cortex-knowledge` `note_anomaly` (a slow average of unexplained error that marks the framework stale at 0.5, once) and `re_represent` (a new category, the epoch, a halved anomaly, the rotation recorded); `cortex-symbolic` `rebase` (composed cyclic rotations, `FLAG_REBASED`, the padding byte now `rebase_count`); `cortex-imagination` `wander_at`; `cortex-embodiment` `VocalFrame` (a second egress record: fundamental, three formants and bandwidths, amplitude, jitter, shimmer, aspiration, voicing, rate, seed), `Resonator` (coefficients by Q16.16 series within $3 \times 10^{-5}$ of the real formulas), `VocalSynth` (a seeded impulse train with aspiration through three resonators, sixteen samples per epoch) and `shape` (the voice by marker, register and valence); `cortex-affect` `appraise_incongruity` (benign when the threat is at most 0.25; `mirth_q16`); `cortex-neuromod` `reward` and `decay_dopamine`. Not added: `recursive_tom_depth` (derived), `pragmatic_subtext_hash` (the intended act is in the frame), the humor fields in `cortex-agency` (the appraisal is affect's), parameters in `TorqueFrame` (a voice is not a joint). Whitepaper §8.14–§8.17 say what is and is not claimed; R-14 and R-15; §8.8 rows; H-5 and H-6; §12; references 38–44. Benchmark `vocal/render_x16`. A review before merge closed six defects of the first cut (the resonator accepted a bandwidth outside the exponential's range, now refused above a thirty-second of the rate; `decay_dopamine` overflowed on a shift past the width and on the most negative signal; the generator discarded bit 0 of the seed; `rebase` refused 65 536 dimensions; `re_represent` accepted a call that changed nothing; an expectation of the self could never be withdrawn, now cleared by `close_exchange`) and wrote down two orderings it found undocumented (tact on a sealed frame is a no-op; play is marked after the recurrent cell, which would otherwise overwrite it). 19 new tests; 274 in all.
- **A term arena and first-order unification (brief 014, ADR-0025).** `cortex-reasoning` gains its second record, `TermNode` (64 B: kind, arity up to 8, functor as a `cortex-symbolic` concept id or a variable number, eight child indices + 1), admitted under ADR-0016's test; `unify` is Robinson's algorithm without recursion over caller-provided slices (a work stack of pairs whose length is the bound, a `Binding` table indexed by variable number, a trail of the variables bound), with the occurs check as a walk in the free part of the stack, every failure (`Clash`, `OccursCheck`, `BoundExceeded`, `Malformed`) undoing the trail; `deref` and `undo`; a first-order literal is a term index + 1 with the sign in bit 31; `resolve_first_order` unifies the first complementary pair and `SymbolicRuleNode::record_resolvent` stores the resolvent (the propositional `apply_resolution` is now `resolve` followed by it). Socrates is mortal in two steps. Whitepaper §11.1's open question is resolved; §5.2.30, §6.10, §8.8, Appendix A (rows 38–40; `SECTION_TERM` = 40), §9, §12 follow. 8 new tests; 255 in all.
- **The `.cortex` image, the clock sweep and the Tier-2 delta record (brief 015, ADR-0024; milestone M4; image format version 7).** `cortex-connectome` gains `SectionEntry` (kind = Appendix A row, record size, offset, length, CRC), a table-free streaming CRC-64/XZ with the published check value, and `CortexFileHeader::{new, validate, encode, decode}` (`section_count` in the header, the checksum over all 64 bytes). `cortex-core` gains `PlasticDelta` (16 B, index + 1 chained), a 32-bit `plastic_delta_head` at `[60..64)` (finding F-20 resolved), and explicit little-endian `encode`/`decode` for both arena records. `runtime/cortex-runtime` gains `Image::{write, open, encode, decode}` (read-into-arenas, fails closed on a foreign version, a bad checksum, a truncated file, a malformed directory, a unit not at rest, a dangling index, an over-horizon delay or more blocks than a token can name; wakes units not at rest), `Executor::{attach_log, sweep, is_quiescent, deltas}` and re-hydration from the write-ahead log after the tick that delivers a message to an evicted unit. Finding F-23 resolved: Appendix A follows the token (67 108 864 blocks). The M4 exit test passes: a run that sweeps and re-hydrates ends in an image byte-identical to one that never evicted. `mmap`, slot reclamation and a hot checkpoint stay Specified. 16 new tests; 247 in all.
- **The executor (brief 012, ADR-0023; milestone M2).** `runtime/cortex-runtime`, a `std` crate outside `crates/` that composes the state crates: a fixed pool of worker threads (worker 0 the caller of `tick`), an in-house bounded Chase–Lev deque of atomics per worker, a wheel per worker, per-worker mailbox node pools with lock-free free lists, a bounded MPMC injector ring as the only way in from other threads, and a sense-reversing spin barrier between the three phases of a tick: turns (drain, `sort_unstable` the batch, integrate, `step_stp`, active set), fan-out (STDP, release, schedule or push) and deliveries (advance, push due releases, drain the injector). A message pushed at tick $t$ is integrated at $t + 1$; a delay $d$ synapse at $t + d$. The arenas are `UnsafeCell` cells behind the one `unsafe` accessor in the workspace, sound because no `&mut` to a record ever overlaps another reference to it (the phases). Nothing allocates after `Executor::new` (a counting allocator over 20 000 ticks). The M2 exit test passes: 10⁶ events from four producers into 64 units, delivered exactly once on one, two and four workers; the first T-1 differential test passes: a 128-unit network with STDP and the oscillator ring are bit-identical on one and four workers. Core pinning is Specified (`sched_setaffinity`). Benchmark `executor/push_to_turn`. No dependency entered. 20 new tests; 231 in all.
- **Synaptic fan-out and STDP (brief 013, ADR-0022; milestone M3; image format version 6).** `SynapseBlock` stores every index as index + 1, so zero is an empty slot, the end of a chain or a unit without fan-out and a zeroed arena is empty; `fan_out` and `chain` walk a chain bounded by the arena; `release` stores the efficacy of the spike per slot for a delayed delivery to read back; `synapse_token` (block and slot in 28 bits) and `spike_message` (an 18-bit efficacy and the compartment) are the wheel and mailbox encodings; `apical_mask` says which compartment a slot lands in; `step_stdp` is the nearest-neighbour pair rule at the presynaptic spike with $A_+ = 0.0100$, $A_- = 0.0105$, $\tau = 2^{11}$ ticks, the window by `stp_decay_factor_q16`, saturating in Q1.15. R-1 step 6 is Implemented; the M3 exit test `tests/oscillator.rs` oscillates three units with periods exact to the tick over a hundred cycles for three delay triples. Two benchmarks (`synapse/fan_out_x8`, `synapse/step_stdp`). Finding F-23 opened: the token addresses $2^{26}$ blocks against Appendix A's $2^{27}$. 14 new tests; 211 in all.
- **Briefs 012–015**, the next rounds after the state model: 012 the executor (a runtime crate outside `crates/`, core-pinned workers, a work-stealing deque decided by ADR against ADR-0005's allow-list, batch draining with the §8.3 order; milestone M2), 013 fan-out through `SynapseBlock` chains and pair-based STDP with the three-neuron oscillator exit test (M3), 014 a term arena and first-order unification as `cortex-reasoning`'s second record under ADR-0016's test (R-10's native track), 015 the `.cortex` section directory, CRC-64, loader and writer, the clock sweep and the Tier-2 delta record resolving finding F-20 (M4). Each carries a spec-guard precondition directive; Appendix C names them.
- **Briefs 009–011**, the first rounds of milestones M1 and M5 on `cortex-core`: 009 the turn gate and the lock-free mailbox (ADR-0006 as code; finding F-19), 010 short-term plasticity in integers (the §8.8 forms on the Q0.8 fields), 011 membrane integration (leak, threshold, refractory period, BAC). Each carries a spec-guard precondition directive.
- **Fourteen state crates (ADR-0016, proposed; the workspace has thirty-two).** `cortex-thalamus` (relay gate: tonic, burst, closed), `cortex-linguistic` (the three-layer native language record: construction-grammar frames over vector-symbolic roles with a Q16.16 linear recurrent cell for prosody; no external language model), `cortex-tools` (brokered tool-invocation frames with a five-state machine), `cortex-attention` (saccade and fixation), `cortex-affect` (allostatic load, comfort, mood), `cortex-autonomic` (voltage, temperature, power limits and cut flags), `cortex-spatial` (path integration with a wrapping heading and a landmark fix), `cortex-curiosity` (novelty decay and urgency), `cortex-social` (empathic resonance and asymmetric trust), `cortex-ethics` (deontological veto: imperative, harm, authorization, benefit never overrides), `cortex-knowledge` (consolidated affordances and hazard), `cortex-reasoning` (AND / OR / NOT / IMPLIES rule nodes), `cortex-arithmetic` (checked 128-bit and Q16.16 opcodes with error flags), `cortex-imagination` (sandboxed rollout frames). Each is a 64-byte `#[repr(C, align(64))]` record with a compile-time assertion, one deterministic saturating rule and four to nine boundary tests (75 new tests; 145 in all). Two width defects and one layout defect in the proposal were corrected on the way in: Q16.16 fields are 32 bits, and 128-bit operands are `u64` low and `i64` high words rather than tuples. The maintainers moved three boundaries to admit them: whitepaper §1.5 puts a brokered digital environment and a language model in scope, §8.10 adds a tool-broker process outside the worker seccomp filter, and §8.9 adds the veto gate and the vitals flags in front of the watchdog. The record also states the six-part admission test the next crate must pass, and the responsibilities of `cortex-sensory`, `cortex-homeostasis`, `cortex-agency`, `cortex-executive`, `cortex-symbolic` and `cortex-hippocampus` are narrowed so that every quantity keeps one owner. Whitepaper §1.6, §3, §5, §6.8, §6.9, §8.8 to §8.10, §11.1, §12 and Appendices A, C and D, the README, `CLAUDE.md`, `CONTRIBUTING.md` and the reader's guide follow; the member-count assertions move from 18 to 32 and fourteen test-module assertions and fourteen record assertions are added. The image format version is unchanged: no existing record changed, and the new arenas get sections when the loader exists (milestone M4).
- **Briefs 004–008** for the remaining open findings: 004 cerebellar delay line (F-8), 005 timing-wheel geometry and drain (F-11), 006 the first benchmark harness and protocol (F-13), 007 a test for every public function (F-14), 008 embodiment frame rings (F-17). Each carries a spec-guard precondition directive, so a brief whose defect is fixed without archiving it fails CI. F-5 is not a brief: it is ADR-0009's decision.
- **`CLAUDE.md`**: the switchboard for coding agents — eight principles, the code invariants, a reading map and the exact command set CI runs.
- **`briefs/`**: numbered, self-contained prompts for rounds of work, with a README defining the mandatory sections and the archive procedure; three seed briefs (001 saturating arithmetic, 002 `no_std` and derives, 003 synaptic weight Q-format); `scripts/check-briefs.mjs` enforces the sections and runs as `npm run spec:briefs` locally and in CI. Live briefs are also read by `spec-guard` (precondition directives) and `spec-graph` (deliverables as obligations).

### Changed

- **The learning runs leave the gate ([ADR-0061](docs/adr/0061-the-learning-runs-leave-the-gate.md), amending ADR-0060).** Brief 027 put five runs of $2^{21}$ ticks at 256 units into the pull request's gate, and ADR-0058's sweep runs the runtime's suite once per runtime mutant: after it, run `35444829805` on `main` measured the runtime suite at 709 to 854 s on the hosted runners (216 to 464 s before) and the six runtime shards at 3 h 27 m to 4 h 19 m (1 h 10 m to 1 h 58 m before), every one over ADR-0058's three-hour mark and the slowest 71 minutes from the job's limit. The five runs are now `#[ignore]`d `exhaustive` tests, unchanged in their tables and trace and passing in the release profile, and the gate runs `the_first_block_of_the_rewarded_run_at_256_units`, sixty-four trials held to the first row of the pinned table; the criterion's test, which reads the tables, stays.
- **A brief's standing directives restate Latest ≠ Newest again, and the brief checker holds it (finding F-39).** Briefs 004 to 020 each carried `CLAUDE.md`'s principle 4 as a standing directive; briefs 021 to 028 carried none, because a brief is written by copying the newest one and `scripts/check-briefs.mjs` checked that the section's heading existed, not what it held. Brief 028 now carries the directive, applied to what its round could be tempted to adopt (a test runner, the `mutants` crate, a newer `cargo-mutants`); the checker fails a live brief numbered 028 or later whose standing directives do not name it, and exports its check so `scripts/check-briefs.test.mjs` can test it under `node:test` (eight cases, no dependency), which `npm run spec` and the specs job now run. Brief 027 is below the floor and unchanged: it was being executed when the check landed.
- **spec-guard 0.5.0 -> 0.11.0 and spec-graph 0.3.0 -> 0.8.0, exact pins as before, and `--ignore-status` on `spec:guard`.** Measured on this tree before the bump rather than inferred from the tools' changelogs. *spec-guard:* 212 of the 213 assertions give identical results; the other is a count that became correct: `CATEGORY_FORWARD` in `cortex-reasoning` appears fourteen times in code and three times in doc comments, and 0.5.0 counted the `///` comment at `category.rs:104` as code, the Rust lexing defect (a lifetime or quote opening a character literal) that 0.10.3 fixed along with raw strings of two or more hashes. The verdict stays `min="1"` and passes either way. Since 0.6.0 spec-guard withholds the directives of a `draft`, `proposed`, `rejected`, `deprecated` or `superseded` document; every live brief is `proposed`, so without `--ignore-status` the precondition directives `briefs/README.md` describes would be reported and never run. The flag keeps the semantics of 0.5.0, which executed everything; nothing is withheld today (58 accepted, 26 archived, one proposed brief with no directives), and a directive over `package.json` now holds the flag in place. *spec-graph:* every verdict is the same and every `link`, `structural` and `front-matter` relation is the same (545, 253, 47); 0.8.0 resolves 129 fewer `text` relations, all of them a prose mention such as "brief 002" resolved to that brief, because a title no longer registers an alias for its file. A probe showed no check stood on them: a dangling prose "brief 099" was ignored by 0.3.0, 0.7.0 and 0.8.0 alike, while all three failed a broken link to `briefs/099_nothing.md` and a dangling `ADR-0099`. The repository keeps no baseline, so 0.8.0's re-fingerprinting asks nothing of it.
- **The weekly mutation sweep runs per area, with a timeout matched to the area and a check that refuses a partial sweep ([ADR-0058](docs/adr/0058-the-weekly-sweep-and-its-timeouts.md); finding F-38).** The weekly level had never completed a sweep: the only scheduled run on record was cancelled by its own `timeout-minutes: 300` after 5 h 00 m 57 s with 1 233 of 3 396 mutants tested, and its `if: always()` upload published a survivor list drawn from 36 per cent of the tree. The artifact says why: fourteen timeouts at the tool's automatic bound (five times the *workspace* test baseline of 434 s, although a mutant runs only its own package's tests) were 96.3 % of the run's 31 597 seconds, and the median mutant costs zero. `.github/workflows/ci.yml` now runs the exhaustive tests in one job and the mutation sweep in a matrix of two, `crates/**` and `runtime/**`, each first running its own tests in a timed step and taking three times that elapsed time plus thirty seconds as the per-mutant bound (bounds written down from a developer machine were refused by the run that measured them: `--timeout` bounds the tool's baseline too, and the runtime's 140-second suite had not finished in 420 on a hosted runner), then sweeping with `--baseline=skip`, the runtime area in six round-robin shards because its 979 mutants against a 359-second suite reached 590 in 5 h 23 m while the whole of `crates/**` swept in 24 minutes, and each followed by a step that compares the outcome counts with `cargo mutants --list` under the same filter and fails the job when they differ. Measured: **the whole tree swept for the first time**, 3 396 of 3 396 mutants with every job green and the slowest shard in 1 h 58 m (`crates/**` in 27 minutes); 21 survivors, which whitepaper §11.1 carries as the next round's list. Nothing about the engine changed; the in-diff gate, the required check names and every rule are untouched.
- **Image format 10 (ADR-0031).** The amendment section (kind 41); a version-9 image fails closed, as every bump does; no record moved.
- `CONTRIBUTING.md` said the arithmetic lint was denied in twelve crates and reviewed in the other twenty; fifteen and seventeen since the three crates of 2026-09-10 (F-4, brief 016).

- **Computational phenomenology and native cognitive language (ADR-0020, ADR-0021; whitepaper 4.1.0; image format version 5).** A directive asked for seven tiers of "synthetic qualia" and four pillars of native language. Every tier or pillar with an $O(1)$ integer form is now a tested rule in the crate that owns the quantity, carved from reserved bytes of six records: `cortex-affect` `update_valence` (valence as $F_{\text{prev}} - F_{\text{now}}$; the existential stake as a slow average of $\lvert dF/dt \rvert$ with a one-LSB floor) and `metaphor_source_domain` (heat, weight, dusk, calm); `cortex-workspace` `step_ignition_at` (threshold scaled by $1 + \min(\lvert\sigma - 1\rvert, 1)$) and `update_attention_schema` (a hash of the broadcast state); `cortex-homeostasis` `update_branching_ratio`; `cortex-imagination` `reflect` (the self-model's fixed point) and `wander` (seeded, temperature-bounded); `cortex-symbolic` `blend` (the header of $\text{target} \otimes M \oplus \text{source}$); `cortex-linguistic` `bind_child`, `set_parent`, `attach_metaphor`, templates `TEMPLATE_RELATIVE` and `TEMPLATE_CAUSAL`, the `ROLE_CHILD` realisation position; `cortex-social` the turn-taking machine (`take_turn`, `yield_turn`, `request_repair`, `ground`, `close_exchange`) and `register`. Two proposed tiers (attractor-manifold topology, information closure) and two proposed fields got no field, since no $O(1)$ integer rule exists for them; $\sigma$ stays with its one owner. Whitepaper §8.12 states what is not claimed: the rules are mechanisms and whether they constitute experience is hypothesis H-3, which the document does not assert; §8.13, R-12, R-13, §8.8 rows, §11.1 (H-3, H-4), §12, Appendix D follow; ADR-0016 to ADR-0019 are marked accepted, as their merges made them. A review before merge closed six defects of the first cut (the causal template dropped its affect tag; a parent index of 0 was ambiguous, now index + 1; calm ignored the energy deficit; the workspace documented a 300-tick hold it did not implement; a fresh canvas frame reported a fixed point; the generator could reseed mid-rollout) and one gap (the other party could not open an exchange: `listen`), and gave the mood the one-LSB floor the stake already had. 16 new tests; 197 in all.
- **Finding F-17 narrowed: the torque decoder exists.** `TorqueFrame::from_burst_counts(epoch, agonist, antagonist, gain_q16)` decodes one period's layer-5 activity by a push–pull rate code, per joint `(agonist − antagonist) × gain` in Q16.16, widened and clamped; four tests (zero frame, cancellation and sign, linear gain and both clamps, determinism). Whitepaper §5.2.4, §6.4 (step 2 Implemented), §8.8 (a row), §11 (F-17 now names only the mapping and the 1 ms loop), Appendix C (M6). No record changed.
- **Prover opcodes are vendor-agnostic; mathematics is described on two tracks.** `ACTION_VERIFY_LEAN4` and `ACTION_SOLVE_SMT_Z3` are renamed `ACTION_VERIFY_PROOF` (check a proof term against axioms) and `ACTION_SOLVE_CONSTRAINTS` (decide a formula by constraint or SMT solving), and `ACTION_SYMBOLIC_EVAL` (0x0003, simplify and rewrite a term) is added; `is_known_action` and the tests follow. No product name remains an identifier under `crates/`, and two executable assertions hold that. Whitepaper §3.2, §5.2.21 and §8.10 name actions, not products; §6.10 is rewritten as two tracks: native resolution and exact arithmetic that are complete offline, and optional brokered acceleration through whatever verification system the broker's operator configured, returning only a certificate hash; §8.8's row follows. ADR-0016 records the principle and that its first revision of the opcodes named products and was corrected.
- **Short-term plasticity (brief 010 executed, ADR-0019).** `DendriticSuperNeuron::step_stp(elapsed_ticks)` is one presynaptic spike: `stp_u_rel` relaxes toward `STP_U` (51/256) with τ_f = 2¹⁴ ticks and `stp_r_ves` toward 255 with τ_d = 2¹⁵ ticks by the factor (1 − 2⁻ᵏ)^Δt, computed in Q16.16 by binary exponentiation (`stp_decay_factor_q16`, at most 32 multiplications, no table), rounded to nearest and by at least one LSB so a rest is reached exactly; then u facilitates by U(1 − u), the pair (u, R) the release uses is returned for `synaptic_efficacy_q16`, and R is depleted by uR. The fields stay Q0.8; the record is unchanged. Six tests (the factor's endpoints and monotonicity; the first spike from rest gives (92, 255) and leaves 163; a 50 Hz train reaches its fixed point within sixty spikes with depression dominant; silence restores rest exactly and one tick still moves the last LSB; the fields never leave [0, 255]; determinism over 10⁴ spikes) and a benchmark `stp/step_stp`. §8.1 gains the Q0.8 rounding rule and the binary-exponentiation rule; the §8.8 row is Implemented. Brief 010 is archived; no live briefs remain. 177 tests.
- **Membrane integration (brief 011 executed, ADR-0018).** `DendriticSuperNeuron::integrate(basal, apical, now_tick)` is one fine tick: per-compartment shift leaks (soma 2⁻¹¹, basal 2⁻⁹, apical 2⁻¹⁰, each taking at least one LSB so rest is reached exactly), inputs into the compartments, the soma driven by 2⁻⁴ of its difference to each compartment, a positive adaptive threshold (steps up 0.02 per spike, decays to 1.0), reset to −0.25, a 200-tick refractory window that drops inputs, and a BAC plateau when the apical compartment is at or above 0.5 at the spike (200 ticks, apical coupling ×4, 50-tick burst window); a threshold at or below zero never fires, so the resting record fails closed; `ticks_since_spike` is the wrap-safe comparison of §8.4. Ten tests (exact return to rest from both signs, the sub-threshold fixed point, the fire tick under constant drive in a predicted window, the refractory count, saturation, the silent unconfigured unit, the wrap, determinism over 10⁵ ticks, the plateau, adaptation to the base) and a benchmark `neuron/integrate`. Whitepaper R-1 step 5 and the LIF and BAC rows of §8.8 are Implemented; milestone M5 reaches the neuron. Brief 011 is archived; brief 010 remains live. 171 tests.
- **Theorem-proving and document-auditing pipelines, without a new crate.** `cortex-reasoning` gains `OP_EQUIV`, a literal encoding (atom with a sign bit; atom 0 is no literal), Robinson's resolution on two-literal clauses (`resolve`, `negate`, `complementary`, `is_tautology`), `apply_resolution` with a second-parent field `resolved_with_idx` and `is_refutation`; a two-step refutation of modus ponens is a test. `cortex-tools` names two brokered categories, `TOOL_CATEGORY_FORMAL_PROVER` (Lean 4 check, Z3 solve) and `TOOL_CATEGORY_DOC_ENGINE` (parse structure, extract entities, search cross-references), with `is_known_action` as the engine-side mirror of the broker's allow-list and `new_call` that refuses an unknown action. `cortex-knowledge` gains `certify` and the theorem bit. `cortex-attention` gains document foveation (`MODE_DOCUMENT_FOVEATION`, `document_target`). Whitepaper §3.2 (two interface rows), §5.2.21, §5.2.22, §5.2.29, §5.2.30 (new layout), §6.10 (R-10, theorem proving through the broker), §6.11 (R-11, saccadic document reading and auditing), §8.8 (two rows), §8.10, §11.1 (a term arena is an open question), Appendix C (M8); ADR-0016 records that the count is locked and that capabilities arrive as protocols, not crates. First-order unification, clause search, the broker's services and the pipelines themselves are Specified. 161 tests; 106 spec-guard assertions.
- **Finding F-19 resolved; image format version 4 (brief 009 executed, ADR-0017 amends ADR-0006).** The turn gate and the mailbox of axiom A3 are code in `cortex-core`: `GateState` (idle 0, scheduled 1, running 2), `try_schedule`, `begin_turn`, `end_turn`; `MailboxNode` (8 bytes, two atomics, in an arena the caller owns), `mailbox_push` (one compare-exchange), `mailbox_drain` (one swap, most recent first), `mailbox_is_empty`, `new`, `Default`. The head encodes node index + 1 so that zero is empty and an image at rest needs no fix-up; the ABA tag is gone (`mailbox_tag` → `mailbox_reserved`, MUST be zero) because a stack pushed and drained whole has no ABA hazard; `end_turn` stores idle and then re-reads the head, re-scheduling the unit itself if a message arrived during the turn, and the four operations around that window are sequentially consistent, the one place where acquire/release is not enough. Six unit tests and `crates/cortex-core/tests/mailbox.rs` (four producers, one consumer, 100 000 messages, each delivered exactly once, none left behind); two benchmarks (`mailbox/push_drain_x16`, `gate/schedule_begin_end`) close the gap in target T-3's subjects. The drain yields reverse arrival order; the executor orders a batch deterministically before integrating (Specified, M2). Milestone M1's exit test passes. Brief 009 is archived.
- **Findings F-19 to F-22 recorded; F-21 and F-22 resolved.** A pass over the whole repository after ADR-0016: `mailbox_tag` cannot be an ABA guard as a plain field (F-19, open, brief 009); `plastic_delta_head: u16` cannot index the 10⁹ deltas of Appendix A (F-20, open, milestone M4/M5); §8.3 named wheel lanes that ADR-0013 removed (F-21, fixed); eleven crates' documentation and four manifest descriptions asserted capabilities or figures the crates do not implement, one of them the traffic reduction hypothesis H-2 calls unmeasured (F-22, fixed: every crate comment now states what it holds, labels the rest and points to its whitepaper section). Whitepaper §8.4 gains the wrap rule for `u32` tick stamps (a wrapping difference read as signed, never `a < b`); Appendix C names the briefs that open M1 and M5.
- **Whitepaper 4.0.0.** The architecture document describes the thirty-two-crate workspace of ADR-0016; its structure, labels and rules are unchanged from 3.0.0, and every new claim about the tree carries an executable assertion.
- **Finding F-5 closed; ADR-0009 accepted.** The workspace is edition 2024 with `rust-version = "1.85"`, both inherited from `[workspace.package]` by all nineteen manifests; `cargo fix --edition` changed no source file and rustfmt's 2024 style edition reordered `use` lists in five files. The workspace resolver is 3 (MSRV-aware). `rust-toolchain.toml` pins the toolchain CI and contributors build with at 1.97.1; a new CI job `msrv` builds and tests on the `rust-version` read from `Cargo.toml`. The timing wheel's `#[allow(clippy::manual_is_multiple_of)]` is gone: clippy reads the MSRV. Whitepaper TC-8 is Implemented and MUST, held by three executable assertions; the README badge says Rust 1.85+.
- **Finding F-17 narrowed (brief 008 executed, ADR-0015).** `cortex-embodiment` gains the frame ABI and the ring protocol: `TorqueFrame` (epoch, twelve Q16.16 torques) and `JointStateFrame` (epoch, twelve Q16.16 positions; velocities are the consumer's finite difference), both 64 bytes; rings of 16; `EmbodimentRingBuffer` carries `abi_version` and `capacity` in its former reserved bytes and exposes `producer_claim`/`producer_publish`/`consumer_peek`/`consumer_release`, index-only with acquire/release on the cursors and no `unsafe`; `heartbeat_ms` is the producer's monotonic clock at publish. Eight tests including a two-thread exchange of 100 000 frames. The mapping, the 1 ms loop and the torque decoder remain Specified. Brief 008 is archived; no live briefs remain.
- **Finding F-14 closed (brief 007 executed).** Every state crate now carries a `#[cfg(test)]` module and every public function and associated constant has at least one test: `cortex-symbolic` (`bind` stores the pair, sets bit 0, preserves the other flag bits, is idempotent, touches nothing else; `DIMENSIONS`), `cortex-sensory` (a stub driver fills the caller's slice through `dyn SensoryPeripheral`, never exceeds it, is `Send + Sync`; `Default`), `cortex-embodiment` (`new` and `Default` all-zero; independent atomics), `cortex-fabric` (`MAGIC`), and layout tests for `cortex-neuromod`, `cortex-hippocampus` and `cortex-telemetry`. Eighteen `spec-guard` directives in whitepaper §1.6 hold one test module per crate; `CONTRIBUTING.md` states the per-item rule as a review rule. Brief 007 is archived.
- **Finding F-13 narrowed (brief 006 executed, ADR-0014).** A nineteenth workspace member, `benches/cortex-bench` (`publish = false`), benchmarks the existing components of target T-3: wheel schedule and advance on the production geometry, `synaptic_efficacy_q16`, `compute_gating`, `step_ignition`, all from a fixed-seed LCG. The harness is **criterion 0.7.0**, pinned exactly with default features off, the workspace's only third-party dependency and a dev-dependency of that crate alone; `Cargo.lock` gains 43 dev-only packages; the state crates' dependency tables stay empty. `docs/benchmarks/README.md` defines an admissible run (reference platform, isolated core, fixed frequency, `perf stat`) and the results convention with an `admissible:` line; the first recorded run, from a Windows developer machine, is marked not admissible and no figure enters §10.2. CI executes the benchmarks once per run and asserts no timing. Brief 006 is archived.
- **Finding F-11 closed (brief 005 executed, ADR-0013 amends ADR-0004).** `FlatTimingWheel<CAP>` has 256 fine slots (10 µs, 2.56 ms) and 256 coarse slots (100 µs, 25.6 ms), both powers of two; each slot is a fixed-capacity list of opaque 28-bit tokens, with the fine residual packed into coarse entries. `schedule(delay, token)` rejects zero delay, delays at or beyond 2 560 ticks, oversized tokens and full slots without mutating the wheel; `advance()` clears the consumed slot, cascades the coarse window that begins at the new tick, and returns the due slot in a deterministic order (fine-scheduled tokens, then cascaded tokens, each group in scheduling order). `WorkerWheel = FlatTimingWheel<2048>` is 4 195 336 bytes (asserted); the capacity model's row 19 falls from 512 MB to 268.5 MB. `schedule_fine` and the lane masks are gone. Eight tests. Brief 005 is archived.
- **Finding F-8 closed; image format version 3 (brief 004 executed).** `CerebellarMicrozone`'s 32 reserved bytes became a seven-slot delay line (`pred_ring`) and a packed control word (`delay_ctl`: head, plant delay, filled count, command-sign bitmap). `step_forward_model` now predicts the observation `d` steps ahead through a learned gain, compares the observation arriving now with the prediction made `d` steps ago, adapts the gain by `sign(u) · round(error / 16)` (rounding, not flooring, so the residual is bounded by half a learning step), and returns the predicted change as the Purkinje output. `set_plant_delay` (clamped to 7; 0 disables learning), `plant_delay`, `filled` and `MAX_PLANT_DELAY` added. Seven tests, including convergence on the plant `y(t+d) = y(t) + k·u(t)` to within 8 LSB after 400 steps for three `(k, d)` pairs and for an alternating-sign command. Brief 004 is archived.
- **Findings F-3 and F-12 closed; image format version 2 (brief 003 executed, ADR-0012).** Sixteen-bit synaptic base weights are Q1.15: `SynapseBlock::weights_q16` is renamed `weights_q1_15`, and `cortex_core::synaptic_efficacy_q16` implements the widening product with the Q0.8 short-term-plasticity factors (exact `i64` product, one shift by 15, floor toward negative infinity) with seven unit tests. `AgentPerspectiveState::intention_vector_ptr` is renamed `intention_vector_idx`. `CortexFileHeader` gains `MAGIC` and `FORMAT_VERSION = 2`; the byte layout is unchanged, the meaning of the weight bytes is not, so version-1 images are refused. Brief 003 is archived.
- **Findings F-6 and F-7 closed; TC-6 Implemented (brief 002 executed).** All eighteen crates are `#![no_std]` (fourteen gained the attribute; none used a `std` item). Every record without atomics derives `Clone, Copy, Debug, PartialEq, Eq`; `DendriticSuperNeuron` and `EmbodimentRingBuffer` (control records) and `FlatTimingWheel` (a 2 248-byte per-worker value) derive `Debug` only, each with a comment citing whitepaper rule L-5. TC-6 and L-5 are now MUST, and an executable assertion holds the `no_std` count at 18. No `Pod` dependency was added: the choice between `bytemuck` and `zerocopy` is deferred until the image loader needs it. Brief 002 is archived.
- **Finding F-4 closed; F-14 narrowed (brief 001 executed).** `compute_gating`, `step_forward_model` and `step_ignition` use saturating arithmetic; `update_circadian_tick` uses `wrapping_add` before its 16-bit mask, wrap being the intended semantics of a phase counter; `evaluate_threat` performs no arithmetic and was left as is. Each of the five functions gained boundary tests (twenty-two in total) that fail under plain arithmetic in a debug build. `FlatTimingWheel::schedule_fine` and `SymbolicHypervectorHeader::bind` remain untested. No Q16.16 newtype was proposed: five call sites do not justify one yet. Brief 001 is archived in `briefs/archive/`.
- **Findings F-9 and F-18 closed.** Crate metadata (`version`, `edition`, `authors`, `license`, `repository`) is inherited from `[workspace.package]`; each manifest keeps only `name` and `description`, and all eighteen now carry one. `cortex-sensory` gained the compile-time assertion that `SensoryEvent` is 8 bytes, 8-aligned; the whitepaper's assertion-block directive now requires 18.

- **Finding F-10 closed.** All crates formatted with `rustfmt` (whitespace and comment alignment only). `Default` implemented for `FlatTimingWheel` and `EmbodimentRingBuffer`, delegating to their `const fn new()`; `FabricPacketHeader::MAGIC` written as a byte-string literal. `cargo fmt --check` and `cargo clippy -D warnings` are now blocking in CI; the two advisory steps and their `continue-on-error` are gone.

- **Whitepaper 3.0.0.** Rewritten on the arc42 template with C4 views. Every claim now carries a status label (Implemented, Specified, Target, Hypothesis). Record layouts are transcribed from source with byte offsets. All performance figures are Targets with measurement protocols; the figures previously stated as results ("P99.99 < 35 ns", "> 120 M spikes/s", "< 100 ms cold boot") are withdrawn. The founding five axioms (virtual existence, state/compute decoupling, turn invariant, discrete delay, metabolic tiering) are restored as the solution strategy. Findings F-1 to F-17 record every disagreement between the previous specification and the tree.
- **README** rewritten to describe the repository as it is: a state-model-stage workspace, with a status table and a crate table.
- **Traditional Chinese document** replaced by a reader's guide (`docs/zh-TW/README.md`) that carries no layouts or figures, per ADR-0008. The former full translation (`docs/zh-TW/architecture-report.md`) is removed.
- **Documentation tools bumped**: `@descent-vtt/spec-guard` 0.4.0 → 0.5.0 (an assertion whose scope holds no files now fails instead of passing vacuously; import assertions read Rust; SARIF output) and `@descent-vtt/spec-graph` 0.2.1 → 0.3.0 (0.2.2 fixed the defect found in this repository where a link to a file holding a register resolved to its last row, so anchored links into the whitepaper were reported broken; 0.2.3 reports a path that names two files as ambiguous instead of binding it to whichever was indexed last; 0.3.0 reads reference-style links and both spellings of every front-matter relation). No directive or configuration changed; 66 assertions and the graph pass on both versions.

### Added

- Architecture decision records ADR-0001 to ADR-0011 in `docs/adr/` (MADR format).
- `CONTRIBUTING.md`, `SECURITY.md`, this changelog.
- Documentation verification tooling: `@descent-vtt/spec-guard` 0.4.0 and `@descent-vtt/spec-graph` 0.2.1 pinned in `package.json`; `.spec-graph.json` configuration; `npm run spec`.
- GitHub Actions workflow `ci.yml`: `cargo check`, `cargo test`, `spec-guard` and `spec-graph` as blocking checks; `cargo fmt` and `cargo clippy` as advisory checks until finding F-10 is closed.

### Fixed

- **Documents reconciled with the tree again (whitepaper 4.3.1; finding F-27).** After ADR-0028 to ADR-0030 a read of every document found twenty stale statements: the format version still 8 in two sentences, the §9 index ending at ADR-0027, fifteen per-crate test counts, the property-kit inventory, the CI header, two "cannot merge" sentences (the gate covers the changed lines; branch protection is not set), "proof" in the ADR index, the README's cross-architecture claim (now: checked at 20 000 ticks in CI, Target T-1 in full), the reader's guide's definition of Implemented and its three levels (seven), the three command sets (one block, in `CLAUDE.md`), `SECURITY.md`'s scope, a directive's reason, section kind 40, "nineteen manifests". The weekly job gains `workflow_dispatch`, so it can be run by hand; the header names all six jobs. The repository's ruleset now requires the `rust`, `msrv` and `specs` checks by name before a merge (set by the owner on 2026-09-10), so those three job names are the ruleset's and a rename is a change to the ruleset first.
- **The verification machinery under its own audit (ADR-0030 follow-up).** Six exclusion patterns in `.cargo/mutants.toml` were wider than their reasons and hid thirteen mutants the existing tests kill (every `+` in `Image::decode`, both `-` of the message's lower clamp, the first head check of `mailbox_push`, the role test of the realisation order, and a sincerity-gap comparison whose reason was stale). The bounds became named literals with compile-time assertions (`MAX_BLOCKS`, `MESSAGE_EFFICACY_MIN`) checked by named functions, the head check is one check per attempt, a complete frame's child position needs no bit test, a fixed-point test holds the sincerity average, and the four patterns are gone or anchored to a function that holds nothing else. The determinism pin names every field of its configuration, so a change to `Config::default()` cannot move it. Three property assertions were weaker than their rules' contracts (the depletion is now exact; a spike's plateau and refractory window follow the apical potential; a walk is called a walk). CI: the concurrency group includes the event, so a push to `main` no longer cancels a running weekly job; the weekly and mutation jobs share one cache that is saved on failure too, so a new pull request restores the built tool; the weekly run's outcome is kept as an artifact; the four ordinary jobs skip on a schedule. `cortex-agency`, `cortex-autonomic` and `cortex-fabric` deny `clippy::arithmetic_side_effects` (fifteen crates now); brief 016 carries the per-crate site counts for the seventeen that remain and the runtime. Six tests whose names outran their bodies now assert what they name (a silent frame renders silence; the attention schema folds each of its five words and not the potential; an activated unit above threshold fires in its turn; the loader's unknown kind and missing section; every fault at once in the veto gate; efficacy monotone in the direction of the weight's sign).
- **Documents reconciled with the tree (whitepaper 4.2.2; finding F-25).** A read of every document against the tree found twenty-five statements the tree contradicted: labels left at Specified after the work that implemented them (the loader and writer in §3.2 and A1/A5, the deque in §5.2.1, the delivery phase in FR-3, unification in §8.8 and M8, over-horizon rejection at load in §6.2, STDP in M5), counts left behind by later rounds (thirty-six records, twenty-four crates with rules, eight crates for ADR-0026/0027, the test counts of `cortex-tools`, `cortex-attention` and `cortex-knowledge`, F-25 and M8 in the reader's guide, the runtime's second dependency, the manifest's two non-state crates), a duplicate `FRAME_ABI_VERSION` (1) beside (2), three sentences in the whitepaper, `SECURITY.md` and §8.10 that said no `unsafe` exists in the workspace, the Target T-7 that carried the withdrawn 100 ms figure, the tick-duration question marked narrowed to a milestone that closed without deciding it, the label definitions that named `crates/` alone, the verification scopes of V-2 and V-3, the benchmark list, the Node version, and `CLAUDE.md`'s and `CONTRIBUTING.md`'s "every crate is `no_std`" (every state crate). The documented commands carry `--locked`, as CI always has. Four records join the README's table.
- **Edge behaviour under audit (ADR-0028; whitepaper 4.2.1; image format version 9).** Every record method was audited for six defect classes (a shift that can reach the width, a negation of the most negative value, an off-by-one bound, a mutator that reports success on a no-op, a state nothing clears, an undocumented no-op) and twelve reproductions were fixed, each with the test that reproduced it: `cortex-core` `stp_decay_factor_q16` panicked at a shift of 64 and never decayed above 16 (a time constant above $2^{16}$ ticks is now read as $2^{16}$), `mailbox_push` stored its payload before it read a corrupt head, `is_at_rest_image` ignored the reserved bytes; `cortex-ethics` a default gate read as permitted (`veto_decision_flag` is now `DECISION_UNEVALUATED` / `DECISION_VETOED` / `DECISION_PERMITTED`, zero closed); `cortex-salience` the freeze reflex, the valence and the replay tag were never released (now released with the override; the conditioned weight persists); `cortex-arithmetic` `OP_REM` of `i128::MIN` by −1 was flagged as overflow (the remainder is 0 and fits); `cortex-cerebellum` the packed control word indexed outside its ring when set through the field or an image (each field is read within its bound); `cortex-reasoning` `literal_of_term` reached the sign bit at $2^{31} - 1$ and overflowed at `u32::MAX` (now `Option<u32>`); `cortex-thalamus` an unknown gating mode relayed nothing, undocumented (documented, like a closed gate); `cortex-runtime` the loader accepted non-zero reserved bytes and a delta slot its block does not have (`ImageError::ReservedNotZero`, `DanglingIndex` slot 5), a sealed header's directory count sized an allocation before the file bounded it (`Truncated`), `WriteAheadLog::append` wrote before it panicked on a unit outside the log (`InvalidInput` before writing), and `is_quiescent` ignored the injector ring (a pair not yet drained is not a quiescent point). Whitepaper §5.2.1, §5.2.2, §5.2.6, §5.2.7, §5.2.19, §5.2.28, §5.2.30, §5.2.31, §6.7, §8.6, §8.7; finding F-24. 11 new tests; 285 in all.
- Every LaTeX expression in the previous whitepaper and translation contained control characters (interpreted `\t`, `\f`, `\a`, `\r`, `\b`, `\v`, `\n` escapes), so no equation rendered. Rewritten.
- Struct definitions reproduced in the previous whitepaper did not match the source for 15 of 19 types, including one 72-byte record described as 64 bytes. Replaced with tables transcribed from `crates/`.
- The README referred to a documentation checker by an absolute path on one developer's machine.

### Removed

- The claim of a "Rust 2026 edition"; no such edition exists. Edition policy is now ADR-0009 (proposed).
- The "Sovereign", "Grand", "Monumental" and "Fidelity 5.0/5.0" vocabulary and the "conscious" description of the workspace subsystem.

## Earlier history

Before this changelog existed, the repository's history is the git log: specification revisions 1.x to 2.8.0 between 2026-09-06 and 2026-09-10, culminating in the eighteen-crate workspace whose layouts this changelog's first entry documents.
