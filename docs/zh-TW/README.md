# VirtualCortex 架構白皮書導讀（繁體中文）

> **本文件是導讀，不是規格。** 唯一具規範效力的文件是英文原版 [`docs/WHITEPAPER.md`](../WHITEPAPER.md)。本導讀不重複任何結構體佈局、欄位偏移或效能數字；凡涉及這些內容，一律以英文原版為準。這樣做的原因記錄在 [ADR-0008](../adr/0008-documentation-governance.md)：舊版曾維護一份完整的平行譯本，結果兩份文件同步漂移、同步損毀，翻譯反而成了第二份互相矛盾的「合約」。

## 這個專案是什麼

VirtualCortex 是一個以 Rust 撰寫的**單機神經擬態虛擬 Actor 引擎**：把脈衝神經網路（SNN）建立在虛擬 Actor 模型之上，並嚴格限制在一台實體伺服器內執行。設計前提是：生物神經組織極度稀疏、事件驅動、非同步，而通用 CPU 只有在每一個資料結構都尊重硬體物理現實（64 位元組快取行、記憶體牆、分支預測器、堆配置與系統呼叫的代價）時，才能有效率地執行這樣的系統。

專案的工程信條是 **「Latest ≠ Newest」**：最新的技術很少是最被理解的技術。任何進入熱路徑的技術都必須具備穩定的規格、至少兩年的第三方生產使用紀錄、已知而非待發現的失效模式，以及能對應到 CPU 與核心實際行為的機械同理心論證（英文原版 §2.1）。

## 讀白皮書之前必須知道的四個狀態標籤

白皮書中每一個關於系統的敘述都帶有下列四種標籤之一，**沒有標籤的敘述視為缺陷**：

| 標籤 | 意義 |
| :--- | :--- |
| **Implemented（已實作）** | 今天就存在於 `crates/` 或 `runtime/`，且由編譯器、單元測試或文件內的可執行斷言檢查。 |
| **Specified（已規範）** | 設計已在白皮書或 ADR 中定案（介面、格式、不變量），但尚無程式碼。 |
| **Target（目標）** | 可量測的品質目標，附有量測方法；**尚未量測**。 |
| **Hypothesis（假設）** | 架構所依賴的研究假設；驗證之前，依賴它的任何敘述都不能升級為 Target。 |

兩條配套原則：

1. **文件與程式碼不一致時，以程式碼為準**，並把不一致記錄為 §11 的編號 finding，不得默默修掉。
2. **白皮書中沒有任何效能數字是量測過的。** §10 的每個數字都是附帶量測協定的 Target；舊版宣稱的「P99.99 < 35 ns」、「86 億節點 100 ms 冷啟動」等已明確撤回（見 [ADR-0010](../adr/0010-measured-or-target.md)）。

## 五大公理（設計骨幹）

白皮書 §4 重新以專案最初設計稿的五大公理為核心，32 個 crate 都建立在它們之上（其中 14 個由 [ADR-0016](../adr/0016-thirty-two-crate-architecture.md) 於 2026-09-10 納入）：

| 公理 | 內容 |
| :--- | :--- |
| A1 虛擬存在 | 神經單元在邏輯上永遠存在，只有脈衝命中時才佔用記憶體；冷單元被淘汰，需要時再惰性載入。 |
| A2 狀態與算力解耦 | 記錄是被動資料，Worker 是無狀態、綁核的執行緒；資源消耗與瞬時活躍度成正比，與總容量無關。 |
| A3 回合制單寫者 | 任一 tick 內，一個記錄的一般欄位最多只有一個 Worker 寫入，由所有權保證：每個 Worker 固定負責一段單元，只有它會跑這些單元的回合；其他 Worker 對同一筆記錄的存取（把訊息推進信箱、標記閘位元組、讀脈衝時戳）都被屏障隔在擁有者寫入的那個階段之外（ADR-0102，原本由 CAS 閘控保證）；無鎖、無死鎖、無資料競爭。 |
| A4 離散軸突延遲 | 時間是 tick，延遲是時間輪的索引，永不使用作業系統計時器；排程為 O(1)。 |
| A5 代謝分層 | 背景掃描把長期沉寂的組織淘汰到本地儲存，熱迴路留在快取友善的陣列中。 |

## 白皮書結構對照（arc42 範本）

| 英文原版章節 | 內容 |
| :--- | :--- |
| [Executive summary](../WHITEPAPER.md#executive-summary) | 今天有什麼、設計了什麼、必須證明什麼。 |
| [§1 Introduction and goals](../WHITEPAPER.md#1-introduction-and-goals) | 問題陳述、功能需求、品質目標、非目標、**32 個 crate 的實作現況表**。 |
| [§2 Constraints](../WHITEPAPER.md#2-constraints) | 「Latest ≠ Newest」信條的可操作定義、技術限制（TC-1 至 TC-9）、書寫慣例。 |
| [§3 Context and scope](../WHITEPAPER.md#3-context-and-scope) | C4 第一層系統情境圖與對外介面。 |
| [§4 Solution strategy](../WHITEPAPER.md#4-solution-strategy) | 五大公理、品質目標的達成策略、分解原則。 |
| [§5 Building block view](../WHITEPAPER.md#5-building-block-view) | 分層圖，以及**每個 crate 的責任、公開 API、逐位元組的記錄佈局與狀態**——這是 ABI 合約所在。 |
| [§6 Runtime view](../WHITEPAPER.md#6-runtime-view) | 單一脈衝的生命週期、時間輪 tick、感官熱插拔、1 ms 具身迴圈、動作選擇（R-5：線性閘自 ADR-0059 起由執行期組合成一個作業的讀出——刺激依試次索引與種子抽出、注入一組單元，讀出從執行器自己的脈衝序列數出該試次落在兩組讀出單元的脈衝數、寫進兩個通道的直接與間接驅動，交給 `cortex-basal-ganglia` 的 `compute_gating` 決定，平手不選，獎賞的正負由結果決定；在 256 與 1 024 單元的參考網路上依事先寫好的判準、對著四組對照量測，正確率在每一個區塊都停在機率水準，所以就這個組合而言獎賞路徑沒有改變這個行為，ADR-0060；ADR-0065 把儀器重新校準：刺激改成間隔超過先驗局部窗口的單元、每個只放電一次，讀出只數先驗延遲帶推得的那個窗口，試次拉長到多巴胺訊號的一個時間常數，增益在給任何獎賞之前、權重凍結時由校準從兩個候選裡挑出，兩個尺寸都通過校準，每個常數都在第一次有獎賞的跑之前先提交；ADR-0066 透過這個儀器再問一次同一個問題：讀出分得出刺激與背景，卻分不出兩個刺激，最後 128 個試次的正確數是 53 與 49、判準要 80，每條耦合在每種回饋下同樣下降，256 單元上權重一動、儀器在一個區塊內就看不到刺激了，所以這個「否」現在說的是規則在這個作業、這兩個尺寸上的表現，而不是讀出；下一輪會改的東西——每單元的學習訊號、結構規則、另一個作業——只點名不建；ADR-0068 把獎賞定址：突觸鞏固時用的調變改由它的兩端決定，多巴胺項只到「這次呈現的刺激」的單元打到「引擎選中的讀出」的單元那些突觸，其餘突觸都在基線下鞏固，執行器每個單元兩個旗標、扇出時逐槽挑調變，`cortex-core` 的規則、記錄與映像都不動，全部定址時與 ADR-0066 逐位元相同；獎賞對稱時正負號和選擇合起來就是該刺激的正解，所以這個傳遞是延後一個試次、指向正解的教學訊號，方向由痕跡的正負決定，跑之前先寫下；ADR-0069 在 1 024 單元上量：最後 128 個試次正確數 56 與 65、判準要 80，全域形式重跑得到 49，對照都守住，一個與四個工作者同一序列，獎賞子句仍不成立；刺激打到它正解讀出的耦合比另外兩條多掉三到五個百分點、方向與痕跡的正負一致，行為讀不到；256 單元從第一個區塊就低於門檻，記為未量測；下一輪要改的只點名：讓配對能上升的背景、單一正負號的獎賞、試次結束時鞏固該試次的刺激、結構規則、另一個作業；ADR-0072 在校準跑上讀出痕跡的組成，並把增益梯子往下走到沒有一階通過，增益留在 1.75、沒有給獎賞的跑；ADR-0074 試了 F-46 點名的那一個改動——刺激改成一則訊息，先取閾值 1.0、再取 1.25、再取兩者中點 1.125，各在校準的凍結跑上用事先寫好的量度讀：執行器把注入的訊息也乘上突觸增益（F-47），一則訊息在靜止的單元上放電一次或不放電，在驅動留下的背景電位下卻讓兩成八到七成的單元在不應期結束時再放一次，沒有一個候選能讓每個單元恰好放電一次；正負號在每個候選下都不成立，所以仍沒有給獎賞的跑；判準需要多少也第一次讀了出來：正解讀出每個試次要多一到三個脈衝；ADR-0076 建了兩次注入：F-46 的驅動不動，再對同一組單元注入負的基底訊息把殘餘取消——brief 寫的 offset（不應期長度 200）會落在不應期裡，膜規則直接把它丟掉，而且讓單元再放電的不是基底殘量而是被耦合拉高的胞體，所以 cancel 改由規則推導：不應期結束後那一個 tick 起、涵蓋齊射分布的九個 tick、每 tick 六則上限訊息，每一步都用整數 oracle 算過再對引擎的探針驗證；在它之下每個單元恰好放電一次、之後不再放，齊射的抑制歸零、背景的抑制減半，但齊射的增強也減半，因為 ADR-0072 量到的反應其實是三次齊射的反應；各項加總是每突觸每試次 −2.0，看得到 62/64、正負號 13/64，所以仍沒有給獎賞的跑，依 H-12 的停損規則刺激端記為已用盡；ADR-0077 走了背景端、兩個候選都不過，H-12 依第三步結案為否；ADR-0078 把「要正負號的作業」寫成 H-13，ADR-0079 跑了一次、讀到是：獎賞送到呈現刺激連往指派讀出的突觸後，那些耦合升了 12 到 15%，指派讀出的反應在最後 128 個試次裡有 128 與 127 次高於不給獎賞的同一試次）、睡眠整理（R-6：兩程序睡眠壓力、清醒／慢波／REM 三階段、慢波期重播情節帳本、REM 期降低其標記，已實作；自 ADR-0056 起每次進入慢波睡眠時執行器都把詞項競技場壓縮到子句庫抵達得到的節點上，一趟由上而下標記、一趟由下而上搬移，庫的讀取逐節點不變；在 256 與 1 024 單元的參考網路上已量測：一夜之後模式內的每條突觸都到上限，提示六個單元能點燃局部叢集其餘六個，隨機模式則否，ADR-0044；情節也能由脈衝序列自行標記：最密的一個基底時間常數窗口、其中放電最多的十二個單元，有獎賞的發明之前那個漣漪裡活躍的模式成為它的情節，一夜之後提示六個能點燃其餘六個中的五個，網路自身在穩態驅動下最密的巧合則不能補全，ADR-0048；自 ADR-0050 起脈衝序列由執行器自己持有：每個 tick 之後把各工作者放電的單元依單元序合併進有界環，任何工作者數都得到同一序列，分叉回傳它，搜尋、獎賞與標記可一次呼叫完成；自 ADR-0052 起項目場與子句庫進了映像：子句庫、項目節點、歸納記錄、情感狀態都是映像的區段，情節記錄自己帶著它所代表的符號，提交的發明先經綁定表實例化再解綁，搜尋在清醒時依 tick 的節律從游標帶著預算自行執行；清掃器仍為 Specified）、冷啟動、政策修正的試驗（R-16：把映像分叉成兩份跑同樣的 tick，行為雜湊相等且成本下降才准提交）、語言（R-9：範疇序列在項目場上歸約成構式框架、框架密封成超向量、經碼本逐角色讀回，全程只有 id，已實作；詞典也已在 id 上實作：主機的 token id 對應概念 id 與詞形，token 序列讀成框架、框架依模板順序連同言語行為標記、語尾助詞、敬語變體與子句實現回 token id，字詞本身留在引擎外，ADR-0046）、數學的兩條軌道（R-10：項目場上的歸納——吸收、辨識、內構造發明新謂詞，每個輸出都能由一步歸結還原輸入——與多項式連分數的收斂子搜尋是原生軌道的猜想來源；子句庫的描述長度是效價規則讀的自由能，一次發明省下的節點數的四分之一是調節器的獎賞，出口測試裡它在下一個突觸前脈衝把待決的資格痕跡固化進權重；證明器回傳的框架只有在狀態完成、類別與動作正確、載荷帶著敘述雜湊與非零證書雜湊時才把節點標為已認證的定理，其餘一律不動，已實作；子句庫上的執行搜尋只提交獎賞為正的發明、撤銷其餘，配對走訪與有界證明搜尋確認每個原本可證的目標仍可證、發明出的謂詞在證明路徑上被讀回，ADR-0045；仲介程序本身仍為 Specified）。 |
| [§7 Deployment view](../WHITEPAPER.md#7-deployment-view) | 參考平台（Target）、記憶體階層、行程與執行緒模型。 |
| [§8 Cross-cutting concepts](../WHITEPAPER.md#8-cross-cutting-concepts) | Q16.16 數值模型、記錄佈局規則 L-1 至 L-6、確定性、時間、並行、記憶體、序列化、**生物機制對照表與參考方程式**、安全、可觀測性、計算現象學（§8.12：把理論命名的狀態變數做成整數規則，並明說不宣稱「經驗」）、原生語言（§8.13：巢狀構式、概念混成、漫遊、對話立地）、社交分寸（§8.14：言行落差、二階期望、分寸、間接言語行為）、再表徵（§8.15：異常、框架失效、換類別、基底旋轉）、聲學合成（§8.16：整數源-濾波嗓音）、計算幽默（§8.17：良性違反、獎賞、玩笑標記，並明說不宣稱「好笑」）、自我修正（§8.18：引擎只能改自己政策裡登記過的參數，改法是四道閘門加一場分叉試驗，行為必須一模一樣；引擎不能改自己的程式，規則的提案交給倉庫的閘門與維護者）。 |
| [§9 Architecture decisions](../WHITEPAPER.md#9-architecture-decisions) | ADR 索引；決策本體在 [`docs/adr/`](../adr/README.md)。 |
| [§10 Quality requirements](../WHITEPAPER.md#10-quality-requirements) | 品質樹與 T-1 至 T-8 目標情境，每列都有量測協定與空白的「Measured」欄。 |
| [§11 Risks and technical debt](../WHITEPAPER.md#11-risks-and-technical-debt) | 編號 finding F-1 至 F-50（F-50 已解決：ADR-0099 量測速度用的工作負載，在每個 tick 之間把所有單元讀一遍，計時量到的不只是引擎，還有量測工具本身，ADR-0100 讀出來的；ADR-0101 規定之後量加速時，計時的 tick 之間不能有引擎自己不會做的讀取，並坦白寫明這條規則是在第一次量測失敗之後才定的；F-38 已解決：週掃描第一次跑完整棵樹，每個區域各自的逾時上限在 runner 上量出來，ADR-0058；F-39 已解決：每份 brief 的常設指令都要重述「Latest ≠ Newest」，自 028 起由腳本檢查；F-40 已解決：§9 的決策表漏了 ADR-0058 那一列，本輪補上；F-36 已解決：突觸的極性由其突觸前單元的旗標決定並作為規則的參數，權重只在該極性的半區間內移動，抑制性突觸採 Vogels 2011 的對稱規則，ADR-0049；F-37 已解決：抑制規則的目標週期不能進登錄表，因為 ADR-0031 的行為閘拒絕任何會改變引擎行為的參數，它改為映像的參數，ADR-0053；同一輪在 256 與 1 024 單元上量測了一天：參考先驗的抑制權重從上限出發只會減弱，興奮權重在穩態驅動下十五個視窗內耗盡，睡眠的佔位常數依事先寫下的規則保留；迴圈內的因果計數也已量測：在突觸訊息的延遲內放電的脈衝計為後代，讀到的是輸入密度而非分支比，控制器不動，ADR-0054；ADR-0055：興奮性配對的抑制量隨權重大小縮放，穩態配對下權重收斂到抑制量等於增強量的固定點而不是耗盡——1 024 單元一天之後興奮權重總和穩在先驗的 0.45、最後十六個視窗內變動不到 1 %，舊的加法規則十五個視窗內就耗盡；256 單元且增益固定時十六個視窗後仍在下降但已減速，事先寫下的判準在 20 Hz 週期差 0.27 個百分點未達，規則保留、常數不調、項目保持開放，ADR-0070 在儀器自己的執行器上、只有背景驅動、跑了八十個視窗把它結掉：brief 026 的判準第九個視窗就先成立（興奮權重總和是先驗的 0.818），但第八十個視窗總和仍在下降、只剩先驗的 0.551、每視窗掉 0.24 %，所以那條判準讀的是速率不是水準，這一項以兩個數字結案；§8.8 的每單元增益維持 Specified，因為增益乘的是輸入，抬不起一個為零的權重；ADR-0056：每次進入慢波睡眠時壓縮詞項競技場，子句庫抵達不了的節點被回收，一趟由上而下標記、一趟由下而上搬移，庫的讀取逐節點不變，映像格式仍是 14；ADR-0057：抑制規則從上限以下讀取（同一產生器、抑制增益 2.0），每個視窗「在目標上的單元比例」作為選目標所需的讀數，不選目標，也不為會改變行為的參數開修正通道——那是 CLAUDE.md 的不變式與 ADR-0031 留給維護者的）、假設 H-1 至 H-12（H-3 是「這些規則是否構成經驗」、H-5 是「再表徵是否算原創綜合」、H-6 是「標了玩笑的話是否真的好笑」，倉庫裡沒有任何測試能判定，文件因此不主張；H-8 是分支比估計器在參考規模是否讀到分支比（在 256 與 1 024 單元上已量測：單一視窗的斜率不是 σ 的讀數，控制器在飽和上限處轉向；在第二個先驗——稀疏隨機網路——上再量測後，記錄不加第二行：粗 bin 的雜訊來自 bin 的長度，細 bin 的斜率讀的是被驅動族群的自相關而非分支比，ADR-0047；在 4 096 單元上依事先寫下的規則再量測，六列中五列的細 bin 斜率與因果比相差在 0.15 內、格子網路在 2.25 增益處高出 0.28，仍不加記錄行，ADR-0051；附錄 A 的規模仍為 Target）、H-9 是睡眠中的重播在參考規模是否鞏固任何東西（同樣已量測：局部模式在一夜後可由半數提示補全，隨機模式不能；由脈衝序列自行標記的模式也已量測：經驗的模式能補全六個中的五個，網路自身最密的巧合不能，ADR-0048）、H-10 是貪婪的移位歸約器是否涵蓋模板所需的構式、H-11 是壓縮進展當作獎賞所固化的痕跡是否被之後的行為讀到（符號的一半已裁定：發明出的謂詞在之後的證明中被讀回；突觸的一半也已在此規模裁定：有獎賞的發明有了一個由獎賞前的漣漪標記出來的單元模式，一夜鞏固後提示一半能點燃其餘，關聯表由呼叫者持有，ADR-0048；獎賞路徑也已在一個行為上閉合量測過：就目前的組合而言，這條路徑無法檢驗突觸的一半，ADR-0060），以及先到先配的文字配對是否找得到子句庫需要的配法，各附量測協定；H-12 是獎賞能否改變引擎自己讀回來的行為——在 256 與 1 024 單元上已量測：正確率停在機率水準，刺激對兩組讀出的影響一樣、相差不到每試次一個脈衝，刺激到讀出的耦合在每一種回饋下都同樣下降，所以就這個組合而言獎賞沒有可著力之處；下一輪該改的是讀出的時間窗、刺激的回響、試次的間距與增益，本輪的常數一個都不動，ADR-0060；這四項改完、儀器在給獎賞之前先校準過之後再量一次：讀出分得出刺激與背景（權重凍結時 64 個試次裡有 58 與 62 個看得到），卻分不出兩個刺激，最後 128 個試次的正確數 53 與 49、判準要 80，鏡像分派 53 與 65，四組對照都守住，一個與四個工作者跑出同一序列，所以這個「否」現在說的是規則在這個作業、這兩個尺寸上的表現，不是讀出；下一輪要改的只點名：每單元的學習訊號、結構規則、另一個作業，ADR-0065、ADR-0066；獎賞改為定址之後再量一次：多巴胺項只送到「這次呈現的刺激」的單元打到「引擎選中的讀出」的單元那些突觸，其餘突觸都只在基線下鞏固，儀器、對照、種子與判準一個都不動；獎賞對稱時正負號和選擇合起來就等於該刺激的正解，所以這個傳遞其實是延後一個試次、指向正解的教學訊號，它對行為的方向由被鞏固的痕跡的正負決定，這些都在跑之前先寫下；1 024 單元上最後 128 個試次的正確數 56 與 65、判準要 80，定址的洗牌對照 53、固定調變 60、全域形式重跑得到 ADR-0066 的 49，一個與四個工作者同一序列，所以獎賞子句在全域形式重現的情況下仍不成立；讀數是：刺激打到它正解讀出的耦合比另外兩條多掉了三到五個百分點、方向與痕跡的正負一致、兩種分派都如此，而行為完全讀不到；256 單元上儀器從第一個區塊就低於門檻，該尺寸記為未量測；下一輪要改的只點名：讓配對能上升的背景、單一正負號的獎賞、試次結束時就鞏固該試次的刺激、結構規則、另一個作業，ADR-0068、ADR-0069；ADR-0070 再問 256 單元能不能靠先跑一段來用：先在背景驅動下跑規則推得的九個視窗再開始試次，校準的量度第一個區塊就只有 64 裡的 50、之後 34 到 44，沒有先跑則是 57 再掉到 31——量度跟著興奮權重總和的水準走，而背景驅動本身就把總和帶到儀器看不到的水準以下，所以 256 單元在這個作業上不管先跑多久都不能用，學習輪的判準留在 1 024 單元；要改的只點名不建：在驅動帶到的網路上重新校準、256 單元用控制器、讓總和穩在更高水準的背景；ADR-0072 量了那條痕跡到底由什麼組成：在 1 024 單元、權重凍結的校準跑上，把刺激打到讀出的突觸上的資格痕跡逐試次從記錄讀出來，再用一個在執行器自己的脈衝序列上重放配對規則的預言機拆開，每一次讀取都與記錄逐位相等——齊射的配對是淨增強，背景的配對是淨抑制而且大它四到五倍，所以 ADR-0066 那句話成了量測；原因在普查裡：每個刺激單元在齊射後一個配對窗口內還會再放電約兩次，而規則每個突觸前脈衝都記一次抑制、每個突觸後脈衝最多記一次增強，F-46 記下儀器的刺激其實在配對窗口內讓每個單元放電三次而不是一次；往下走的增益梯子 1.5、1.25、1.0 在「看得到」與「正負號」兩個事先寫好的量度下沒有一階通過，1.5 時痕跡轉為淨增強、讀出卻看不到刺激了，所以沒有給獎賞的跑、增益不動；下一輪要改的只點名不建：一個在配對窗口內只讓每個單元放電一次的刺激；F-46 由 ADR-0074 收窄：一則訊息做不到讓每個單元恰好放電一次，痕跡仍是淨抑制、正負號 0 到 2/64，沒有給獎賞的跑；F-47 記下執行器把注入的訊息也乘上突觸增益，而文件寫的是未乘增益的值，ADR-0038 的重播驅動在參考網路的增益 1.75 下讓靜止的單元放電兩次；F-46 由 ADR-0076 解決：儀器上有了一個讓每個單元恰好放電一次、之後不再放的刺激——F-46 的驅動加上不應期之後九個 tick、每 tick 六則上限訊息的取消——齊射的抑制歸零、背景的抑制減半、齊射的增強也減半（之前量到的是三次齊射的反應），各項加總每突觸每試次 −2.0、正負號 13/64，沒有給獎賞的跑；H-12 的停損規則第一步到此，刺激端已用盡，下一步在背景端；ADR-0077 走了第二步、背景端，規則點名的兩條路各自單獨試、順序固定：先讓網路在作業前穩定下來（增益固定在 1.75，ADR-0055 的判準寫成整數規則、第九十六個視窗成立，興奮權重總和是先驗的 0.925，讀出單元的放電率仍是先驗的每試次 0.28——權重穩了、背景沒有變安靜），再開臨界控制器（ADR-0055 的步長八分之一，第三十五個視窗就穩在 0.453、正是 ADR-0055 那天讀到的固定點，增益在 2.2 到 2.8 之間逐視窗擺盪，讀出響七到二十四倍，和事先寫下的預測一致）；各自跑靜、存成基線歸零的映像，再以事先寫好的三個量度讀一次凍結的跑：穩定後的網路每單元恰好放電一次、看得到（62/64）、痕跡平均而言首次成為淨增強（每突觸每試次 +0.4，四對的總和在一個區塊後全為正），但正負號只有 53/64、差三個；控制器齊射不全、三個量度全不過；沒有候選通過、沒有給獎賞的跑，依停損規則第三步，H-12 就這個作業、這個工作狀態、這條規則結案為「否」：三因子規則加上 ADR-0068 的定址傳遞學不會參考網路 1 024 單元上的雙選項差值作業，因為它的配對統計是背景的；下一個決策由 ADR 在「要正負號而非差值的作業」、「會新增突觸的結構規則」、「對照 ADR-0036 的臨界性重新檢視運作狀態」三條路中選；ADR-0078 選了第一條，寫成 H-13：在穩定後的網路上、基線歸零，每個試次結束後不管引擎選了哪邊，都把獎賞送到呈現刺激連往指派讀出的突觸，看這些耦合、以及引擎從那個讀出讀到的反應，有沒有比同一批試次不給獎賞時高；判準、預測和它自己的停損規則都在任何一次跑之前寫好，結構規則延後，運作狀態列為開放問題；ADR-0079 跑了一次 H-13，先重現 ADR-0077 的校準，讀到**是**：兩種指派下，指派配對的耦合在 512 個試次裡升了 12 到 15%、還在升，配對外的每條突觸都與映像逐位元相同，指派讀出在最後 128 個試次裡有 128 與 127 次數到的脈衝比同一試次不給獎賞時多、平均多六個；選擇也跟著反應走（106 與 115），但這只是讀數、不重開 H-12；痕跡的增強隨耦合上升而變大，ADR-0078 點名的兩個煞車機制都沒煞住；依 H-13 的停損規則，下一個決策是關於強化形式——由選擇決定傳遞——的 ADR，本輪不做；ADR-0080 做了這個決策，在任何一次跑之前寫下 H-14：就用現有的作業，獎賞送到呈現刺激連往引擎所選讀出的突觸、正負號看答對與否，在穩定後的網路上、基線歸零，跑 1 536 個試次，兩種指派下最後 128 個試次都要答對至少 80 次；試次數是從 H-13 各區塊的讀數推出來的，選錯那一對依規則永遠不會固化，隨機獎賞那一臂只用來看會不會鎖死、不設門檻；ADR-0081 跑了一次 H-14，先重現 ADR-0077 的校準，讀到**是**：兩種指派下最後 128 個試次全部答對（128/128），選擇在第 448 與第 384 個試次越過每區塊 40/64、鏡像指派先到，和 ADR-0080 事先寫的預測一致、比它估的約 900 早；答案配對的耦合升到映像的 1.35 到 1.45 倍、還在升，配對外的每條突觸——包括選錯那兩對——都與映像逐位元相同，ADR-0080 從規則推出的斷言三臂皆成立；隨機獎賞那一臂把刺激 A 鎖死在一個讀出上、答對率仍在機率水準；依 H-14 的停損規則，下一個決策是一份 ADR，在「每條突觸都會固化的基線」、「跑到一半反轉指派」、「運作狀態」、「另一個規模」之間選學習線接下來要問什麼，本輪不做；ADR-0082 先選了基線，在任何一次跑之前寫下 H-15：其他設定都和 H-14 一樣，只把基線從 0 改成 0.5（brief 027 的值，也是 ADR-0077 讓網路穩定時用的值；引擎預設是 1.0，那時獎賞沒有作用空間），讓每條突觸都固化一半的配對，另外加一臂不給獎賞的對照；不預測結果，只預測不給獎賞時會漂向讀出 1，所以答案是讀出 0 的那個刺激決定成敗；這一輪的測試放進自己的測試檔，檔名要排在 instrument 前面，才不會讓兩個重的測試檔擠進同一個分片；ADR-0083 跑了一次 H-15，先重現 ADR-0077 的校準，讀到**否**：assignment 最後 128 個試次答對 78 次、差門檻 80 兩次，鏡像指派答對 106 次；不給獎賞時四對耦合全都升到映像的 1.36 到 1.55 倍、往讀出 1 的兩對升得更快，所以兩個刺激都漂向讀出 1，和事先寫的預測一樣；獎賞只把答案配對再推高映像的 2% 到 9%、把錯誤配對壓低 0.4% 到 8.5%，跟漂移本身的偏向差不多大，所以答案是讀出 0 的那個刺激決定成敗——assignment 的 A 始終沒學到、鏡像指派的 B 較晚才到；斷言三臂皆成立，抑制性總和在二十四個區塊裡掉到映像的 0.41；H-12 旁不加任何句子；依 H-15 的停損規則第四步，下一個決策是一份 ADR，在「只由獎賞觸發固化、基線歸零」和「運作狀態」之間選，本輪不做；harness 拆成 `instrument.rs` 與新的 `everywhere.rs` 共用的一個模組；ADR-0085 選了「只由獎賞觸發固化」，並先處理它的代價：抑制規則原本也走同一個調控，基線歸零就會停擺，所以讓抑制性突觸有自己的基線、不受 dopamine 影響，興奮性突觸則仍只在有獎賞時固化；在任何一次跑之前寫下 H-16——其他設定和 H-14 一樣，只把抑制性基線設為 0.5，正好把 H-15 的「否」拆成興奮性和抑制性兩部分；ADR-0086 把這個機制做出來：抑制性基線放進調節器區段原本保留的位元組——一個旗標位元組加一個 Q16.16 的值——映像格式升到 15，沒設定時和原本的規則逐位元相同；ADR-0087 跑了一次 H-16，先重現 ADR-0077 的校準，讀到**是**：兩種指派最後 128 個試次都答對 128 次，越過每區塊 40 的門檻落在第 512 與第 256 個試次（H-14 是 448 與 384），抑制性總和和 H-15 一樣每個區塊都在掉、二十四個區塊後只剩映像的 0.41，答案配對以外的興奮性突觸逐位元和映像相同、不給獎賞時一條也沒動；所以 H-15 的「否」出在興奮性那一邊——沒有獎賞也在固化的興奮性突觸——而不是抑制規則的走勢；不給獎賞那一臂的選擇偏向兩邊都只差幾次、等於擲硬幣，所以 ADR-0085 事先寫的第二個預測讀數在刺激 A 上沒有照字面成立，照實記下；引擎學習用的設定就此定名：興奮性突觸只在有獎賞時固化、抑制性突觸有自己的基線；依 H-16 的停損規則第三步，下一個決策是一份 ADR，在獎賞預測誤差、同一次跑裡把指派反過來、運作狀態、另一個規模之間選，本輪不做；ADR-0089 選了「把指派反過來」，在任何一次跑之前寫下 H-17：同樣的設定跑 3 072 個試次，在正中間把答案的對應翻轉一次，兩個方向各跑一臂，翻轉前後最後 128 個試次都要答對至少 80 次；**事先預測是「否」**——基底核的閘門是純粹比大小、沒有任何探索，而在只由獎賞觸發固化的設定下，一對突觸只有在「被選中而且答對」時才會固化，學會之後答案讀出的計數大約是另一邊的兩倍，所以翻轉後引擎會一直選舊答案、拿不到獎賞、什麼都不會變；這一輪要量的是這個「否」有多徹底——翻轉後究竟有幾次選到另一邊、拿到幾次獎賞——那個數字就是下一個機制（在選擇裡加入探索，或在獎賞閘門下讓配對削弱）必須滿足的實測需求；ADR-0090 在任何一次跑之前先修正了 H-17 的斷言——翻轉後的第一個試次仍會把翻轉前最後一次獎賞固化進去，所以舊答案的配對要從第 1 537 個試次結束時算起才不再變動；ADR-0091 跑了一次 H-17，讀到**否**，和預測一致：兩臂翻轉前最後 128 個試次都答對 128 次、翻轉後最後 128 個試次答對 0 次，前半段和 H-16 那一臂逐位元相同；翻轉後的 1 536 個試次裡，引擎只選到另一邊 2 次和 1 次、拿到同樣次數的獎賞，舊答案讀出的計數是新答案的 2.2 到 3.2 倍（每個試次多 16 到 24 個 spike），舊答案的配對挨了 1 534 次懲罰卻完全沒動，它們的資格痕跡仍在累積每條突觸每個試次 +5 到 +10 的增強，只是調節在零被截住、一點都固化不進去；抑制性總和還在往下掉，剩映像的 0.17；依 H-17 的停損規則第四步，下一個決策是一份 ADR，在「選擇裡加入探索」和「獎賞閘門下讓配對削弱」之間選，以這一輪的讀數作為實測需求，本輪不做；ADR-0093 選了「獎賞閘門下讓配對削弱」：新增一個映像參數「有號閘門」，設定後被指定的興奮性突觸改用 clamp(基線 + dopamine, −1, 1) 固化，被懲罰時權重往資格痕跡的反方向移動，痕跡同時被消耗掉同樣的量（不這樣做會失控）；在任何一次跑之前寫下 H-18：H-17 的設定再開有號閘門，翻轉後的第二段加長到 3 072 個試次、總共 4 608 個，長度從 ADR-0091 的領先量和痕跡推出；不預測結果，只預測翻轉後舊答案那一對會一路往下掉、一開始掉最快；這個機制要由 brief 041 實作，映像格式升到 16；ADR-0094 把有號閘門做進規則、執行器和映像裡（調節器區段的 `[25]` 是它的旗標，映像格式 16；沒開的時候跟原本的規則逐位元相同）；ADR-0095 在任何一次跑之前修正了 H-18 的停損規則：格式一升，樹裡唯一一個涵蓋整個映像位元組的釘選值（H-17 映像的 CRC）一定會跟著變，所以「釘選值變了就停」指的是引擎算出來的數字和各區段的位元組，不含標頭的版本號和它的校驗碼，並且把新映像的標頭改回第 15 版重新封好，確認 CRC 仍是 H-17 讀到的那一個；ADR-0096 跑了一次 H-18，讀到**是**：兩臂在翻轉前最後 128 個試次答對 128 次，跑到第 4 608 個試次時最後 128 個試次也答對 128 次；翻轉後，舊答案的配對每次被懲罰都往資格痕跡的反方向走，翻轉後第一個區塊每次懲罰掉 9 070 到 13 310，最後回到映像的 0.95 到 1.06 倍，各個刺激在翻轉後第 14 到第 20 個區塊改選新答案，新答案的配對則長到 1.41 到 1.55 倍而且還在漲；懲罰碰到的突觸試次裡，大約五分之一因為痕跡是負的反而被增強，被削弱的稍微多一點，淨效果就是舊答案那一對的下降；前半段錯誤的配對也被壓到映像的 0.96 到 0.98 倍；ADR-0093 的三個預測讀數全部成立，斷言也成立；抑制性總和掉到映像的 0.11 倍左右，第一次停住；依 H-18 的停損規則第三步，學得會、也改得過來的這組設定就此命名，下一個決策是一份 ADR，在獎賞預測誤差、反轉的排程、運作區間和換一個規模之間選，本輪不做；F-49 已解決：§1.1 說神經組織任一時刻大約只有 1–2 % 在活動，ADR-0023 也說「靜止的單元不花任何成本」，兩句話描述的都是成本跟著活動走的執行器；但 ADR-0097 量到，執行器要等單元的每個電位都回到零才算靜止，而在增益 1.75 下，一則 0.125 的訊息就要 2 503 個 tick（25 ms）才退得乾淨，所以在 ADR-0044 的驅動下，1 024 與 4 096 單元的參考網路每個 tick 都有 99.99 % 的單元要服務，真正放電的只有 0.0018 %；附錄 A 的單元數、10 µs tick 的即時性和輸入密度三者，以現在的執行器無法同時達成。§1.1 的句子和附錄 A 的表格都沒改，只在表格下加了一段指向 F-49 的說明；整合模型——每個 tick 算每個醒著的單元，還是只在訊息抵達時才算——由 ADR-0098 決定：維持每個 tick 計算；附錄 A 的三個目標改成分兩種模式達成：接上身體的即時模式，單元數由預算決定，在 ADR-0044 的輸入密度下是 64 個 worker 約 2.6 萬個單元（Target）；沒接身體的離線模式，單元數由記憶體決定，也就是附錄 A 的 4 300 萬，但在同樣的密度下比即時慢約 1 700 倍（估算，不採計）；加速的五個手段都點名了：三個不改規則、所有 pin 都能逐 bit 不變的（按最短延遲批次推進、不再逐一排程、為 SIMD 另排積分欄位）排在學習線之前先做，兩個要改規則的（放寬靜止條件、加長 tick）暫不採用；ADR-0099 排定三個不改規則的手段的順序：先做不經 gate 的掃描（brief 043），再為 SIMD 另排積分欄位，最後做按最短延遲批次推進；每一項都要讓所有行為讀數逐 bit 不變，而且要在事先寫下的門檻內才保留：跟改動前交替各跑五次，ADR-0097 的 (a)、(d) 每 tick 時間要降到 0.80 倍以下，(b)、(c) 不能超過 1.10 倍；ADR-0100 做了第一項：每個 Worker 固定負責一段連續的單元，依排程位元圖照單元順序服務，回合裡不再有 deque、不再互相搶工作，也不再做 CAS；所有行為讀數逐 bit 不變，(a)、(b)、(d) 的每 tick 時間降到原本的 0.41、0.37、0.49 倍，但最稀疏的 (c) 反而是 1.18 倍，超過 1.10，所以依事先寫好的門檻不保留，程式碼還原、留在歷史紀錄裡；門檻之外的兩個診斷顯示，(c) 變慢是量測工具每個 tick 都把所有單元讀一遍造成的，不是掃描本身；下一步交給新的 ADR 決定，本輪不做；ADR-0101 選了第二個選項：同一份掃描原封不動重新套用，在 ADR-0097 的四組設定上、計時的 tick 之間不做普查，用 ADR-0099 原本的門檻（(a)、(d) ≤ 0.80，(b)、(c) ≤ 1.10）重新量一次，含普查的舊量法也並列重跑、一起公開；由 brief 044 執行；ADR-0102 照這個量法量完：同一份程式碼在 (a) 到 (d) 的每 tick 時間是原本的 0.517、0.527、0.745、0.643 倍，四個門檻都達標，所有行為讀數逐 bit 不變，所以掃描保留下來，公理 A3 改由所有權保證，ADR-0017 的 CAS 認領留在 `cortex-core`、執行器不再呼叫；並列重跑的舊量法在 (c) 仍是 1.156，跟 ADR-0100 讀到的一樣；只用一個 Worker 時 (a)、(c) 是 0.521、0.649，一個回合的成本就只剩積分本身（11.4 ns，積分的基準測試是 10.96 ns），這就是下一輪工作佈局要處理的需求）、開放問題。 |
| [§12 Glossary](../WHITEPAPER.md#12-glossary) | 術語表。 |
| [附錄 A](../WHITEPAPER.md#appendix-a-capacity-model) | 容量模型（是計畫，不是量測）。這張表是離線模式的計畫：一台伺服器裝得下多少，而不是能即時跑多少（ADR-0098）。ADR-0097 量出每個 tick 要服務的單元比例，並寫下單元數、即時 tick 與輸入密度三個目標必須一起滿足的預算；在 ADR-0044 的輸入密度下，離線跑這份計畫比即時慢約 1 700 倍，接上身體的即時模式則只能跑預算允許的單元數，約 2.6 萬個（Target）。表格本身沒改，F-49 已由這個決定解決。 |
| [附錄 B](../WHITEPAPER.md#appendix-b-verification-and-conformance) | 七層驗證 V-1 至 V-7（編譯期佈局斷言、spec-guard 文件對程式碼、spec-graph 文件對文件、brief 章節、manifest 依賴、PR 改動行的 mutation gate、兩種架構上的判定 pin）加上建置閘門（release profile 測試、rustdoc、MSRV、benchmark 冒煙）。 |
| [附錄 C](../WHITEPAPER.md#appendix-c-roadmap) | 里程碑 M1 至 M9，每個都有退出測試。 |
| [附錄 D](../WHITEPAPER.md#appendix-d-references) | 參考文獻。 |

## 如何在本機驗證文件與程式碼一致

```bash
cargo check --workspace --all-targets
cargo test --workspace
npm ci
npm run spec
```

`npm run spec` 會執行四個檢查：[`@descent-vtt/spec-guard`](https://www.npmjs.com/package/@descent-vtt/spec-guard) 執行白皮書與 README 中嵌入的 `<!-- @assert-* -->` 斷言，確認 `crates/` 仍然含有文件宣稱的型別、且不含被禁止的東西（`f32`、`f64`、`unsafe`、`Box<`、`Vec<`…）；[`@descent-vtt/spec-graph`](https://www.npmjs.com/package/@descent-vtt/spec-graph) 檢查所有 Markdown 文件之間的連結、ADR 狀態與開放問題是否一致。兩者都以精確版本釘在 `package.json`；第三、第四個是倉庫內的 `scripts/check-briefs.mjs`（每份 live brief 都帶有必要章節）與 `scripts/check-deps.mjs`（state crate 不宣告任何依賴，TC-2）。四者都在 CI 中作為阻斷性檢查執行。

## 如何貢獻

流程、提交訊息慣例、文件治理規則與「完成的定義」在 [`CONTRIBUTING.md`](../../CONTRIBUTING.md)；安全性回報在 [`SECURITY.md`](../../SECURITY.md)；變更紀錄在 [`CHANGELOG.md`](../../CHANGELOG.md)。給編碼代理的總機是 [`CLAUDE.md`](../../CLAUDE.md)。下一輪工作以編號的自足 prompt（brief）形式放在 [`briefs/`](../../briefs/README.md)，執行後凍結封存到 `briefs/archive/`；brief 是輸入，不是紀錄。

## 授權

Apache-2.0 OR MIT，由使用者自行選擇。
