# Architecture decision records

One file per decision, in [MADR](https://adr.github.io/madr/) format, numbered in order of acceptance. The `status:` front-matter field is read by `spec-graph`, which fails CI if a live document depends on a retired decision or if an open question is delegated to one.

To add a decision: copy the most recent file, take the next number, set `status: proposed`, open a pull request. A decision becomes `accepted` when merged with that status, `superseded` when a later ADR names it in `supersedes:`, and `rejected` if closed without adoption. Never renumber.

| ID | Title | Status |
| :--- | :--- | :--- |
| [ADR-0001](0001-64-byte-pod-records.md) | 64-byte cache-line POD records as the unit of state | accepted |
| [ADR-0002](0002-q16-16-fixed-point.md) | Q16.16 fixed-point arithmetic; no IEEE-754 on the hot path | accepted |
| [ADR-0003](0003-zero-allocation-hot-path.md) | Zero allocation and zero syscalls on the hot path | accepted |
| [ADR-0004](0004-two-tier-timing-wheel.md) | Two-tier timing wheel for axonal delay | accepted |
| [ADR-0005](0005-crate-per-subsystem.md) | One crate per subsystem; no dependencies among state crates | accepted |
| [ADR-0006](0006-virtual-actor-turn-invariant.md) | Virtual-actor turn invariant enforced by an atomic gate | accepted |
| [ADR-0007](0007-cortex-image-format.md) | The `.cortex` memory-mappable image format | accepted |
| [ADR-0008](0008-documentation-governance.md) | Documentation governance: arc42, MADR, BCP 14, executable assertions | accepted |
| [ADR-0009](0009-rust-edition-and-msrv.md) | Rust edition 2024 and a pinned MSRV | accepted |
| [ADR-0010](0010-measured-or-target.md) | Every performance figure is Measured or Target, never asserted | accepted |
| [ADR-0011](0011-epoch-based-reclamation.md) | Epoch-based reclamation for structural plasticity | accepted |
| [ADR-0012](0012-synaptic-weight-q1-15.md) | Sixteen-bit synaptic base weights are Q1.15 | accepted |
| [ADR-0013](0013-timing-wheel-geometry.md) | Timing wheel geometry: 256 × 10 µs fine, 256 × 100 µs coarse, fixed-capacity token lists (amends ADR-0004) | accepted |
| [ADR-0014](0014-benchmark-harness.md) | Benchmark harness: criterion 0.7, confined to a bench-only crate | accepted |
| [ADR-0015](0015-embodiment-frame-abi.md) | Embodiment frame ABI and single-producer single-consumer ring protocol | accepted |
| [ADR-0016](0016-thirty-two-crate-architecture.md) | Thirty-two state crates: fourteen subsystems admitted, three boundaries moved, and the admission test for the next one (amends ADR-0005) | accepted |
| [ADR-0017](0017-mailbox-and-gate-protocol.md) | Mailbox and gate protocol: an index stack drained whole, no ABA tag, four sequentially consistent operations (amends ADR-0006) | accepted |
| [ADR-0018](0018-membrane-integration.md) | Membrane integration: shift leaks with a one-LSB floor, difference coupling, a 2 ms refractory window, apical-gated plateaus, adaptive threshold | accepted |
| [ADR-0019](0019-short-term-plasticity.md) | Short-term plasticity: event-driven Tsodyks–Markram on the Q0.8 fields, exponentials by binary exponentiation | accepted |
| [ADR-0020](0020-computational-phenomenology-and-synthetic-qualia.md) | Computational phenomenology: the state variables the theories name, as integer rules, and what the document does not claim for them | accepted |
| [ADR-0021](0021-native-cognitive-language-and-conceptual-blending.md) | Native cognitive language: nested constructions, conceptual blending, default-mode wandering and dialogue grounding, without a language model | accepted |
| [ADR-0022](0022-synapse-fan-out-and-stdp.md) | Synaptic fan-out and STDP: index + 1 chains, synapse tokens, stored releases, spike messages, the nearest-neighbour pair rule at the presynaptic spike; image format 6 | accepted |
| [ADR-0023](0023-executor.md) | The executor: a runtime crate, in-house work-stealing deques, three barrier-separated phases per tick, and the one `unsafe` in the workspace | accepted |
| [ADR-0024](0024-cortex-image-and-clock-sweep.md) | The `.cortex` image: section directory, table-free CRC-64/XZ, a read-into-arenas loader and writer, a write-ahead log for the clock sweep, and the Tier-2 delta record; format version 7 | accepted |
| [ADR-0025](0025-term-arena-and-unification.md) | A term arena and first-order unification for `cortex-reasoning`: a second record under ADR-0016's test, bindings in a caller's table, a trail undone on failure, bounds that are results | accepted |
| [ADR-0026](0026-social-acumen-and-re-representation.md) | Social acumen and re-representation: a sincerity gap and a second-level expectation, tact and indirectness, an anomaly that marks a framework stale and a re-representation, a basis rotation; what is not claimed | accepted |
| [ADR-0027](0027-vocal-synthesis-and-computational-humor.md) | Vocal synthesis as motor output and computational humor: a `VocalFrame` with an integer source–filter renderer, the benign-violation appraisal, reward and the playful marker; what is not claimed | accepted |
| [ADR-0028](0028-edge-behaviour-audit.md) | Edge behaviour under audit: a gate is closed until evaluated (format version 9), a reflex is released with its override, every packed or shifted input is read within its bound, the loader refuses what the writer would never produce, quiescence includes the injector ring | accepted |
| [ADR-0029](0029-structural-enforcement.md) | Structural enforcement of the invariants that were held by review: workspace lints for `unsafe` and floats, a manifest check for dependencies, rustdoc as a gate, the arithmetic lint where it passes, pinned actions and normalised line endings | accepted |
| [ADR-0030](0030-verification-governance.md) | Verification governance: a mutation gate on the lines a change touches, a zero-dependency property kit with a lattice and a seeded walk, exhaustive tests where the domain allows, the tests in the engine's profile, a determinism pin checked on two architectures; what is not adopted | accepted |
| [ADR-0031](0031-policy-amendment.md) | Self-amendment: a policy amendment on trial in two forks of the image, committed only when the engine behaved exactly as before and cost less; the engine never amends its own code; the amendment register; image format 10 | accepted |
| [ADR-0032](0032-three-factor-plasticity.md) | Three-factor plasticity: an eligibility trace per synapse in the block, consolidated into the weight by the modulator at the presynaptic spike; the apical mask in the chain word; the engine's modulator in the executor and the image; format version 11 | accepted |
| [ADR-0033](0033-tick-duration-in-the-header.md) | The tick in the image header: its duration, refused when it is not this build's, and the clock at the write, which the loader resumes so that every stamp keeps its meaning | accepted |
| [ADR-0034](0034-synaptic-density.md) | Synaptic density: the four-synapse block stands; eight-bit logarithmic weights, eight synapses per block and an in-engine far-memory synapse tier rejected; the density levers in order, each gated by a measurement | accepted |
| [ADR-0035](0035-cadence-and-the-population-tally.md) | Multirate stepping as a cadence: a power-of-two period and a phase, a mask on the tick; the population spike tally; the idle-tick benchmark; conservative lookahead left to a measurement | accepted |
| [ADR-0036](0036-criticality-control.md) | Criticality control: the branching ratio by lag-one regression over bins of population activity in the homeostasis record, a bounded global synaptic gain as the actuator, stepped once per window and persisted in the image; per-unit scaling Specified; format version 12 | accepted |
| [ADR-0037](0037-sleep-regulation.md) | Sleep as a state machine on the window cadence: a two-process sleep pressure against a circadian threshold, three stages under an ultradian budget, a wake as an input; the homeostasis record's sleep fields; format version 13 | accepted |
| [ADR-0038](0038-episodic-ledger-and-replay.md) | The episodic ledger and its replay: an append-only second record of `cortex-hippocampus` under ADR-0016's test, tagged as an input, replayed into the network on a ripple cadence in slow-wave sleep and consolidated by the modulator's rule, depotentiated in REM; the canvas hydration Specified; sections 44 and 45 | accepted |
| [ADR-0039](0039-hypervector-body.md) | The hypervector body: a second record of `cortex-symbolic` under ADR-0016's test, 160 words of 64 bits, with the vector-symbolic algebra as integer rules over the words; no intrinsic, no `unsafe`; the width 10 240 | accepted |
| [ADR-0040](0040-categorial-reduction.md) | Categorial reduction on the term arena: a category is a term, the four combinatory rules are unifications, a greedy shift-reduce reducer with a log; type raising and a chart Specified; the runtime composes the derivation into a frame, the frame into a hypervector and back | accepted |
| [ADR-0041](0041-induction-on-the-term-arena.md) | Induction on the term arena: a definite clause is a term over a reserved functor; Plotkin's least general generalisation and the inverse-resolution operators (absorption, identification, intra-construction with a predicate invented from a reserved band) as bounded rules over caller slices, each output resolving back to its inputs by one definite-clause resolution step; first-fit matching; every failure restoring the scratch | accepted |
| [ADR-0042](0042-continued-fractions.md) | Exact continued fractions on the scratchpad: the convergents of a polynomial continued fraction as the first sequencing of slots into an expression, overflow as the slot's flag; an exact tolerance test against a rational target; a bounded coefficient search pinned by an oracle outside the tree; the meet-in-the-middle search Specified | accepted |
| [ADR-0043](0043-discovery-path.md) | The discovery path: a clause store's description length as the free energy the valence rule reads, an invention's drop as the valence and a quarter of it as the modulator's reward, composed in the runtime with no executor field; a conjecture as a prover frame, a node certified only by a completed prover frame's payload (statement hash, certificate hash); the interoceptive record stays out of the image | accepted |
| [ADR-0044](0044-reference-network.md) | The reference network: a seeded anatomical prior in `cortex-connectome`, the runtime's synthesis, a drive that is a function of the tick and a causal branching-ratio oracle by perturbation of forks with attribution through the connectome; the estimator, the controller and a night's replay measured at 256 and 1 024 units; H-8 and H-9 dispositioned at that scale | accepted |
| [ADR-0045](0045-clause-search.md) | The executive clause search: the candidate pairs and a bounded proof search in `cortex-reasoning`, the runtime's search over a clause store that commits an invention whose reward is positive; H-11's symbolic half decided; the lexicon and the cortico-striatal gating not adopted | accepted |
| [ADR-0046](0046-lexicon.md) | The lexicon: a caller's table of token ids to concept ids with a lexical shape in the runtime, categories instantiated on the term arena, a token sequence read into a frame with its markers and a frame realised into token ids with its nested clause, markers and particle; a role term that names its filler; `surface_token_id` written; the words the host's | accepted |
| [ADR-0047](0047-second-prior-and-the-estimator.md) | A second anatomical prior and the estimator's regime: the sparse random network as the same generator at its widest window; the lag-$k$ slope over a caller's series in `cortex-homeostasis`; the record's estimate cross-checked against the train and the fine-bin slopes measured beside the causal ratio on both priors; no second line of the record | accepted |
| [ADR-0048](0048-episodes-tagged-from-the-train.md) | Episodes tagged from the spike train: the densest span and the pattern that fired most in `cortex-hippocampus`, the runtime's composition with the ledger and with a rewarded invention; a night's readout of an experience's pattern, the network's own and the invention's; H-9's open item and H-11's synaptic half decided at 256 and 1 024 units; the online capture Specified | accepted |
| [ADR-0049](0049-dale-principle-in-plasticity.md) | Dale's principle in plasticity: a block's polarity is its presynaptic unit's flag, taken by the rules of `cortex-core` as an argument; the trace is the change of the weight's magnitude and consolidation keeps the weight within the polarity's half of the width; an inhibitory block takes the symmetric window of Vogels et al. 2011 with a depression per presynaptic spike from a stated target rate; F-36 resolved, the nights pinned again, the determinism pin untouched | accepted | 2026-09-14 |
| [ADR-0050](0050-the-train-inside-the-executor.md) | The spike train inside the executor: every worker's spikes of a tick merged after the tick in unit order into a bounded ring the executor owns, bit-identical on every worker count; the forks return it; the capture rules and the discovery loop composed over it in one call between ticks; the store, the affect state and the association in the image Specified with their format bump | accepted | 2026-09-14 |
| [ADR-0051](0051-the-estimator-at-4096-units.md) | The estimator at 4 096 units: the measurement of ADR-0047 on both priors at the third size as the weekly job's `exhaustive` test, the decision rule for a record line written before the run, and its outcome: no line | accepted | 2026-09-14 |
| [ADR-0052](0052-the-term-arena-in-the-image.md) | The term arena in the image and the discovery loop inside the tick: the clause store, the term nodes, the arena's counters, the affect state and the episode's binding to an invented predicate are the executor's and the image's (format 14); a commit's outputs instantiated through the bindings so the store reads without the table; the search on a cadence of the tick while awake from a cursor it keeps | accepted | 2026-09-14 |
| [ADR-0053](0053-the-waking-day-and-the-target-period.md) | The waking day and the inhibitory target period: the period a parameter of the image, not of the registry, whose behaviour gate admits no parameter that changes what the engine does (F-37); the rules taking the depression per spike as an argument; a day measured at 256 and 1 024 units under two periods; the sleep placeholders kept with the reading | accepted | 2026-09-14 |
| [ADR-0054](0054-the-causal-count-inside-the-loop.md) | The causal count inside the loop: a message bit, the unit's stamp of a synapse's last message and the descendant rule at the oracle's latency, tallied beside the spikes and measured against the oracle at three sizes on both priors under a rule written first; the count reads the input's density and not the branching ratio, so the record and the controller are untouched; the determinism pin moved for the stamp | accepted | 2026-09-14 |
| [ADR-0055](0055-a-weight-that-settles.md) | A weight that settles: the excitatory depression scaled by the weight's magnitude, so that under stationary pairing a magnitude has a fixed point where its depression equals its potentiation; the potentiation additive as it is; measured over the day at 256 and 1 024 units under a criterion written first; §8.8's per-unit gain Specified, since a gain cannot lift a weight from zero; the determinism pin moved for the depression's amount | accepted | 2026-09-14 |
| [ADR-0056](0056-a-compaction-of-the-term-arena.md) | A compaction of the term arena: the nodes the clause store does not reach reclaimed by one descending pass that marks and one ascending pass that moves, a rule of `cortex-reasoning` over its own bottom-up arena; the executor runs it between ticks on a call and at every entry into slow-wave sleep; the image unchanged | accepted | 2026-09-14 |
| [ADR-0057](0057-the-inhibitory-rule-from-below-the-rail.md) | The inhibitory rule read from below the rail: the reference generator with its inhibitory gain at 2.0, a day at 256 and 1 024 units at the two periods with the fraction of units at the target as the reading a chosen target needs, under a rule written first; no target chosen; no lane for a parameter that changes behaviour, which `CLAUDE.md`'s invariant and ADR-0031 keep with the maintainers | accepted | 2026-09-14 |
| [ADR-0058](0058-the-weekly-sweep-and-its-timeouts.md) | The weekly sweep's cost is its timeouts, not its mutants: a mutation run per area with an absolute timeout matched to that area's own tests, a completeness check that refuses a partial sweep, and the runtime's area sharded six ways by the rule that measured it | accepted | 2026-09-18 |
| [ADR-0059](0059-a-task-a-readout-and-a-reward.md) | A task, a readout and a reward: the runtime composes a two-alternative task on the executor, a stimulus drawn from the trial's index and a seed, a readout that counts a trial's spikes per set from the executor's own train and selects through `cortex-basal-ganglia`'s gating rule, and a reward whose sign is the outcome's; every mis-read and every silent no-op refused by name; the harness's state the caller's and the image format unchanged | accepted | 2026-09-19 |
| [ADR-0060](0060-the-reward-path-measured.md) | The reward path measured: the task on the reference network at 256 and 1 024 units under a criterion written before the run, against a shuffled reward, a fixed modulation, the mirrored assignment and a second worker count; the accuracy at chance in every block of every run, so the rewarded clause fails and no constant moves; the reason located in the readout's window and the pairing statistics, not the modulator; what a next round would change named and not built | accepted | 2026-09-19 |
| [ADR-0061](0061-the-learning-runs-leave-the-gate.md) | The learning runs leave the gate: brief 027's five full runs at 256 units become weekly `exhaustive` tests and the gate keeps the rewarded run's first block, because a run in the gate is paid again by every runtime mutant of the weekly sweep | accepted | 2026-09-20 |
| [ADR-0062](0062-the-first-complete-sweeps-list.md) | The first complete sweep's list: a triage rule that tells an inherent timeout from a directive-class one, thirty-six timeouts classified and four loops made ranges, twenty-one survivors dispositioned by a test, a shape or a named exclusion, and one timeout that was a slow pass starved by the hang beside it | accepted | 2026-09-20 |
| [ADR-0063](0063-the-sweep-reads-its-own-timeouts.md) | The sweep reads its own timeouts: the weekly job writes, beside `timeout.txt`, when each timeout ran, what ran in the shard's other slot and whether its test run was still finishing tests, from the artifact it already keeps; it reports and does not classify, because progress on another thread is not progress toward a verdict (F-42) | accepted | 2026-09-20 |
| [ADR-0064](0064-the-documentation-gate-and-the-version.md) | The documentation gate is one command and the whitepaper's version is checked: CI runs `npm run spec` rather than a list of steps that drifted from it (F-44), the document's two version declarations must agree (F-43), and a pull request that changes the document moves its version | accepted | 2026-09-20 |
| [ADR-0065](0065-the-instrument-recalibrated.md) | The instrument recalibrated: a set as a periodic pattern of units so that a stimulus is units spaced beyond the prior's local window and fires each once, a readout window derived from the prior's delay band, a trial of one time constant of the dopamine signal, and a calibration with the weights frozen that shows the readout sees the stimulus before any reward is given (1.75 fails and 2.0 passes at 256 units, 1.75 passes first at 1 024); every constant committed before the first rewarded run; format 14 | accepted | 2026-09-20 |
| [ADR-0067](0067-the-weekly-dispatch-has-a-scope.md) | The weekly dispatch has a scope: a round runs the exhaustive tests, where its own measurement lives since ADR-0061, and runs the whole-tree sweep beside them only when it added or removed a test, moved one between the gate and the weekly job, or changed the sweep itself; a scheduled run always does both | accepted | 2026-09-20 |
| [ADR-0066](0066-the-reward-path-measured-again.md) | The reward path measured again, through the calibrated instrument: at 256 and 1 024 units under a criterion over the last 128 trials written first, the rewarded run reads 53 and 49 correct against the 80 asked for, the mirrored assignment 53 and 65, the controls hold and the run is the same on one worker and on four; the instrument resolves the stimulus from the background but not one stimulus from the other, every coupling falls alike under every feedback, and at 256 units the resolution is lost within a block once the weights move; no constant moves, and what a next round would change is named | accepted | 2026-09-20 |
| [ADR-0068](0068-the-reward-addressed.md) | The reward addressed: the modulation a synapse consolidates under is a function of both its ends, the dopamine term reaching the synapses from the units of the stimulus presented onto the units of the readout the engine selected and every other synapse consolidating under the baseline alone; two flags per unit and a modulation per slot in the fan-out, the rule, the records and the image unchanged, a run that addresses every unit ADR-0066's bit for bit; written before any rewarded run, what the delivery can do on a two-alternative task with a symmetric reward | accepted | 2026-09-20 |
| [ADR-0069](0069-the-addressed-reward-measured.md) | The addressed reward measured: the instrument, the controls, the seeds and the criterion of ADR-0066 with the delivery of ADR-0068 the one variable; at 1 024 units the addressed rewarded run reads 56 correct of the last 128 and the mirrored assignment 65 against the 80 asked for, the addressed shuffled reward 53, the fixed modulation 60 and the global form 49 (ADR-0066's, reproduced), the run the same on one worker and on four, so the rewarded clause fails with the global form reproduced; the couplings from a stimulus into its rewarded readout part from the other two by three to five per cent in the direction the trace's sign set, and the behaviour reads nothing of it; at 256 units the instrument is below the mark from the first block; no constant moves, and what a next round would change is named | accepted | 2026-09-20 |
| [ADR-0070](0070-where-256-units-settle.md) | Where 256 units settle: the excitatory sum read over eighty windows under the drive alone on the instrument's executor; brief 026's clause first holds at the ninth window at 0.818 of the prior's and the sum is still falling at the eightieth at 0.551, by 0.24 per cent per window, so the clause reads a rate and the item is discharged with both numbers; behind the lead-in of nine windows the rule derived, the calibration's measure reads 50 of 64 in the first block and 34 to 44 after, so 256 units is not usable for this task behind a lead-in of any length and a learning round's criterion stays at 1 024 units; no constant moves | accepted | 2026-09-21 |
| [ADR-0071](0071-the-exhaustive-job-times-its-own-binaries.md) | The whole-domain tests report what each binary took: one duration for thirty-seven `exhaustive` tests across four binaries could not say what a round had added — seven runs read 32 to 71 minutes while about twenty-one heavy runs were added, and two runs of identical content read 40 and 71 — so the job reads the seconds cargo already prints per binary into the summary and an artifact, and sharding it stays a decision for the round that has the numbers (F-45) | accepted | 2026-09-21 |
| [ADR-0072](0072-what-the-trace-is-made-of.md) | What the trace is made of: the eligibility on a stimulus's synapses into a readout, read from the record at 1 024 units with the weights frozen and split by what paired it through an oracle that replays the pair rule over the executor's own train and agrees with the record at every reading; the volley's pairing is net potentiation and the background's net depression of four to five times it, so ADR-0066's sentence is a measurement, and its mechanism is the count of a stimulus unit's spikes within the pair window after its volley (F-46); a ladder of gains below the present one passes at no rung under the sight and the sign, so there is no rewarded run and no constant moves | accepted | 2026-09-21 |
| [ADR-0073](0073-the-whole-domain-tests-sharded.md) | The whole-domain tests run in three shards: the first per-binary table showed one job at 1 h 43 m of its 120-minute bound with half of it in `instrument.rs`, so the shards are taken from the binaries `--list` names at run time rather than from a list written down, each checks what it ran against what the list counted, and the floor — the largest single binary — is named rather than hidden | accepted | 2026-09-21 |
| [ADR-0074](0074-a-stimulus-that-fires-once.md) | A stimulus that fires each unit once, the one change F-46 names, tried as one message at the unit's threshold, one of 1.25 and one of 1.125, each read over a frozen run at 1 024 units and the gain 1.75 by a measure written first; the executor scales an injected message by the synaptic gain (F-47), no candidate fires every unit once under the drive, the trace stays net depression under each and ADR-0072's hypothesis of +5 is not met, so there is no rewarded run; what the criterion requires is read off the trials: one to three spikes per trial on the answer readout | accepted | 2026-09-21 |
| [ADR-0075](0075-the-dispatch-scope-follows-the-diff.md) | The dispatch's scope follows the diff: a round sweeps the whole tree only when its actual change can have left a mutant uncaught — source under `src/`, a test removed or moved out of the swept suite, the sweep's own configuration — decided by the executing round from its diff rather than written into the brief before the diff exists; adding a test never qualifies, because an added test can only catch more, and three of the four measuring rounds since ADR-0067 swept a tree they had not touched | accepted | 2026-09-21 |
| [ADR-0076](0076-two-injections.md) | Two injections: F-46's drive kept whole and a cancel, a negative basal message into the same units inside the trial, as a field of the task's stimulus with its refusals and a bit-for-bit pin; the brief's offset lands inside the refractory window, which the membrane rule drops, and its sizes are too small for the soma the coupling primed, so the cancel is derived from the rule — the tick after the window, over the nine ticks the volley spreads, six messages at the bound per tick — and held to the engine's probe; under it every unit fires once and not again, the volley's depression goes to nothing and the background's halves, the volley's potentiation halves too because the response measured before was to three volleys, and the terms sum to −2.0 per synapse per trial against the +5 hypothesised: the sight passes, the sign reads 13 of 64, there is no rewarded run, and H-12's stopping rule records the stimulus side as exhausted | accepted | 2026-09-21 |
| [ADR-0077](0077-the-background-side.md) | The background side, step 2 of H-12's stopping rule: the network settled before the task under the gain held (ADR-0055's criterion as an integer rule, held at the ninety-sixth window at 0.925 of the prior's with the readout units' rate the prior's) and the criticality controller on at its step (settled at the thirty-fifth at 0.453, the gain swinging between 2.2 and 2.8 and the readouts seven to twenty-four times louder, as predicted), each alone, run quiet, saved as an image with the baseline at zero and read over a frozen run by three measures written first; the settled network fires once, is seen at 62 of 64 and its trace is net potentiation on average for the first time at a gain at which the readout sees, but the sign reads 53 against 56; the controller fails all three; no candidate passes, there is no rewarded run, and by the rule's step 3 H-12 closes as a no for this task in this regime, the three routes named and not built | accepted | 2026-09-22 |
| [ADR-0078](0078-the-rewards-direction.md) | After H-12, the reward's direction before a task's difference: of the three routes H-12's stopping rule named, the task that asks for a sign is chosen and written as H-13 — on the network ADR-0077 settled, with the baseline at zero, a reward delivered after every trial to the synapses from the presented stimulus onto its assigned readout raises those couplings and the response the engine reads from that readout, against the same trials with the reward withheld (a sign test on paired trials, 80 of the last 128, in both assignments) — with its criterion, its prediction and its own stopping rule written before any run; the structural rule deferred for want of a measured need; the operating regime recorded as the open question a no on H-13 makes the next decision | accepted |
| [ADR-0079](0079-the-rewards-direction-measured.md) | The reward's direction, measured: H-13 is yes — on the network ADR-0077 settled, with the baseline at zero, a reward delivered after every trial to the synapses from the presented stimulus onto its assigned readout raised those couplings by 12 to 15 per cent over 512 trials, still rising, and that readout's response above the same trial's with the reward withheld in 128 and 127 trials of the last 128, by six spikes per trial, in both assignments, every synapse outside the pairs bit for bit the image's; the calibration reproduced ADR-0077 first, the oracle replayed the consolidation and agreed with the record at every trial, the trace's potentiation grew as the couplings rose and the two mechanisms named as bounding it were outrun; the next decision is an ADR on the reinforced form, not taken there | accepted | 2026-09-22 |
| [ADR-0080](0080-the-reinforced-form.md) | The reinforced form, H-14 written before any run: on the network ADR-0077 settled, with the baseline at zero, the task as ADR-0059 and ADR-0068 built it — the reward addressed to the synapses from the presented stimulus onto the readout the engine selected, its sign the outcome's — asked to move the selection to the answer, 80 of the last 128 in the assignment and the mirrored assignment, over 1 536 trials, a length derived from H-13's blocks before any run; a wrong selection's pair shown by the rules never to consolidate, the shuffled reward read for lock-in rather than bounded by a clause, the relation to H-12 and the stopping rule written first | accepted |
| [ADR-0081](0081-the-reinforced-form-measured.md) | The reinforced form, measured: H-14 is yes — on the network ADR-0077 settled, with the baseline at zero, the task as built moves the selection to the answer in 128 of the last 128 trials in the assignment and in the mirrored assignment over 1 536 trials, the selection passing 40 of 64 per block by trial 448 and 384, the mirrored first, as predicted and earlier than estimated; the answer couplings rise to 1.35 to 1.45 of the image's and are still rising, every synapse outside them — the wrong pairs among them — bit for bit the image's, ADR-0080's derivation true on every arm; the shuffled reward locks one stimulus onto one readout; the calibration reproduced ADR-0077 first; H-14's stopping rule at step 3, the note written beside H-12 and the next decision named and not taken | accepted |
| [ADR-0082](0082-plasticity-everywhere.md) | Plasticity everywhere, H-15 written before any run: H-14's configuration with the modulation baseline at 0.5 instead of zero — brief 027's, the baseline ADR-0077's lead-in settled under and H-12's rewarded runs used — every other constant H-14's; 80 correct of the last 128 in both assignments; the drift the unrewarded consolidation brings predicted from ADR-0077's terms to favour readout 1 and read in a withheld arm; the note beside H-12 for a yes and the stopping rule written first; the runs in a test binary of their own, named so that the round robin leaves `instrument.rs` alone in its shard | accepted |
| [ADR-0083](0083-plasticity-everywhere-measured.md) | Plasticity everywhere, measured: H-15 is no — on the network ADR-0077 settled, with the modulation baseline at 0.5, the task as built moves the selection to the answer in 106 of the last 128 trials in the mirrored assignment and in 78 in the assignment, two short of the mark; the unrewarded consolidation carries every coupling up by a third to a half of the image's and faster onto readout 1, the withheld arm drifting there for both stimuli as predicted, the reward parting an answer pair from that course by two to nine per cent, the stimulus whose answer is readout 0 deciding each arm as predicted; the assertion true on every arm, the calibration reproduced ADR-0077 first, the inhibitory sum at 0.41 of the image's after the run; H-15's stopping rule at step 4, nothing beside H-12, the next decision named and not taken; the harness shared as one module between `instrument.rs` and a binary of its own |
| [ADR-0084](0084-the-shards-take-tests.md) | The shards take tests, not binaries: `instrument.rs` read 61 per cent of its shard's bound with nothing added to it, so every binary's tests are spread over the three shards, each binary's share run in one process with its tests as exact filters read from `--list` at run time; a listing that fails or names nothing now fails the shard (F-48); ADR-0082's rule on a binary's name superseded | accepted |
| [ADR-0085](0085-inhibition-off-the-reward-gate.md) | Inhibition off the reward's gate: of H-15's two routes, consolidation gated by the reward is chosen and its named cost taken first — an inhibitory synapse may consolidate under a baseline of its own, a parameter of the image (format 15), while every excitatory synapse stays under the reward's gate; written as H-16 before any run, H-14's configuration with that inhibitory baseline at 0.5, which splits H-15's no into its excitatory and inhibitory parts; the reward-prediction error and the regime named for after it | accepted |
| [ADR-0087](0087-inhibition-off-the-reward-gate-measured.md) | Inhibition off the reward's gate, measured: H-16 is yes — on the network ADR-0077 settled, with the modulation baseline at zero and the inhibitory baseline at 0.5, the task as built moves the selection to the answer in 128 of the last 128 trials in both assignments, the crossing at trials 512 and 256 beside H-14's 448 and 384, while the inhibitory sum falls in every block to 0.41 of the image's as under H-15 and every excitatory synapse outside the two answer pairs ends the run the image's bit for bit; H-15's no put on the excitatory side; the withheld arm's lean a coin either way, so ADR-0085's second predicted reading did not hold as written for A; the calibration reproduced ADR-0077 first; H-16's stopping rule at step 3, the configuration the engine learns in named and the next decision named and not taken | accepted | 2026-09-23 |
| [ADR-0086](0086-the-inhibitory-baseline-built.md) | The inhibitory baseline built: ADR-0085's parameter in the modulator section's reserved bytes — a flag byte at `[24]` and a Q16.16 value at `[28..32)` — the image format moved from 14 to 15, unset encoded as the zeros a format-14 writer left so that its record reads as unset while its header is refused as every foreign version is; the configuration taking an `Option`, a third modulation published beside ADR-0068's two and picked by the block's polarity in the fan-out; unset, every pinned number and the determinism pin stand bit for bit | accepted | 2026-09-23 |
