admissible: no (Windows developer laptop; no core isolation; boost not fixed; workers unpinned; not the reference platform of whitepaper §7.1)
commit: the working tree that became ec49124 (pull request #46, brief 018), at measurement time
toolchain: rustc 1.97.1 (8bab26f4f 2026-07-14), criterion 0.7.0
platform: AMD Ryzen 7 7735HS (8 physical / 16 logical cores, 3.2 GHz base, boost enabled), 31 GB RAM, Windows 11 10.0.26200
isolation: none (interactive desktop session; worker threads unpinned, scheduled by Windows)
frequency: not fixed
command: cargo bench -p cortex-bench --bench hot_path --locked -- "executor/" --warm-up-time 1 --measurement-time 3
samples: 1 s warm-up, 3 s measurement, 100 samples per benchmark

# Result — 2026-09-12, developer machine, NOT ADMISSIBLE

The subject of whitepaper §11.1's conservative-lookahead question ([ADR-0035](../../adr/0035-cadence-and-the-population-tally.md)): the tick that does nothing, on one, two and four workers. These figures must not be cited in whitepaper §10.2 and carry the words "not admissible" wherever they are quoted.

```text
executor/push_to_turn   time:   [184.87 ns 191.40 ns 197.96 ns]
executor/idle_tick/1    time:   [49.372 ns 51.523 ns 53.634 ns]
executor/idle_tick/2    time:   [537.82 ns 570.41 ns 602.66 ns]
executor/idle_tick/4    time:   [1.6641 µs 1.6804 µs 1.6997 µs]
```

Observations, for whoever runs the admissible version:

- On one worker no thread waits at the barrier (the party count is one), so 52 ns is the
  coordinator's own work per tick: the modulation and the gain published, four barrier calls
  that return at once, the tally.
- The two- and four-worker figures are the four barrier waits with unpinned threads on a
  desktop scheduler: about 570 ns and 1.7 µs per tick, a tenth to a sixth of the 10 µs tick.
  They say nothing admissible about the reference platform (pinned, isolated cores, 64
  workers), which is the measurement the §11.1 question waits for; they do say the cost grows
  with the worker count on this machine, which is why the question is open and not closed.
