admissible: no (Windows developer laptop; no core isolation; boost not fixed; not the reference platform of whitepaper §7.1)
commit: 286012a (main) plus the uncommitted tree of pull request #10 at measurement time; the merged hash is in CHANGELOG.md
toolchain: rustc 1.97.1 (8bab26f4f 2026-07-14), criterion 0.7.0
platform: AMD Ryzen 7 7735HS (8 physical / 16 logical cores, 3.2 GHz base, boost enabled), 31.2 GB RAM, Windows 11 10.0.26200
isolation: none (interactive desktop session)
frequency: not fixed
command: cargo bench -p cortex-bench --bench hot_path
samples: criterion defaults (3 s warm-up, 5 s measurement, 100 samples per benchmark)

# Result — 2026-09-10, developer machine, NOT ADMISSIBLE

These figures exist to exercise the results convention and to give a developer a rough scale.
They must not be cited in whitepaper §10.2 and carry the words "not admissible" wherever they are
quoted. The single-call benchmarks include roughly one LCG draw per iteration (about 1 ns).

```text
wheel/advance_empty                       time:   [1.5460 ns 1.6170 ns 1.6926 ns]
wheel/schedule_then_advance/both_rings    time:   [9.4592 ns 9.6140 ns 9.7627 ns]
wheel/schedule_then_advance/fine_ring     time:   [5.5684 ns 5.7902 ns 6.0143 ns]
efficacy/single                           time:   [2.8163 ns 2.8430 ns 2.8715 ns]
efficacy/batch_1024                       time:   [1.2319 µs 1.2402 µs 1.2491 µs]   (1.21 ns per element)
gating/compute_gating                     time:   [2.2795 ns 2.3124 ns 2.3456 ns]
ignition/step_ignition_x64                time:   [81.159 ns 81.723 ns 82.309 ns]   (1.28 ns per step)
```

Observations, for whoever runs the admissible version:

- `schedule_then_advance/both_rings` is about 3.8 ns slower than `fine_ring`; the difference is
  the coarse path (a division by 10 and the residual pack) plus the cascade amortised over ten
  ticks. That is the cost ADR-0013 accepted for a 25.6 ms horizon.
- The batched efficacy figure is 2.3× faster per element than the single-call figure, which is
  the generator and `black_box` overhead in the single case, not the arithmetic.
- No `perf stat` counters: not available on this platform.
