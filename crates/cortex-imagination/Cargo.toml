[package]
name = "cortex-imagination"
description = "Mental canvas frames for counterfactual rollouts that never drive the motor channel for VirtualCortex"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
